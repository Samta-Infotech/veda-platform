# inference/ (ASGI query/retrieval service — warm-loads BGE-M3 (dense + sparse),
# the reranker, the 5-signal engine, and veda_core) — pip install -r requirements/inference.txt
#
# ML deps carried over verbatim from the POC's requirements.txt, plus the ASGI/serving
# stack needed to run inference as a standalone process (migration_plan.md §1.3, §8).

numpy<2
torch
transformers<5   # FlagEmbedding 1.2.10's BiTrainer does `from transformers.trainer import *`
                 # expecting Optional etc. to be re-exported — gone in transformers 5.x, which
                 # crashes EVERY query (NameError: Optional) as soon as the retrieval engine
                 # cold-inits its sparse ranker. Unpinned == whatever's latest on rebuild day.
torch-geometric
sentence-transformers
FlagEmbedding==1.2.10   # WP3: BGE-M3 dense + learned-sparse (pulls peft)
scipy==1.11.4           # WP5: PPR transition matrix (scipy.sparse CSR)
scikit-learn
dateparser

fastapi
uvicorn[standard]
anyio
psycopg2-binary
redis
langgraph
langchain-core
sqlglot
