# apps/ (Django API/DRF/Celery orchestration tier) — pip install -r requirements/api.txt
#
# Deliberately thin (migration_plan.md §1.3): NO torch/transformers/sentence-transformers
# here. This image is the api/worker/beat tier and must stay small — all ML/inference
# deps live only in requirements/inference.txt (docker/Dockerfile.inference).

Django>=5.0,<5.1
djangorestframework>=3.15
# JWT login/refresh/logout (apps/authentication). [crypto] pulls `cryptography`,
# needed only if a deployment switches SIMPLE_JWT["ALGORITHM"] to RS*/ES* — the
# default HS256 signing does not require it.
djangorestframework-simplejwt[crypto]>=5.3
django-environ>=0.11
celery>=5.4
redis>=5.0
psycopg2-binary
gunicorn>=22.0
django-cors-headers
langgraph
langgraph-checkpoint-redis
