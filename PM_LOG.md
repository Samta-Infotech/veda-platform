# PM LOG — Task Tracker

One row per completed task, **newest first**. This is an **index, not a narrative**:
each row says what was done, whether it was verified, and *where the detail lives*.
The technical story stays in the domain logs and is never restated here.

**Maintenance rule:** append a row as part of finishing a task, not later.

| Detail doc | Domain |
|---|---|
| `RBAC_PROGRESS_LOG.md` | authentication / authorization / RBAC (api tier) |
| `VEDA_PROGRESS_LOG.md` | query engine, retrieval, understanding layer |
| `AUTH_API_CONTRACT.md` | frontend contract — identity verification (login/refresh/logout) |
| `ACCESS_MANAGEMENT_API_CONTRACT.md` | frontend contract — user administration + RBAC |
| `AUTH_ISSUES_BACKLOG.md` | open findings — auth (`C/H/M/L/N`) **and** RBAC (`R1`–`R7`) |
| `VEDA_RBAC_ARCHITECTURE_REVIEW.md` | formal audit #1 (2026-08-06, 6.0/10) |
| `VEDA_RBAC_AUDIT_2.md` | formal audit #2 (2026-08-06, 6.5/10) |
| `docs/adr/` | architecture decision records (RBAC resource path, …) |

**Verification levels** — used literally, never inflated:
`code` = written + reviewed · `local-test` = automated tests pass on a local/sqlite
harness · `probed` = verified by direct out-of-band experiment · `live` = run against
the real Postgres/PgBouncer + redis-cache stack · `frontend` = exercised by a client.

---

## Open blockers

Full list with fix sketches: **`AUTH_ISSUES_BACKLOG.md`**.

| ID | Blocks | Item |
|---|---|---|
| H2 | **enabling `VEDA_JWT_AUTH`** | `token_blacklist` retention is prose, not code (~96 rows/user/day). Needs a Celery beat entry for `flushexpiredtokens`. |
| H3 · M1 · M3 | enabling | duplicate `auth_user` SELECT per refresh (+ a comment that denies it); revocation over-scan; a provably wrong docstring |
| M4 · M5 · M8 · N1 | calling it production-grade | token in JS-readable storage; signing key == `SECRET_KEY`; no queryable audit trail; distributed brute force bounded only by per-IP throttles |
| Q1 · Q2 | consistent API | 401/403/429 and 500s escape the platform envelope (DRF `{"detail":…}`); no `EXCEPTION_HANDLER` configured |
| — | **enforcement is off** | Gate 2 exists but `VEDA_RBAC_MODE=off` by default. Run **shadow** in a real environment before `enforce`, or every unprovisioned user is denied at once |
| — | **data-path authorization** | Gate 2 covers the **admin surface only**. `/api/v1/query` — the endpoint that actually reaches customer data — is still `AllowAny`. That is Gate 1 |
| — | catalog freshness | Discovery is manual (`manage.py sync_catalog`); not triggered by ingestion, so the catalog goes stale after every re-ingest |
| — | production | Nothing has been run `live` or against a `frontend` yet |
| — | **`bootstrap_admin` in any real environment** | `apps/chat/migrations/0002_seed_dummy_admin_user.py` seeds a permanent `username=admin` user into every migrated DB (dev + prod), self-documented as removable "once the real authentication service replaces LoginView" — that already happened. Doesn't break bootstrap (dummy is never staff), but is a pre-existing dev credential sitting in prod. Decision needed: remove the migration, or leave it for `apps/chat`'s dev-fallback identity |
| — | **name-based Admin-role identity — mostly closed** | `admin_guard.ADMIN_ROLE_NAME = "Admin"` (a hardcoded string) is how `bootstrap_admin`, `UserRoleService.revoke()`, and `get_admin_role()` all identify the protected role. T50 found renaming it via `roles/update` silently broke `revoke()`'s guard; T55 fixed this at the source — `roles/update`/`roles/delete` now refuse to rename OR deactivate/delete the Admin role at all (`AdminRoleProtected`), so the rename-then-break scenario can no longer occur through the API. Still name-based (not a dedicated flag column — user's explicit call), so a direct DB edit could still break it, but that's outside the API surface this guards |

---

## Tasks

| # | Date | Task | Status | Verified | Detail |
|---|---|---|---|---|---|
| T74 | 2026-09-22 | **A row cap nobody asked for was deciding the answer** — user asked why a refined count came back as `SELECT 8 columns ... LIMIT 100` with "86% of the assets listed are in Pune", then directed: cap the TABLE if needed, but do not mislead the chart and summary. **(A)** the cap was in THREE places, not one: `generation.py`'s own `_DEFAULT_ROW_LIMIT = 100` (beside config's unused `SQL_DEFAULT_LIMIT = 1000`), a silent `+ " LIMIT 100"` in `validation.py` applied at the last step before execution, and `execution.py`'s `fetchmany(EXECUTION_RESULT_LIMIT=1000)`. Removing the first alone changed nothing — the second put it straight back, which is why the first fix measured as useless. First two removed; a limit the USER asked for is untouched. **(B)** it was never a display cap: everything the reader is told is derived from the rows returned, so the page became the population — measured on assets_asset (7,814 rows), capped runs asserted "There are 1000 assets listed" and "The average carpet area is 1628.46 square meters", and the original "86%" (on a query whose WHERE already restricted every row to Pune, so the true share is 100%) is the same defect with a percent sign. **(C)** three enforcement gaps closed, each found only by running it live after the previous fix looked complete: the prompt already said "never state a percentage or proportion" and was ignored (now a deterministic guard into the existing fallback); `result_analyzer` FEEDS shares and means in as "Verified findings", knowing nothing of truncation, so the model was told never to state a share one line under a block of shares (dropped at source, which also stops `blend_patterns` re-appending them); and averages leaked back through that same blend after proportions were filtered. Ranges and outlier VALUES still pass — they occur in the data. **(D)** a user-requested limit is no longer treated as truncation, or every "top 5" answer would have been stripped of correct figures. | Done | 35 new tests; 187 summariser tests pass. Live: truncated lists now answer "Returned 1000 row(s)" with no derived figure, `top 5`/`first 20` keep theirs, `how many assets` still answers 7,814. `test_rag_layer_events.py` has 1 failure — verified pre-existing by re-running with these changes reverted. | `veda_core/veda/generation.py`, `veda_core/veda/validation.py`, `veda_core/query/result_explainer.py`, `tests/test_summary_truncation_proportions.py`. **A regression I introduced and caught:** removing the SQL LIMIT blinded `_sql_truncated`, which reads the SQL text — a result cut by the executor's fetch was no longer flagged, silently disabling every guard downstream; detection now also consults `EXECUTION_RESULT_LIMIT`. **Still open:** the CHART gets no truncation signal (its subtitle still reads "First 50 of 1000 rows", where 1000 is itself the page); and we stop misleading without telling the truth — a `COUNT(*)` on truncation would let the summary say "1000 of 7,814" instead of "Returned 1000 row(s)". **Ruled out by measurement:** the engine cannot do count+filter on this source at all (asked plainly it REFUSES honestly; the same inability reached through a refinement fabricates), and rendering the operation into the resolved query produced byte-identical SQL, so none of this was fixable from the chatbot layer. The original bad answer is also served from `substrate_verifiedquerycache` (`_from_cache: True`), so no engine fix reaches it until that entry is invalidated. |
| T73 | 2026-09-22 | **Document follow-ups stayed in their document, and a real question stopped being refused as chit-chat** — **(A)** after an answer from `maintenance_policy.docx`, "and what about response times" was answered from the employee HANDBOOK, about absence time tracking, and the frame followed it there; the handbook holds 165 of 179 chunks, so an unanchored follow-up drifts to it on volume alone. Follow-ups now name their document. This reversed an earlier decision of mine not to append the file name (the fear was "April 2026" being read as a date) — measured false: no date filter, 67s→26s, and the answer went from "the context does not specify" to a cited "Yes, it is paid". **(B)** the first version of that fix keyed on `delta_type`, passed 27 unit tests and did NOTHING live: measured over 6 document turns, `delta_type` is `new_topic`/`ambiguous` on ALL of them, continuations and new questions alike, because the delta block reasons about filters and slots and a document frame has neither. Re-keyed on `classify_node`'s existing `action` label — no extra model call. Errs toward under-anchoring: 2/2 new questions unanchored (0/3 trapped in the control), 2/3 continuations caught. **(C)** the frame still moved, separately: entity comes from `datasets[0]` and the engine listed the handbook first even when the answer was about the maintenance policy. A follow-up now keeps its document if that answer actually READ it (evidence-only, gated on the same referential signal). **(D)** the biggest find, and not a document bug at all: with conversation history present, "what is the dress code" and "how long is the probation period" were classified `smalltalk` 3/3 and answered "I'm here for questions about your data" — the question never reached the engine. My first A/B blamed the document frame; it moved two variables at once (a fresh session has no history either). Isolated properly: **history** is the trigger, frame changes nothing across none/document/relational. Cause was the HARD RULE enumerating RELATIONAL nouns, so a question naming none of them fell off the list. Adding document nouns would only move the cliff — the rule now states the invariant (asking for information is never smalltalk; smalltalk requests nothing). **(E)** that rewrite cost one row: "more details" had been resting on the model calling it smalltalk, not on the ungrounded-referential backstop, whose regex requires the referential word to be last. Backstop widened structurally (very short + referential + no data hint, and only when NO frame exists), no word list. | Done | Live on the real engine; scenarios **54/54** in `--mode full` (baseline held), corpus 299/299, suite 351/354, deterministic harness clean, 467 unit tests. Prompt A/B both directions: 28/30 → **29/29**, and 47/48 acknowledgement phrasings still smalltalk (`ic` verified identical under BOTH rules, not a regression). | `chatbot/memory/frame.py`, `chatbot/nodes.py`, `chatbot/prompts/supervisor.py`, `tests/test_document_anchor.py` (56 tests). **Open:** the `identity` family (`what are your capabilities`, `what should i ask you`, `give me some example questions`) reaches the engine — 2 were already open, (D) made it 3; these need the bot to answer for itself, a separate action type. Left visible in the harness, not filed as a KNOWN_GAP. Also seen and not chased: the SLM host's DNS fails intermittently (25 failures in one run), and a failed call degrades to `answer`. |
| T72 | 2026-09-21 | **Shape phrases on a document answer, and the suite finally covers the document path** — **(A)** `"make it top 10"` asked of a retrieval answer cost 19 s at the engine and came back "The provided context does not contain information about a 'top 10' list" — the same failure shape as `"go back"` (T71), and permanent on that path because a RAG answer has no row limit, grouping or ordering and never will. Added `matches_shape_phrase`, which answers "is a different shape being ASKED for?" separately from `detect_shape_delta`'s narrower "and is there a slot to change?" — those need different answers where there are never any slots. A shape phrase on a document frame is now settled from memory in 0 s with no model call, naming the document and saying there is no ordering or row count to change. SQL frames are untouched: `make it top 10` against a frame holding limit 100 still returns replace/limit/10. **(B)** the 366-message suite was entirely SQL-shaped, so this whole path could regress unseen. Added a `DOCUMENT_FRAME` fixture (entity + route and nothing else — the permanent shape of memory on a filesystem source), 15 real document questions taken from `chat_chatmessage` that MUST reach the engine, and 12 shape/drill phrasings that must NOT. Suite now 393 messages. Two harness bugs surfaced while wiring it: de-duplication was keyed on the message alone, which silently dropped every document case sharing a phrase with the SQL suite (now keyed on message AND frame, since the same words are a legitimate shape change in one state and meaningless in the other), and `KNOWN_GAPS` matching broke with that change until it was pointed at the message half of the key. A third was caught by the tests themselves: the pytest wrapper passed only ACTIVE_FRAME, so every document row fell through to the model and failed as _SlmForbidden while the CLI passed — exactly the drift that wrapper exists to prevent | ✅ done | `code` + `local-test` (10 conversation-layer files pass, 238 thinking-steps; offline harness corpus 299/299 + scenarios 42/42 + suite 267/267 with 20 recorded gaps; gate 54/54 against the real 7B) | `chatbot/memory/frame.py` (`matches_shape_phrase`), `chatbot/nodes.py` (`classify_node`, `recall_node`), `evaluation/conversation/{build_suite.py,run_eval.py,suite.jsonl}`, `tests/test_conversation_eval.py` |
| T71 | 2026-09-21 | **Aggressive memory test on the DOCUMENT path — three bugs found and fixed** — 17 turns across both sources against the real engine, exercising frame build, context carry, all three recall kinds, re-present, acknowledgement survival, new topic, drill, shape, reset and per-source isolation. **Per-source memory proved itself here**: a SQL turn on source 2 mid-conversation left source 3's document frame completely intact (turn 9 -> 10). Three real defects: **(1) `"go back"` cost 27 SECONDS and reached the engine**, coming back "The provided context does not contain the answer to the question 'go back'" — a retrieval pipeline asked to find a navigation word in a PDF. The deterministic drill-up path requires a drill stack, and a document conversation never builds one, so this was its PERMANENT state. Now answered from memory in 0 s with "There's nothing to go back to", and NO model call. **(2) `"what did i ask earlier"` answered with a description of the ANSWER** — "Your last question was: Answered from Samta-Employee Handbook April 2026 using 5 relevant passages". `understanding` means different things per head: a restatement of the QUESTION on SQL, a description of the PROCESS on retrieval, and recall read the same field for both. Document recall now quotes the user's own words from the episodic buffer, which already stores them verbatim; SQL recall is unchanged because there the engine's restatement is genuinely better than the raw words. **(3)** the document name printed twice — `"X (X)"` — because a document frame's entity IS its display name. **One existing test was updated rather than worked around**: `test_go_back_without_drill_stack_falls_through_to_llm` pinned the MECHANISM ("falls through to the LLM") while its own docstring stated the INTENT ("instead of forcing a drill_up transition into nothing"). The intent is preserved and still asserted — no bogus drill_up, frame untouched — and the test now also asserts zero model calls, since the fall-through was the bug rather than the guarantee | ✅ done | `code` + `local-test` (12 new in `tests/test_recall_questions.py`, 30 there; 10 conversation-layer files pass, 238 thinking-steps; offline harness corpus 299/299 + scenarios 42/42 + suite 240/240; gate 54/54 against the real 7B) + `live` (17-turn document memory run, before and after each fix) | `chatbot/nodes.py` (`classify_node`, `recall_node`), `tests/test_recall_questions.py`, `tests/test_chatbot_classify.py` |
| T70 | 2026-09-21 | **Document conversations had NO memory at all — one missing key** — the user asked to test the filesystem path because the SQL engine is weak there, and it exposed that the whole memory layer was dead on the one source that answers reliably. Measured against the real `docs_contracts` source (179 chunks): after TWO correctly answered document turns, `"what sql did you run"` replied *"nothing has been run in this conversation yet"*. Root cause is a single absent key: a retrieval result's `res0` carries `{answer, chunks, citations, explain, _route}` and **no `status`** — `_extract_engine_result` DERIVED the status and returned it but never wrote it back — so `memory_write_node` proceeded on `state["status"]` while `harvest_frame` re-checked `engine_result["status"]`, found None, and refused. No frame was ever written, and with it recall, re-present, drill and every frame-based feature were silently inert on that source. Three changes: (1) the derived status is written onto `res0`, the same normalisation that function already does for `cols`; (2) `harvest_frame` uses the DATASET as the entity when there is no table — not invented, it is the engine's own business-facing name, already used for `entity_display` — and records `route` explicitly rather than leaving later code to infer "no sql, so probably retrieval"; (3) `render_frame_as_query` returns the message unchanged for a document frame, because its entity is a FILE NAME and appending `(for Samta-Employee Handbook April 2026)` would hand the retrieval pipeline "April 2026" as a date the user never asked about — and the RAG path was measured resolving follow-ups correctly from the message alone. Recall now names what it read instead of implying a fault. **Also found and stated, not fixed**: sources 4 and 5 answer NOTHING — their substrate holds 116 of homzhub's table names with ZERO columns, so csv_lake refuses with "access denied" and parquet clarifies; their status says `ready` while source 3, which works, says `failed`; and all three Celery workers are in a restart loop on `ModuleNotFoundError: rest_framework_simplejwt`, so nothing has been re-ingested since 8 July. Designing memory for those two is premature until they can answer a question | ✅ done | `code` + `local-test` (4 new in `tests/test_recall_questions.py`; 14 conversation-layer files pass, 238 thinking-steps; offline harness corpus 299/299 + scenarios 42/42 + suite 240/240) + `live` (6-turn document conversation on the real engine, before and after) | `chatbot/nodes.py` (`_extract_engine_result`, `recall_node`), `chatbot/memory/frame.py` (`harvest_frame`, `is_document_frame`, `render_frame_as_query`, `QueryFrame.route`), `tests/test_recall_questions.py` |
| T69 | 2026-09-21 | **Ungrounded referential text can no longer reach the engine — and the fix hardcodes nothing** — `"the other ones"` and `"them"` walked past `_BARE_REFERENTIAL_RE`, which is anchored on a closed list of endings that does not include "ones". That guard exists because ungrounded referential text once reached the engine, matched an unrelated table and returned a real person's name and email address. The obvious fix — adding pronouns to the regex — was started and ABANDONED on the user's instruction not to hardcode; it would have been the same open-set trap as T63's acknowledgement list. **The non-hardcoded fix was already written in the repo**: the grounding check reads a vocabulary built from the tenant's OWN ingested substrate (table names, column names, sampled values, synonyms), so it needs no curation and grows with the data. It was disabled because the vocabulary is ~5,700 tokens and was serialised into ChatState — and therefore into Redis — on every turn of every conversation. **Removed that cost instead of the check**: the api tier now computes ONE boolean (`message_mentions_data`) where the vocabulary already lives process-cached, and passes that. Then measured, over the 282 real data questions in `corpus_real.jsonl`: the data check ALONE would withhold 6 of them (2.1%, all "rows in the catalog" — "catalog" lives in a source outside the tested scope), which is exactly the failure this repo refuses. So the gate was split into two cases with different costs: a FOLLOW-UP with no frame AND naming nothing is incoherent by construction and withholds **zero** real questions (none of the 282 classify as followup) — always on; a NEW QUESTION naming nothing keeps its 2.1% cost and stays behind the existing flag, default OFF, unchanged. Verified deterministically: followup+no-frame+names-nothing -> no_match; +names-something -> followup; +WITH a frame -> followup (every refinement stays safe); mentions unknown (None) -> followup, because absence of evidence is not evidence | ✅ done | `code` + `local-test` (12 new in `tests/test_grounding_gate.py`, 27 there; 12 conversation-layer files pass, 238 thinking-steps; offline harness corpus 299/299 + scenarios 42/42 + suite 240/240; gate 54/54 against the real 7B) + `live` (5 leak cases all blocked, 5 control questions all reach the engine including the one the data check alone would have withheld) | `chatbot/nodes.py::classify_node`, `chatbot/state.py`, `chatbot/run.py`, `apps/chat/services.py` (`_mentions_the_data`), `tests/test_grounding_gate.py` |
| T68 | 2026-09-21 | **Silent-wrong found by an 18-turn conversation: a NEW dimension overwrote an existing filter** — built a deep chain (drill down x2, drill up, replace, remove, two shape changes, recall, presentation, acknowledgement, drill after ack, drill up x2, new topic, follow-up, reset, follow-up after reset) with a fake engine that HONOURS the resolved query, so the frame evolves the way a real engine would drive it and a dropped delta shows up as a filter that never changed. 11 turns behaved. Turn 3 did not: frame held City=Pune and Status=ACTIVE, the user said `"just the verified ones"` — a new dimension — and the model returned `replace/Status/"verified ones"`. ACTIVE was overwritten with a value no column holds, and the user's `active` constraint vanished with no signal. **Both existing guards passed it**: Status IS a field the frame holds, and "verified ones" IS verbatim in the message; nothing else could have caught it. Third guard added — `_names_a_set_not_a_value`: a replacement value ENDING in a collective noun (ones/rows/records/items/entries/values/results) is how English names a SET of rows, never a value in one, so the swap is declined. The argument is the asymmetry, not a guess about wording: a wrongly-declined replace costs NOTHING (the frame carries forward and the user's words still reach the engine, which then sees "verified" alongside Pune and ACTIVE and can resolve all three) while a wrongly-accepted one destroys a remembered fact silently. Verified: the bug case now sends `'just the verified ones (for Assets, City equals Pune, Status equals ACTIVE)'`, and `"what about Mumbai"` still replaces. **Still open, found by the same run**: `go back` from depth 1 clears the BASE filter too, because `rebuild_frame_from_stack` derives filters from the drill stack and turn 1's filter was never pushed onto it — annoying, not wrong. Also revalidated the whole smalltalk/conversation breadth after the day's changes: 3 parallel shards, **85/87** (2 of the 4 reported failures were transport — `wow` and `cool` each took ~8020 ms with a DNS error logged immediately before, and both are 3/3 smalltalk on re-run). The 2 real ones are the known capability gap (`what are your capabilities`, `give me some example questions` → both 3/3 `answer`, so a full engine round-trip then a refusal, when `CAPABILITY_REPLY` is already written) | ✅ done | `code` + `local-test` (7 new in `tests/test_context_delta.py`, 47 there; 10 conversation-layer files pass; offline harness corpus 299/299 + scenarios 42/42 + suite 240/240; gate 54/54 against the real 7B) + `live` (18-turn chain; 3-shard revalidation) | `chatbot/memory/frame.py` (`_COLLECTIVE_NOUNS`, `_names_a_set_not_a_value`, `apply_context_delta`), `tests/test_context_delta.py` |
| T67 | 2026-09-21 | **Conversation-layer prompt optimisation — one win shipped, one attempt reverted on measurement** — brief asked for three things; the harness decided which survived. **(A) SHIPPED: the first-turn followup rewrite is gone.** `context_resolve_node` spent a model call asking `FOLLOWUP_SYSTEM_PROMPT` to rewrite a message into a self-contained question whenever no frame existed — i.e. the first turn of a session, whose question is self-contained by definition. The rewrite could only paraphrase, and a paraphrase is a fresh chance to lose a word the engine parses as data. Measured: a first-turn data question went from **2 model calls to 1** (1307 ms, 1 call). The referential case is untouched — classify_node's backstop downgrades it before this node runs. **(B) REVERTED: shrinking the supervisor's 671-token delta block.** New `chatbot/prompts/delta_types.py` rendered it compactly from a canonical source; the gate caught three separate regressions in sequence — first `delta_field` came back as the whole rendered filter (`'Year equals 2025'` instead of `'Year'`), fatal for shape slots since `'limit 100'` matches no alias; fixing that left 53/55 with `compare` -> ambiguous and `what about Mumbai` -> new_topic, **5/5 on repeat, so not noise**; restoring the examples made it **52/55**, worse again. Original text restored, 54/54. That is now the THIRD independent measurement that this prompt degrades under edits, and the first showing it degrades under *compression* as well as addition. `delta_types.py` is kept for the closed set only, with the failed attempt recorded in its docstring so the next person does not repeat it blind. **(C) SHIPPED: base prompt 521 -> 447 tok**, removing only human-oriented rationale (the relative-date examples, the followup justification clause, the HARD RULE's consequence sentence). 54/54. The brief's ~280 target was NOT reached and is not safely reachable: everything left is either an action definition or an example, and examples were just measured load-bearing. **(D) unclear input: MOSTLY already safe, one real hole found and closed.** Measured: `helerrroo`, `asdkjhasdjk`, `qweqweqwe`, `xjskl 123 zzq` all classify as `smalltalk`, which never reaches the engine, while `revenuuu for 2025` and `whattt about 2024` correctly classify as `answer` and do — so the routing requirement was already met for word-shaped gibberish, and no detector was built for it (every candidate needs a dictionary, which the brief bans, or an edit to the prompt just proven fragile). But `"???"` was NOT safe: it classified as a data question and spent a full engine round-trip (1205 ms). Closed with the one unintelligible shape recognisable without a dictionary OR a model — `_is_punctuation_only`, which tests Unicode CATEGORY (every non-space character in class P*). A message with no word in it is not asking anything. Deliberately excludes emoji: `👍` is category So, and telling someone who agreed with the answer that they were not understood is worse than saying nothing. Now **19 ms, 0 model calls**, reply `"I'm not sure I understood that. Could you rephrase your question?"` (§12's wording). With a clarification pending it DROPS the slot rather than answering it — "???" is the user saying they are lost, and gluing it onto the stored request is the compounding failure. **No Tiny Entry SLM was added**, per the prior audit | ✅ done (A, C) · ⛔ reverted (B) · ⏸ not built (D) | `code` + `local-test` (25 new in `tests/test_entry_gate.py`, 108 there in total; 13 conversation-layer files pass, 238 in test_thinking_steps; gate 54/54 and offline harness corpus 299/299 + scenarios 42/42 + suite 240/240) + `measured` (gate run after EVERY prompt change, 6 gate cycles; benchmark of 7 paths) | `chatbot/nodes.py::context_resolve_node`, `chatbot/prompts/supervisor.py`, `chatbot/prompts/delta_types.py` (new), `chatbot/prompts/delta_classify.py`, `chatbot/prompts/smalltalk.py` (`REPHRASE_REPLY`), `tests/test_entry_gate.py`, `evaluation/conversation/scenarios.jsonl` |
| T66 | 2026-09-21 | **Second smalltalk model call removed — one call per conversational turn** — `smalltalk_node` asked the model for the WORDS of a reply on top of the classify call that had already decided the turn was smalltalk. Two calls on every turn the canned patterns could not spell-match. Measured before/after on the real 7B: `helo` 9.9s -> 1.36s, `hola` 12.9s -> 1.26s, `hlo` 5.8s -> 1.12s, `okay` ~5.8s -> 1.29s — all now **1 call instead of 2**. It was also a drift surface, not just latency: `hola` had been answering `'¡Hola! ... preguntas sobre数据分析'`, Spanish and Chinese mixed. Replaced with `SMALLTALK_FALLBACK_REPLY`, deliberately tone-NEUTRAL — `FALLBACK_REPLY` is a greeting and reads wrong as an answer to "ok" or "thanks", which is exactly what reaches this path now. Behind `CHATBOT_SMALLTALK_LLM_REPLY=1` to restore. The trade is stated rather than hidden: replies to unmatched smalltalk are now generic instead of tailored; for a data-analytics assistant a fixed neutral line beats a 12-second wait and a bilingual answer | ✅ done | `code` + `local-test` (9 conversation-layer files pass, 238 in test_thinking_steps; harness corpus 299/299 + scenarios 42/42 + suite 239/239) + `live` (7-message before/after on the real 7B, call counts via collect_usage) | `chatbot/nodes.py::smalltalk_node`, `chatbot/prompts/smalltalk.py`, `chatbot/prompts/__init__.py` |
| T65 | 2026-09-21 | **Turn Entry Gate — implemented as an EXTENSION, not a new classifier** — the brief asked for a gate answering "does this turn need VEDA, and does it need context?" before the Conversation Layer. Inspection found that `classify_node` ALREADY is that gate: its seven deterministic fast paths are the L0 tier, its single SLM call is the model tier, and `graph.py::_route_after_classify` already routes to or away from the engine. A gate in front would re-derive the same verdict from the same message, and two classifiers that can disagree is precisely the failure class this package's three overrides exist to patch (T63 is the most recent). So the contract is DERIVED: `entry_decision(action, has_history)` reads the verdict classify_node already reached and returns `{requires_veda, requires_context}`, mirroring the ROUTER (which keys on history) rather than the action label, so the gate can never report a path the turn did not take. Fails toward more processing — an unrecognised action means requires_veda=True, never a confident direct answer. **One genuinely new behaviour**: `_normalise_for_l0` retries the EXISTING canned patterns once on a conservatively normalised spelling — runs of THREE or more identical letters collapsed (so "hello" and "morning" are untouched) and a trailing run of punctuation/whitespace/emoji dropped. It adds NO greeting to any list, which is the brief's "do not create a giant greeting dictionary" and this repo's own repeatedly-measured open-set trap. A misspelling is deliberately not guessed at: "helo", "hlo", "gud morning" still reach the model. Before it, 15 of the brief's own 17 L0 examples hit; now all of them do, plus "heeeey 😂" and "good morninggg". Observability via `classify_with_entry_gate`, a thin wrapper registered as the graph's classify node — it exists only because classify_node returns from six places and every path must report `entry_path` (L0_DIRECT / L0_FASTPATH / SLM_GATE) and `classification_latency_ms`; it changes no decision, which is pinned by a test comparing wrapped against plain. Surfaced through `run_chat_turn` into the turn's audit row. **Measured latency** (engine faked, median): L0 direct 17.5ms total / 0.07ms gate · L0 fast path 32.3ms / 0.05ms · context-only 46.0ms / 0.02ms · SLM gate on a greeting typo 12.3s (two model calls — classify then the reply) · VEDA no context 1.36s · context+VEDA 5.8s. **Security unchanged**: the gate sits downstream of `memory_read_node`, which is the graph's entry point and holds the RBAC guard, so it never sees a frame nobody checked; `entry_decision` takes an action and a boolean and cannot consult source ids, data scope or the frame — pinned by a signature test | ✅ done | `code` + `local-test` (83 new in `tests/test_entry_gate.py` covering L0 hits and false positives, normalisation limits, the four contract states, 10 malformed-model-output shapes, the reporting wrapper and four security properties; 15 conversation-layer files pass, 238 in test_thinking_steps; harness corpus 299/299 + scenarios 42/42 + suite 239/239 with 21 new normalisation cases) + `measured` (latency table above) | `chatbot/nodes.py` (`entry_decision`, `_normalise_for_l0`, `classify_with_entry_gate`, `_canned_smalltalk_reply`), `chatbot/graph.py`, `chatbot/state.py`, `chatbot/run.py`, `apps/chat/services.py`, `tests/test_entry_gate.py`, `evaluation/conversation/build_suite.py` |
| T64 | 2026-09-21 | **Broad conversation suite — 346 messages, 16 categories** — `evaluation/conversation/suite.jsonl`, generated from reviewable lists in `build_suite.py`. The third leg of the harness: `corpus_real` is what users really typed (real, unlabelled, no follow-ups), `scenarios` is 26 multi-turn scripts (labelled, one shape each), and neither covers BREADTH — the many ways one move is phrased ("go back"/"zoom out"/"undo that") plus the typos, Hinglish and emoji real traffic is full of. Deliberately adversarial: greeting typos (`hhhwwlo`, `helo`, `gud morning`), 48 acknowledgements, messages that LOOK like a shape change but are new questions (`top 5 cities by assets` vs `make it top 5`), bare referential text, prompt injection, and 20 real data questions as the CONTROL GROUP — no conversational fast path may swallow one. Expectations are per CATEGORY, not per message, so a new phrasing needs no new label. **First run: 219/219 asserted, 20 real gaps found.** The worst is functional, not latency: `_VALID_ACTIONS` is `{smalltalk, followup, clarify_reply, answer}` — there is no "reset", so the ONLY path that wipes memory is `_RESET_RE` matching in `memory_read_node`, and **7 of 12 reset phrasings match nothing** (`clear everything`, `new conversation`, `wipe memory`, `start again`, `forget what i said`, `forget it all`, bare `clear`) — the frame survives and the user believes it is gone. Also 7 recall phrasings fall through to the model and can reach the engine (the 101-second "what did i just ask" class), and 4 presentation phrasings cost a full round-trip to redraw rows already on screen. Gaps are recorded in `KNOWN_GAPS` and reported in their own section, never as a regression; one that starts passing is reported too, so the markers cannot rot either way. Growing the regexes is NOT proposed — that is the open-set trap of T63, where a word list was written and reverted before it was tested; this is evidence for a design decision, not a TODO | ✅ done | `code` + `local-test` (3 new guards in `tests/test_conversation_eval.py`, 7 total there — unrecorded failures, gap-set drift in both directions, and the control group on its own; deterministic run corpus 299/299 + scenarios 42/42 + suite 219/219) | `evaluation/conversation/{build_suite.py,suite.jsonl,run_eval.py,README.md}`, `tests/test_conversation_eval.py` |
| T63 | 2026-09-19 | **"okay" was being sent to the SQL engine as a query** — found by the user asking the obvious question nobody had tested: you answer something, I type "okay", then what? Measured live, one answered turn then one acknowledgement: `okay`, `ok`, `hmm`, `got it`, `cool`, `i see`, `makes sense` ALL became action='followup' and the literal word reached the engine — 7 of 8 tried, and `ok`/`okay` is the single most common thing anyone types after reading an answer. **The model said smalltalk for every one of them, correctly, and was overruled by our own Python.** Three causes, all ours, none of them the 7B: (1) classify_node's third override fired on "a frame exists AND this is not a literal greeting/thanks/bye" — the set of things a person types after an answer is OPEN, so every word missing from the greeting list became a query; it now needs POSITIVE evidence via `_mentions_frame_subject` (does the message name the entity/filters/grouping/measures the frame actually holds), word-level not substring, so "tell me something about transaction" still overrides and no filler ever can. (2) the standalone-check prompt asked "does understanding this depend on the earlier turns?" and a 'dependent' answer was read as "this is a data question" — for `got it` the model answered 'dependent' CORRECTLY (the phrase means nothing alone) and we turned that into a query; it now asks the thing the caller needs: is the user ASKING FOR SOMETHING. (3) the supervisor prompt's smalltalk category was "ONLY pure greetings, thanks, goodbyes" with a HARD RULE pushing away from smalltalk, so `hmm` had no category to land in; acknowledgements are now named as smalltalk explicitly. **A first attempt was a word list of acknowledgements and it was the wrong shape — reverted before it was tested**, on the user's push-back: it patches the same brittle mechanism that caused the bug, and would break again on the next word nobody listed. The prompt fix generalises instead — `acha` (Hinglish, in no list anywhere) classifies correctly | ✅ done | `code` + `local-test` (61 new in `tests/test_acknowledgements.py`; 11 conversation-layer files pass; deterministic eval corpus 299/299 + scenarios 42/42) + `live` (8 of 8 acknowledgements now stay smalltalk and never reach the engine: okay, ok, hmm, got it, cool, i see, makes sense, acha). Verification finished after the SLM endpoint recovered: 12/12 acknowledgements stay smalltalk (the 8 above plus `thik hai`, `right`, `understood` and a bare 👍 — none of which appear in any list anywhere), and all four regression cases behave: `tell me something about transaction` and `how many invoices are there` reach the engine, `what about the other one` and `only the ones in Mumbai` reach it WITH the frame's context appended. **One regression was caught by the harness and fixed**: the first version of the supervisor-prompt edit was six lines long and knocked `what about Mumbai` from `replace` to `refine` (eval 54/54 -> 52/54) — the identical damage the earlier shape-example edit did to the same prompt, which is now twice-measured evidence that ANY addition to that prompt degrades its delta half. Shortened to a single clause: `what about Mumbai` back to `replace` 4/4 on a direct probe, acknowledgements still smalltalk, eval back to 54/54. A one-off 53/54 in between was a transient (`what about 2024` returned no delta at all; 4/4 correct on a direct re-probe). **Also observed live, and NOT fixed**: `only the ones in Mumbai` resolves to `'only the ones in Mumbai (for Assets (assets_asset), City equals Pune)'` — Mumbai from the message, Pune from the frame, both handed to the engine. That is the latent `refine` defect recorded in T57, now confirmed in a live run rather than a probe | `chatbot/nodes.py` (`_mentions_frame_subject`, `_content_words`, `classify_node`'s third override), `chatbot/prompts/standalone_check.py`, `chatbot/prompts/supervisor.py`, `tests/test_acknowledgements.py` |
| T62 | 2026-09-18 | **Task 4 (question-shape preservation) HALTED on evidence; a different, measured defect fixed instead** — went to validate Task 4's premise on the REAL engine before touching `veda_core`, and could not: of two 3-turn conversations, BOTH base questions came back `clarify` ("show the top 20 assets by carpet area" 14.9s, "count of assets by city" 11.5s — the second is a verbatim real message from `chat_chatmessage`). With no answered turn there is no frame, so there is no question-shape to preserve and Task 4's premise is **untestable on this environment**. Engine-side, out of the conversation layer's scope; Task 4 stays unimplemented rather than built against a premise I could not verify. What the runs DID expose is reachable and was fixed: because the engine clarifies often, **a pending clarification is a common state, not an edge case**, and an explicit re-shaping of the question was being eaten by it. Measured: `_is_clarification_answer("by city instead")` and `("top 5")` both returned True, and the SLM independently called "make it top 5" a clarify_reply — so the turn was glued onto the unanswered request (`'show the top 20 assets by carpet area for make it top 5'`) and the engine answered something entirely unrelated with full confidence: a count of `accounts_generalledger` rows grouped by `project_name`, 20 rows, which then poisoned the frame for turn 3. Fix: `_SHAPE_PATTERNS` now marks each pattern EXPLICIT (names an operation: "make it top 5", "by city instead") or BARE (names only a value: "top 5"), `detect_shape_delta` takes `explicit_only`, and classify_node consults it BEFORE the pending-clarification branch — an explicit re-shaping can no longer be read as an answer, and also DROPS the pending slot rather than leaving it armed. The bare form is deliberately still allowed to answer a clarification, because a bare value is exactly what a clarifying question asks for. **Known limit, stated rather than hidden**: this needs the frame to hold the slot, so it does NOT cover the case the live runs actually hit (turn 1 clarified, so no frame exists at all) — amending an UNANSWERED request is a separate design question and was not guessed at | ✅ done (the fix) · ⛔ blocked (Task 4) | `code` + `local-test` (6 new in `tests/test_shape_delta.py`, 53 total there; clarification/classify/recall/presentation files all pass; eval 54/54 against the real 7B) + `live` (2 real-engine 3-turn conversations, which is what produced the evidence; fake-engine shape run unchanged) | `chatbot/memory/frame.py` (`_SHAPE_PATTERNS`, `detect_shape_delta`), `chatbot/nodes.py` (`_is_clarification_answer`, `classify_node`), `tests/test_shape_delta.py` |
| T61 | 2026-09-18 | **Per-session turn serialization (Conversation Layer plan, Task 6)** — reproduced first: 4 concurrent turns on ONE chat left **2 of 8 history entries**, silently. LangGraph's checkpoint is last-write-wins, so six turns of conversation were simply gone with nothing anywhere reporting it. `session_turn_lock` (Redis SET NX PX + a Lua compare-and-delete release) now wraps `graph.invoke` in `run_chat_turn`: same session and tenant serialize, different sessions and different tenants never block each other. **Serialization, not optimistic retry** — the checkpoint carries no version to retry against, so a retry could only ever repair the QueryFrame, which already has an optimistic lock and already aborts correctly, and never the history that was actually being lost. Three deliberate choices, each with a failure it prevents: the LEASE (600s, env-tunable) outlives the slowest realistic turn because a lease expiring mid-turn hands the lock to a waiter and puts BOTH turns back in the race; the WAIT is bounded (120s) and past it the turn proceeds UNSERIALIZED rather than refusing the user's message, so a dead holder cannot wedge a chat forever; and the release is a compare-and-delete on the holder's own token, because after a lease expiry the key may already belong to the next turn and a blind DELETE would hand a third turn into the race with it. An unreachable Redis degrades to no locking — this is a consistency improvement, not a new hard dependency. One measurement corrected along the way: the repro also expected `frame.version` to reach 4 and it stayed at 1, but a SEQUENTIAL control run gives 1 as well — every turn in the repro is a `new_topic`, which resets the frame by design, so the version was never evidence of a race and only the history loss was real | ✅ done | `code` + `local-test` (11 new in `tests/test_session_turn_lock.py` against real redis-stack, including 6-way mutual exclusion, lease expiry, cross-session/cross-tenant independence, release-on-exception and the Redis-down path; 544 conversation-layer tests across 17 files pass; eval 54/54 against the real 7B) + `live` (4 concurrent turns through `run_chat_turn`: history 2/8 -> 8/8, plus the sequential control) | `chatbot/memory/store.py` (`session_turn_lock`, `_LOCK_*`, `_RELEASE_LUA`), `chatbot/run.py`, `tests/test_session_turn_lock.py` |
| T60 | 2026-09-18 | **Conversation understanding surfaced to the user (Conversation Layer plan, Task 5)** — `explainability` has always described how the ANSWER was produced; nothing described how the QUESTION was read, which is the half that fails invisibly: a follow-up resolved against the wrong remembered topic still returns a confident, fully-explained answer to a question nobody asked. New `context` block — `carried` (the remembered entity, its filters, its source), `changed` (the operation and the slot it acted on), and `resolved_query` (the exact text handed to the engine). Built in `chatbot/nodes.py::_context_used` from facts the turn ALREADY produced — the frame it merged and the delta it applied — so it cannot report an understanding the turn did not act on; nothing is re-derived, inferred or asked of a model. **Absent, not null**, on every turn that carried no context (first question, smalltalk, recall, re-render), because an empty panel is itself a claim; `_context_used` returns None when the frame has no entity or the resolved query is just the user's own words. Streamed as a new SSE event BEFORE the first `content` event — a user who reads the answer first has already believed it. Cleared explicitly in `classify_node`'s per-turn reset AND in all five of its early returns, the same trap that made `memory_reset` sticky: the checkpointer persists the whole state, so a field only ever set would report the PREVIOUS turn's understanding as this one's. `clarify_reply_node` reports its own shape (`completing: <the earlier question>`), since a clarification bound to the wrong pending request is equally invisible. Folded by `TurnEventAccumulator`, persisted in the assistant message's `metadata` (JSONField, no migration) and replayed by the history projection's explicit allowlist, so a reloaded conversation shows the same panel the live stream did | ✅ done | `code` + `local-test` (13 new in `tests/test_context_understanding.py`; 232 `test_thinking_steps` + 34 `test_chat_visualization` + the conversation-layer files all pass; eval 54/54 against the real 7B, unchanged) + `live` (5-turn run: turn 1 null, each shape follow-up carried entity+filters+operation+resolved text, recall null) | `chatbot/nodes.py` (`_context_used`, `context_resolve_node`, `clarify_reply_node`, `classify_node`), `chatbot/state.py`, `chatbot/run.py`, `apps/chat/services.py`, `apps/chat/turn_events.py`, `apps/chat/views.py`, `CHAT_API_CONTRACT.md`, `tests/test_context_understanding.py` |
| T59 | 2026-09-18 | **Per-source analytical memory (Conversation Layer plan, Task 1)** — one QueryFrame per SOURCE instead of one per session. A frame is evidence harvested from one source's executed SQL, so a session that touched two sources kept only the newer one and the earlier topic vanished with no signal (live-tested 2026-09-17). Keys are now `veda:mem:{tenant}:{session}:src:{source}:frame|stack`, with an `:active` pointer (the source that answered last) and a `:sources` SET so a full reset can delete every source's keys without SCANning. **The episodic buffer stays session-wide** — it is the conversation, which has one thread however many sources answered it. `source_id=None` on every method is the pre-scoping key, so the CLI and any deployment that sets no source are byte-identical to before; a scoped read falls back to the legacy key so a conversation live across the deploy keeps its context, and that fallback REFUSES to answer when the legacy frame names a different source than the one asked for (otherwise it would reintroduce the exact cross-source bleed being removed). The active pointer is written after the frame and outside the optimistic-lock transaction, so an aborted write cannot leave a pointer to a frame that never committed. **The RBAC revocation guard got NARROWER in what it destroys, never in what it catches**: it still runs on every turn in `memory_read_node` (the one node every route reads through), but now wipes only the revoked source's keys — losing a grant on one source is not a reason to discard the user's work on another, and scoped keys are what make that distinction expressible. A frame with no recorded source still wipes the session. Live-verified through the real graph on real Redis + real 7B: Scenario A — asked on source 2, asked on source 3, then "what about Mumbai" back on source 2 resolved against source 2's own frame (`City equals Mumbai`, Pune replaced) while source 3's frame survived untouched; Scenario C — with source 2 revoked, its frame was discarded, recall honestly refused, and source 3's frame and the active pointer were left correct | ✅ done | `code` + `local-test` (14 new in `tests/test_memory_source_scope.py` against real redis-stack, module-skips when unreachable; 280 conversation-layer tests across 14 files pass; eval harness 54/54 against the real 7B, unchanged) + `live` (Scenarios A and C through `run_chat_turn`) | `chatbot/memory/store.py` (`_k`, `_active_key`, `_sources_key`, `read_frame`, `write_frame`, `read_stack`, `write_stack`, `active_source`, `known_sources`, `reset`), `chatbot/nodes.py` (`memory_read_node`, `memory_write_node`), `tests/test_memory_source_scope.py` |
| T58 | 2026-09-18 | **Shape deltas — limit / grouping / ordering / measures as structured operations (Conversation Layer plan, Task 2)** — "make it top 10", "show it by month instead", "don't sort by amount" now mutate the QueryFrame's shape slots in Python instead of being re-derived from words. `apply_context_delta` gained a shape half beside its filter half, with the SAME three guards (the slot must already hold a value; a replacement must be a literal the user typed; anything unbindable carries the context forward unchanged) plus one of its own: `replace` acts only on a single-valued slot, because "by month instead" against `group_by [Year, Region]` names no one of them and picking would be guessing. A filter of the same name always wins, so a source with a real "Limit" column is unaffected. **The operation is decided deterministically, not by the model** — measured: the merged supervisor call identifies the SLOT correctly every time and the OPERATION never (all four shape cases came back "refine"), which is fatal because `parse_delta_response` drops `delta_field` for any type outside replace/remove, so the slot name is discarded and no signal survives. Teaching the merged prompt was tried and REVERTED: it left the refine bias intact and regressed an ordinary filter swap ("what about Mumbai" went from replace/City to refine/filters). The focused standalone prompt gets 4/4, but reaching it costs a second SLM call on the common path. So `detect_shape_delta` reads the operation off the message with whole-message-anchored patterns — the same fast-path pattern already used for reset/drill-up/recall/presentation — gated on the frame actually holding that slot. Anchoring is what separates "top 10" from "top 5 cities by assets": **0 misfires across all 299 real user messages**. Asked only when the model did not already produce a usable replace/remove and never when it called the turn a new topic. Two follow-on fixes: `context_resolve_node`'s remove-downgrade compared filter COUNT, so a shape removal was reported as having matched nothing — now an identity check on the returned frame, which is exact because `apply_context_delta` returns the caller's own object when it declines; and `render_frame_as_query` grew `shape_delta`, because a filter removal renders context-only (its words name a value the engine must not be handed back) while a shape removal is the opposite — the shape slots are deliberately absent from the rendered context, so the message is the only thing carrying the change. No operation prose reaches the engine, which is the 2026-09-17 54-second incident this design exists to avoid | ✅ done | `code` + `local-test` (40 new in `tests/test_shape_delta.py`, asserting absence; eval harness 42/42 offline and 54/54 against the real 7B, up from 27/27 and 39/39) + `live` (5-turn run through `run_chat_turn` on real Redis + real 7B: all three shape deltas fired with zero extra SLM calls, filters carried, turn 1 byte-identical) | `chatbot/memory/frame.py` (`detect_shape_delta`, `_apply_shape_delta`, `render_frame_as_query`), `chatbot/nodes.py::context_resolve_node`, `chatbot/prompts/delta_classify.py`, `tests/test_shape_delta.py`, `evaluation/conversation/` |
| T57 | 2026-09-18 | **Conversation-layer regression harness (Conversation Layer plan, Task 3)** — `evaluation/conversation/`: an extractor that pulls all 299 distinct real user messages out of `chat_chatmessage`, 26 hand-written multi-turn scenarios with explicit frame fixtures, and a runner that exercises `classify_node` alone (no graph, no Redis, no engine, no SQL). Two premises in the plan this implements were measured and found WRONG, and the harness is built around the corrections: the history is NOT labelled (`metadata["action"]` exists on 4 of 1,723 rows, added 2026-09-17), and it contains NO follow-ups (12 of 292 sessions are multi-turn and even those turns are self-contained), so the real corpus can only measure that no deterministic fast path swallows a real question, and delta behaviour needs the synthetic half. Seeded vs hand-reviewed labels are counted separately in every report so a machine-seeded label can never be presented as ground truth. Two modes: `deterministic` forbids SLM calls outright (offline, CI-safe) and `full` uses the real 7B. Found on the first run: (1) LATENT — `apply_context_delta` ignores every delta type but replace/remove, so a `refine` on a single-valued filter keeps the old value and the resolved query carries both (`'what about Mumbai (for Assets, City equals Pune)'`, probed directly); with production-shaped state the 7B says `replace` and it does not fire, so it is latent, not an observed failure; (2) CONDITIONAL — `_is_clarification_answer` matches 5 real questions (`notice period`, `total users`, …), reachable only while a clarification is pending, now pinned by a test so the set cannot grow silently; (3) `action` does not discriminate followup from answer (`_route_after_classify` routes on history, not the label) — four of my own assertions were testing a field with no behavioural consequence; (4) `_presentation_kind` misses "show it as a bar chart instead", recorded as target-pending, not a bug. Planned-but-unbuilt behaviour (shape deltas, comparison context) is carried as `expected_to_fail` and never counted as a regression, with a test that fails if one starts passing. No production code touched | ✅ done | `local-test` (4 new in `tests/test_conversation_eval.py`; 181 existing chatbot tests across 6 files still pass) + `probed` (full mode against the real 7B via `vedademo`) | `evaluation/conversation/{README.md,extract_corpus.py,corpus_real.jsonl,scenarios.jsonl,run_eval.py}`, `tests/test_conversation_eval.py` |
| T56 | 2026-08-12 | **Category-based RBAC round (§2b of `RBAC_QUERY_PIPELINE_TEST_SUITE.md`)** — 22 queries spanning all 6 `component_suite_*.jsonl` categories (simple/aggregate/filter/grouped/ranking/temporal), run twice against the same role: full access, then `assets_leaselisting`+`assets_leasetransaction` DENIED. 9/9 denied-table queries correctly blocked, 13/13 allowed-table queries correctly answered — 0 leaks across every category. One apparent miss ("How many lease listings exist?" → generic clarify, not access-denied) investigated via container logs: the query router picked an allowed sibling table (`assets_leaselistinglead`) as primary and never touched the denied table at all — same entity-resolution noise as §3's headline finding, not an RBAC gap. **Added a defense-in-depth guard anyway**: `pipeline.py`'s qualifier-completeness gate now also checks whether the SQL it would have run referenced an RBAC-restricted table, independent of whether the specific dropped qualifier term matches a restricted name — closes the general shape of T53/T54's gap-class pre-emptively, even though this round never actually hit it | ✅ done | `code` + `local-test` (50, unchanged) + `probed` (live: category round; the new guard doesn't misfire on the investigated case) | `veda_core/veda/pipeline.py` (qualifier-completeness gate, just before `qualifier_dropped` return) |
| T55 | 2026-08-12 | **Found + fixed: `roles/delete`/`roles/update{is_active:false}` on the seeded "Admin" role had zero protection** — third instance of the same bug-class as T50/T49 (name-based Admin-role identification, missing guard at a specific mutation point). Reproduced live: deleting role_id=1 (Admin) returned 200 with no error; the platform's own only admin was instantly locked out of `roles/detail` (403) because their sole role assignment had just been deactivated. Recovered live via `manage.py shell`. User's explicit call on the fix: name-based matching (not a new flag column), block BOTH deactivate/delete AND rename. Added `AdminRoleProtected` (`ConflictError`, code `ADMIN_ROLE_PROTECTED`) — a THIRD, previously-missing guard, distinct from `LastAdminProtected` (protects a user from losing last-admin status) and `LastAdminRoleProtected` (protects a user from losing their last admin role): this one protects the ROLE RECORD itself, unconditionally, regardless of who holds it. Guarded in `update_role()` (covers `roles/update` and `roles/delete`, which calls it internally) | ✅ done | `code` + `local-test` (116/116 in `test_role_management.py`, 8 new) + `probed` (live: `roles/delete {role_id:1}` and `roles/update {role_id:1,name:"..."}` both now return 409 `ADMIN_ROLE_PROTECTED`; role_id=1 confirmed still active/named "Admin" afterward) | `apps/access_management/services/roles.py::AdminRoleProtected`, `apps/core/messages.py`, `apps/access_management/services/__init__.py`, `tests/test_role_management.py` |
| T54 | 2026-08-12 | **Found + closed T53's earlier bail-out path.** Traced it to `pipeline.py`'s SQL-validation gate: the LLM/deterministic head successfully GENERATES SQL referencing a real, RBAC-restricted column (schema-linking/sql_planning run to completion — that's why some of T53's live probes took 8-30s, not the sub-2s of a true "found nothing" case), `narrow_allowed()` strips that column from the allowlist right before `validate_and_parameterize`, the AST firewall correctly REJECTS the SQL (`status="invalid"`) — but that branch never called `_feedback` at all, so `ask_clarification_node` fell to its hardcoded generic string with zero context. Fixed: capture `restricted_names(sm, ctx)` *before* `narrow_allowed()` narrows (which only returns the narrowed sets, not what it removed), and on an `"invalid"` rejection check whether the generated SQL's text actually references one of those names via a quoted-identifier match (`'"name"' in sql` — not a bare substring, so an unrelated word merely containing a restricted name can't false-positive). A hit builds real feedback via a new `explain_failure(status="access_denied")` branch, sharing its exact wording (`ACCESS_DENIED_WHY`/`ACCESS_DENIED_WHAT` constants) with T53's `qualifier_dropped` fix so the two paths never disagree. Live-verified: the EXACT phrasing that returned the empty generic fallback in T53 now returns the real access-denied message; a non-restricted column in the same table still answers normally; a genuinely nonexistent term still gets the ordinary "I couldn't map" wording (no false positive) | ✅ done | `code` + `local-test` (50: +3 new) + `probed` (live: before/after on the exact repro case, plus the two regression checks) + `manage.py check` (n/a — veda_core has no Django) | `veda_core/veda/pipeline.py` (SQL-validation "invalid" branch), `veda_core/veda/feedback.py::ACCESS_DENIED_WHY/_WHAT`, `tests/test_feedback.py` |
| T53 | 2026-08-12 | **Query pipeline RBAC enforcement — verified live end to end**, and a UX gap found (closed by T54). Confirmed against the real docker stack (LLM/embedding pipeline, not mocked): zero-permission user → 403 pre-inference; unauthenticated → 401; full access → real SQL + real answer; staff → bypass intact; a query naming a table/column the role's grants exclude never leaks its existence (RBAC narrows retrieval before schema-linking sees it). **UX gap flagged by the user**: that "never leaks existence" design makes an access-denied case indistinguishable from a genuine typo/non-existent column — both render as "Could you clarify what you're asking about?" Discussed the industry tradeoff (public multi-tenant SaaS hides existence; internal single-org BI tools like Looker/PowerBI show it explicitly) — user's call: VEDA is single-org, so show it explicitly. **Fixed for the one status path it's deterministic for**: `veda.rbac_filter.restricted_names()` (new) + `veda.feedback.explain_failure`'s `qualifier_dropped` branch now say "You don't have permission to access this data. Contact your Admin to request access." for an EXACT match against an RBAC-restricted name (deliberately not fuzzy — a substring match would itself leak). Wired via `pipeline.py`'s `_feedback` closure into a COPY of `sm`, never the live one `run_query` reasons over — zero behavior change to retrieval/planning/SQL-gen. **Not fully closed by this task alone**: live re-testing found real column names also route through an earlier, different bail-out (the SQL-validation gate) that built no feedback at all — see T54, which found and closed it | 🟡 see T54 | `code` + `local-test` (47: 36 rbac_filter + 11 new feedback) + `probed` (live: enforcement fully confirmed; message fix confirmed only for the `qualifier_dropped` path, not universally) | `veda_core/veda/rbac_filter.py::restricted_names`, `veda_core/veda/feedback.py::_restricted_match`, `veda_core/veda/pipeline.py::_feedback`, `tests/test_feedback.py` |
| T52 | 2026-08-12 | **Live RBAC enforcement test on the primary data path** (`/api/v1/conversations/query`), real docker stack, real LLM pipeline — table-level and column-level denial both confirmed: a role with `db:homzhub` access but no grant on a specific table, or a table grant with one column DENY, made that table/column invisible to the query engine (generic "clarify" response, not the actual data) while sibling tables/columns kept working normally. Set up + tore down disposable test roles/users for it | ✅ done | `probed` (live) | — |
| T51 | 2026-08-11 | **Documented the Edit Role flow in `api_contract.md` (new §6.4.1)** — user asked how a frontend safely prefills/re-saves a role's resource grants given `roles/detail` returns `permission_ids` only and `catalog/tree` only ever resolves one level at a time. Answer: `GET /api/v1/roles/permissions/list?role_id=<id>&page_size=100` — one call, returns every `RolePermission` row (global + resource-scoped) for the role, live-verified. Documented: `resource_grants` and `permission_ids` on `roles/update` are each an **independent full replacement**, not a patch — the frontend must resubmit every untouched row (including `resource_exists: false` ones) or they are silently revoked. Also fixed a real contradiction in the catalog-tree doc: §12.1 said category filtering was fixed everywhere, §12.2/§12.3 still said "only with `parent_path`" — re-verified live for all three category values (`database`/`datalake`/`file_system`) at root level, all work | ✅ done | `probed` (live, all 3 categories) | `api_contract.md` §6.4.1, §12.1-§12.3 |
| T50 | 2026-08-11 | **Comprehensive live multi-scenario testing** of roles/users/catalog/permissions/auth/conversations against the running docker stack (2 rounds, ~80 requests) — auth (lockout threshold, replay detection, per-account+IP scoping, token expiry/tamper), roles/users CRUD edge cases, cross-user isolation, non-staff 403s, concurrent-update race safety. **Critical finding, reproduced live**: renaming the seeded "Admin" role (which the API allows with zero restriction) breaks `UserRoleService.revoke()`'s admin-protection guard (`grants.py` still matches by `name__iexact="Admin"`) AND `manage.py bootstrap_admin` (`Role.DoesNotExist` instead of `AlreadyBootstrapped`) — revoking the renamed role from the platform's only admin succeeded with 200, left them `is_staff=True` but zero RBAC roles, locked out of every `RequiresPermission` screen. Recovered live (reassigned + renamed back). **Not fixed** — user's call, `users/roles/revoke` isn't in the API set currently in use; `_sync_roles`'s equivalent gap (see T49) WAS fixed since it's in `users/update`. Also found and reported (not all fixed): role delete doesn't warn/block an in-use role (AC gap vs the user-story spec), login doesn't block a user whose only role was retired (checks assignment-row existence, not `role__is_active`), deactivated-account/no-role-assigned logins share one generic message (deliberate enumeration-safety tradeoff, flagged as a spec deviation not a bug) | ✅ done | `probed` (live, real docker stack) | conversation transcript; `AUTH_ISSUES_BACKLOG.md`-style findings not yet filed as their own doc |
| T49 | 2026-08-11 | **Reviewed the user's own new `role_ids` feature** (`UserService.create_user`/`update_user`, `UserCreateSerializer`/`UserUpdateSerializer`) and found + fixed 3 real bugs. **Critical, reproduced live**: `_sync_roles`'s full-replace delete had no admin-protection check at all — `users/update {"role_ids": []}` on the platform's last active admin silently stripped their Admin role (200, no error), same crippling effect as the T50 finding but through a brand-new code path with no rename needed. Fixed with a `LastAdminRoleProtected` guard mirroring `UserRoleService.revoke()`'s. **Found while regression-testing that fix**: `role_ids`'s serializer-level `default=None` made the field always present in `validated_data`, silently breaking `UserUpdateSerializer`'s "provide at least one field" check for every update (not just role_ids ones) — a bare `{"user_id": ...}` reached the service's own `ValueError` unhandled (raw 500). Fixed by removing the unnecessary default. Also: `InvalidRole`/`CODE_INVALID_ROLE` were defined but never exported from `services/__init__.py`. Added 11 regression tests (full-replace, last-admin guard both directions, unknown role_id, no-op-update-is-400-not-500) | ✅ done | `code` + `local-test` (604) + `probed` (live: 409 on the guard, 400 not 500 on the no-op) + `manage.py check` | `apps/access_management/services/users.py::_sync_roles`, `tests/test_user_management.py` |
| T48 | 2026-08-11 | **Reconciled tests with the user's own `roles.py` changes** — `public_fields(role)`'s `created_at`/`updated_at`/`deleted_at` switched from `api.iso_z` to `api.human_date` (display strings, `deleted_at: ""` not `null`), and `role_name`/`last_updated` (duplicates) removed from `roles/list`. Updated the 3 tests these broke to assert the new, intentional shape rather than the old one; confirmed live | ✅ done | `code` + `local-test` (all role/user suites) + `probed` (live) | `tests/test_role_management.py` |
| T47 | 2026-08-11 | **`roles/update` wasn't bumping `updated_at` on a grants-only change** — found via live multi-scenario testing (T50): a request carrying only `permission_ids`/`resource_grants` (no `name`/`description`/`is_active`) skipped `role.save()` entirely, since that call only happened inside the `if fields:` branch. Fixed with an `elif` branch that saves just `updated_at` when only grants changed. New test backdates `updated_at` via `.update()` (bypasses `auto_now`) to assert the bump without depending on wall-clock precision | ✅ done | `code` + `local-test` (554) + `probed` (live, 2-second-gap timestamps) | `apps/access_management/services/roles.py::update_role` |
| T46 | 2026-08-11 | **`RoleService._sync_grants`'s raw `ValueError`/`IntegrityError` leaked as unhandled 500s** — `create_role`/`update_role` only caught `IntegrityError`; an invalid `permission_ids`/`resource_grants` payload raised a bare `ValueError` the view's `except AccessManagementError` couldn't catch. New `InvalidGrant(AccessManagementError)` (400) — `create_role`/`update_role` now catch `ValueError` and translate it, logging the real reason server-side. Also hardened `_sync_grants` itself: its own `bulk_create`/`update_or_create` now catch a concurrent-write `IntegrityError` and re-raise as `ValueError`, so a race routes through the same translation instead of being misattributed by `_classify_conflict` (which only knows about role-name conflicts) as an unattributed 500 | ✅ done | `code` + `local-test` (553) + `probed` (live: `400 INVALID_GRANT` not 500) + `manage.py check` | `apps/access_management/services/roles.py::InvalidGrant` |
| T45 | 2026-08-11 | **`CatalogService.get_tree` refactor + 3 real bugs fixed**, all live-verified. (1) Category filter was a no-op in the default grouped view (`?category=database` still returned non-empty `datalake`/`file_system`) — only the lazy-load branch honoured it; now applies identically everywhere. (2) Descendant-grant inheritance never resolved past the exact granted path — the ancestor walk split on `.` while this codebase's paths are `:`-delimited, so a parent's `ALLOW` never reached its children; rewrote to use `resource_path.prefixes()`, the same helper the runtime resolver (`services/resolver.py`) already uses, so tree display and actual enforcement now agree. (3) `name` was the full path, not the leaf segment (same `.`-vs-`:` mistake). Bonus: search now reaches every level once a search term is given, not just roots. 120-line function split into 5 small helpers (`_resource_grants`, `_has_children_index`, `_nodes`, `_node`, `_resolve_effect`). +15 regression tests | ✅ done | `code` + `local-test` (553, +15 new) + `probed` (live, before/after on all 3 bugs) + `manage.py check` | `apps/access_management/services/catalog.py::CatalogService.get_tree`, `tests/test_catalog_resources.py` |
| T44 | 2026-08-09 | **Correction to T43 — GET-only, not GET+POST.** User asked for POST to be removed outright from the 14 read-only endpoints rather than kept alongside GET, so nothing can silently keep using the non-cacheable verb. Renamed `def post` -> `def get` (no alias) on all 12 `AdminView` subclasses + 2 `apps/chat` views; POST now 405 on every one. Much larger test-fix surface than T43's: every functional test (list filters, pagination, dropdown, effective-permission decisions) called these endpoints via POST through shared per-file helpers — fixed by changing each helper to `.get()` (one line fixes dozens of tests); `test_gate.py` had 16 call sites using these endpoints as Gate-2 stand-ins, fixed via targeted regex + 2 manual (a call-expression receiver the regex correctly couldn't match, and one parametrized generic-`url` test) | ✅ done | `code` + `local-test` (618 + `test_gate.py`'s 23 confirmed separately) + **`live`** (POST now 405, GET unchanged, re-verified on the running stack with no rebuild) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-09 (correction), `VEDA_Postman_Collection.json` "03b" folder |
| T43 | 2026-08-09 | **Additive GET support on 14 read-only endpoints.** User questioned the all-POST convention; after discussing the real tradeoff (GET is cacheable/bookmarkable/safely-retryable behind the nginx already fronting this stack — POST is neither), added GET as an ADDITIONAL method on the 14 endpoints with no side effects (`users/roles/permissions/catalog` list/detail/dropdown/effective, `conversations/list`+`history`), leaving every mutating endpoint (create/update/delete/assign/grant/query/login) POST-only. One shared line in `AdminView.validate()` picks `query_params` vs `data` by method; each read-only view gets a one-line `get = post` alias. Zero behavior change for existing POST callers — verified live (gunicorn autoreload picked up every change with no rebuild): GET returns real data on 3 sampled endpoints, POST unchanged, GET on a mutating endpoint still 405 | ✅ done | `code` + `local-test` (618, 6 files' tests updated not weakened) + **`live`** + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-09, `apps/access_management/views/base.py::AdminView.validate` |
| T42 | 2026-08-08 | **🔴 CRITICAL — live/manual testing found `VEDA_RBAC_MODE` was never wired into Django settings.** `config/settings/base.py` had no line copying the env var onto a setting; `rbac_mode()`'s fallback triggers on an ABSENT attribute, not an absent env var, so a real deployment silently ran RBAC-off forever regardless of configuration — missed by 3 audit rounds and 88+ tests because every test sets the mode via `override_settings()`, which bypasses this exact wiring. Found by starting the real docker-compose stack, creating a real zero-permission user, and getting a real 200-with-data answer from `/api/v1/conversations/query` (the actual production query API) where a 403 was expected. Fixed with one line; added `tests/test_settings_env_wiring.py` (3 tests, subprocess-based, deliberately NOT using `override_settings`) so this exact class of bug can't silently regress. Re-verified live post-fix: zero-permission user → 403 (no leakage); staff → 200 (unaffected) | ✅ done | `code` + `local-test` (501, +3 new) + **`live`** (real docker stack, real HTTP, real JWT, real DB) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-08 (CRITICAL), `config/settings/base.py`, `tests/test_settings_env_wiring.py` |
| T41 | 2026-08-08 | **Gate 1 — closed the Round-2 audit's remaining Medium findings.** Fixed `/v1/retrieve` (dormant debug endpoint, zero current caller, but zero RBAC too) by applying the existing `filter_retrieval_results`. Fixed NoSQL sources having no table/column enforcement at all — new `filter_nosql_collections()`, the NoSQL mirror of `filter_sm` for connector-native `NoSQLCollection` objects, wired into `_run_nosql` before the LLM query-builder sees the schema. Verified (correcting an audit assumption) that the Django-side catalog already works for NoSQL sources unmodified — `CatalogDiscoveryService` is dialect-agnostic and ingestion's shared schema pipeline already covers relational/datalake/nosql uniformly; only the engine side was missing the check | ✅ done | `code` + `local-test` (454: 419 combined + 33 + 2 standalone) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-08 (closing remaining findings), `veda_core/veda/rbac_filter.py::filter_nosql_collections`, `inference/routes/retrieve.py` |
| T40 | 2026-08-08 | **Gate 1 production audit + fix — closed the real table/column bypass.** Principal-Engineer-format audit found candidate-list filtering (Task 16) doesn't stop a forbidden table/column reaching generated SQL: `select_primary_table`, `anchor_hint`, single-table column derivation, FK/entity expansion, and `try_multitable` all consult raw `sm`/a RBAC-oblivious global FK graph independently — plus two MORE bypasses found while fixing it (FastPath and verified-cache replay skip retrieval, and therefore the filter, entirely). **Fix**: one centralized `narrow_allowed()` gate wired into all 4 `validate_and_parameterize(...)` call sites (Tier-1 + 3 Tier-2 paths) — reuses that function's EXISTING hard-allowlist AST enforcement rather than patching every discovery site. Also fixed `apps/chat/views.py::_resolve_user`'s silent dummy-admin substitution for unauthenticated requests (401 now fires, as every call site already expected). +12 tests incl. 3 end-to-end proofs against the real SQL firewall | ✅ done | `code` + `local-test` (445: 419 combined + 26 standalone for the veda_core-only file) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-08 (post-audit fix), `veda_core/veda/rbac_filter.py::narrow_allowed`, `apps/chat/views.py` |
| T39 | 2026-08-08 | **Gate 1 (User Story 3) Task 17 — 403 handling + centralized resolution (GATE 1 COMPLETE).** New `resolve_effective_permissions(user)` — the ONE per-request resolver call, threaded into both `permitted_source_ids` (renamed from `_permitted_source_ids`, made public) and `compute_data_scope` instead of each independently re-resolving. Both views now short-circuit to a generic 403 (own existing response shape each — no new envelope invented) the instant `permitted_source_ids` returns an empty set, BEFORE any scope resolution or engine/inference call. **Two self-caught bugs before shipping**: a module-level import broke the minimal-Django test harness (10 failures) — reverted to the same lazy-import pattern Task 13 established; the lazy-import branch initially ran even for a plain `user=None` call, regressing the original zero-import fast path — fixed by checking `user is None` first. `/api/v1/query` and `/api/v1/conversations/query` now enforce end to end: authorized/unauthorized/multi-role/full/partial/RBAC-off/admin-bypass all verified over real HTTP with the 403 body checked for zero resource/table/column/RBAC-vocabulary leakage | ✅ done | `code` + `local-test` (417: new file's 11 + 406 existing) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-08, `apps/query/views.py`, `apps/chat/views.py`, `tests/test_gate1_authorization.py` |
| T38 | 2026-08-08 | **Gate 1 (User Story 3) Task 16 — engine-side RBAC filtering.** New `veda_core/veda/rbac_filter.py` filters the retrieval CANDIDATE LIST after `get_engine(sm).retrieve(...)`, never the shared per-scope engine/index itself. **Self-caught regression before shipping**: first draft filtered `_load_semantic_model()`'s return value — the SAME object handed to `get_engine(sm)` — which would have baked one user's permissions into the shared, per-scope-cached engine permanently; reverted immediately on re-reading the call graph. Matches the engine's own `col_id.rsplit(".", 1)` convention so the multi-source merge's 3-dots-deep `src{ID}.table.column` qualified names split correctly. Known scoped limit flagged (not silently claimed covered): only the retrieval-driven candidate path is audited, not every `sm`-consuming code path in the ~1000-line `run_query` (existence checks, FK-join expansion, anchor-hint salvage) | ✅ done | `code` + `local-test` (422: new file's 15 + 407 existing; 2 pre-existing unrelated failures) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-08, `veda_core/veda/rbac_filter.py`, `tests/test_rbac_filter.py` |
| T37 | 2026-08-07 | **Gate 1 (User Story 3) Task 15 — data-scope payload across the HTTP boundary.** Confirmed chat's engine call and `/api/v1/query` converge on the SAME `InferenceClient` (chatbot runs in-process in the api container) — one client to modify, not two. Threaded `serialize_data_scope(compute_data_scope(...))` (plain JSON dict, never the Django user/dataclasses) through both views → chat's LangGraph state → `call_engine_node` → a new `X-Veda-Data-Scope` header, omitted when there's no restriction (matches the `X-Veda-Source-Ids` precedent). `veda_core/context.py::RequestContext` gets a new `allowed_resources` field — a Django-free hashable nested-tuple, keeping both the "no Django in veda_core" and "no veda_core in api tier" boundaries intact. `inference/main.py`'s middleware fails CLOSED (`()`, not `None`) on a malformed header | ✅ done | `code` + `local-test` (407: new file's 15 + 392 existing; 2 pre-existing unrelated failures reproduced identically on the pre-change tree) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-07, `veda_core/context.py`, `tests/test_gate1_data_scope_wiring.py` |
| T36 | 2026-08-07 | **Gate 1 (User Story 3) Task 14 — table/column allow-payload.** New `compute_data_scope(user, source_ids)`: "fully open" (bare ALLOW + no deeper DENY, no enumeration) vs "restricted" (walked table-by-table, then column-by-column, via `EffectivePermissions.allows()`'s own deny-wins semantics — not reimplemented). **Bug caught by a test**: a table reachable only via column-level grants (no redundant table-level ALLOW — a legitimate admin pattern) was being silently excluded; fixed by checking every non-open table's columns directly rather than gating on the table path's own `allows()` first. Resolves real table/column names via `CatalogResource.substrate_id` → `SchemaTable`/`SchemaColumn`, not the lowercased resource-path segment. Fails closed when no catalog projection exists for a source | ✅ done | `code` + `local-test` (376: new file's 13 + 363 existing) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-07, `apps/access_management/services/data_scope.py`, `tests/test_data_scope.py` |
| T35 | 2026-08-07 | **Gate 1 (User Story 3) Task 13 — source-level scope narrowing.** Both real entry points (`/api/v1/query`, chat's `conversations/query`) converge on one function, `apps.query.scope.resolve_query_scope()`; added an optional `user=` param there instead of duplicating logic per-view — no caller passing it stays byte-identical. New `_permitted_source_ids()` derives permitted sources straight from `EffectivePermissions.grants` (`data.read`/ALLOW/non-global); staff (`is_staff`) bypass narrowing entirely — required because the seeded Admin role's grants are all global and a global grant never opens a specific source. **Bug caught before shipping**: `resource_path.build()` lowercases source names but `Source.name` is stored as typed — a plain `name__in` lookup would silently drop any mixed-case source; fixed with `Q(name__iexact=...)`. **Known gap surfaced, not fixed here**: when RBAC narrows to zero sources, `resolve_query_scope()` still falls back to the dev default source rather than `[]` — pinned with an explicit test rather than silently patched, since the 403 decision belongs to the view layer (Task 17) | ✅ done | `code` + `local-test` (111: new file's 13 + 98 existing) + `manage.py check` | `RBAC_PROGRESS_LOG.md` §2026-08-07, `apps/query/scope.py`, `tests/test_query_scope_rbac.py` |
| T34 | 2026-08-07 | **Catalog auto-sync on ingestion success** — closes the long-flagged gap ("Discovery is manual, not triggered by ingestion, catalog goes stale after every re-ingest"). New `VEDA_AUTO_SYNC_CATALOG` (default OFF — `NEVER hamper existing pipeline` rule honoured: flag off, `task_ingest_source` is byte-identical to before). Hooked at the exact point `Source.ready` flips (`apps/ingestion/tasks.py:254-257`, the spot `CatalogDiscoveryService`'s own docstring already pointed at). Sync failure is caught and logged, never raised — a broken catalog projection must not turn a successful ingestion job into a failed one. `apps/ingestion` had zero test coverage before this; added a focused suite for the new function only (flag off = no-op, flag on = calls sync_source, failure = swallowed) | ✅ done | `code` + `local-test` (544, +4 new) + `manage.py check` | `apps/ingestion/tasks.py::_sync_catalog_if_enabled`, `tests/test_ingestion_catalog_sync.py` |
| T33 | 2026-08-07 | **`Role.deleted_at`** — same semantics as `UserProfile.deleted_at` (T31/T32), but a plain column on `Role` itself, not a related table: this app owns the model outright, no Django-swap risk like `auth_user`. `RoleService.update_role` stamps/clears it in the same write as the guarded `is_active` toggle. Moved into the shared projection (`public_fields`, not a list-only extra) since it's a real column every role endpoint can show. Migration 0009 | ✅ done | `code` + `local-test` (540, +3 new) + `manage.py check` | `apps/access_management/models/roles.py` |
| T32 | 2026-08-07 | **`users/delete` + `roles/delete`** — dedicated endpoints, per user's ask. Both a named convenience over the existing soft-delete path (`update {is_active: false}`), NOT a new code path — same guards apply for free (last-admin protection, token revocation, idempotency). No row ever removed. Replaced a now-obsolete test (`test_there_is_no_delete_endpoint`, from when role assignment didn't exist) with real coverage of the new route | ✅ done | `code` + `local-test` (537, +12 new) + `manage.py check` | `apps/access_management/views/{users,roles}.py` |
| T31 | 2026-08-07 | **`users/list`/`roles/list` — frontend-requested fields, additive** (user brought a target JSON shape; corrected mid-build twice: no camelCase pagination duplicates, no field-name duplicates — only genuinely missing fields get added). Added: `roles` per user (1 batched query), `users_count`/`connected_sources` per role (2 batched queries, kind→label from `resource_path.kind_of`), one shared `apps/core/api.py::human_date()` (alongside `iso_z`, not replacing it). `updated_at`/`deleted_at` needed a real column — new `UserProfile(TimeStampedModel)` (migration 0008), NOT a column on `auth_user`. `deleted_at` is a timestamp ON the `is_active` decision, not a second flag — `UserService.update_user` stamps/clears it in the same transaction as the guarded `is_active` write. Lazy backfill (`get_or_create`) for the users that predate the table | ✅ done | `code` + `local-test` (525, +17 new) + `manage.py check` | `apps/access_management/models/profile.py`, `apps/core/api.py::human_date` |
| T30 | 2026-08-07 | **`roles/dropdown`** — every active role, unpaginated, `{role_id, name}` only. User's explicit call: a separate endpoint, not a reuse of `roles/list` (which stays paginated/searchable for the admin table). `RoleService.list_active_roles()` — 1 query, ordered by name. Resource-tree question answered without new code: `catalog/list {parent_path}` already IS a lazy-loaded tree API (confirmed against its own docstring); the one real gap (`for_role_id` grant-state per node) stays backlog R2, not built | ✅ done | `code` + `local-test` (514, +8 new) + `manage.py check` | `apps/access_management/services/roles.py`, `apps/access_management/views/roles.py` |
| T29 | 2026-08-07 | **User Story 1 — Admin Onboarding, remaining gaps.** Codebase inspection first found the brief's "already implemented" list was partly wrong: no self-registration endpoint exists anywhere, no activate/deactivate/password-change endpoint existed, and login had no role/staff check — but login lockout WAS already fully built (reused, not rebuilt). Delivered: (1) migration 0007 seeds an "Admin" `Role` + grants it every existing permission; (2) `AdminBootstrapService` + `manage.py bootstrap_admin` — backend-only, race-safe via `select_for_update` on the seeded Admin role row, first user gets BOTH `is_staff=True` and the Admin role (user's explicit call — "use both", not one or the other); (3) `admin_guard.py` — one `is_last_active_admin()` predicate, reused by `UserService.update_user` (deactivation) and `UserRoleService.revoke` (Admin-role removal); (4) `is_active` folded into `users/update` rather than separate activate/deactivate endpoints (user's explicit call), same for "delete" — no hard-delete endpoint, deactivation via `update` IS the platform's "delete" (user's explicit call); (5) `PasswordComplexityValidator`, configurable via `AUTH_PASSWORD_VALIDATORS` OPTIONS; (6) `POST /auth/password/change`, its own throttle scope; (7) login response carries `roles`+`permission_codes` immediately, gated on `VEDA_JWT_AUTH` so the legacy contract stays byte-identical. **Found and fixed a real bug**: `UserService.deactivate_user`'s first draft tried `save(update_fields=["is_active", "updated_at"])` — `django.contrib.auth.User` has no `updated_at` field at all, that's a `TimeStampedModel`-only column. **Found a serious pre-existing issue, flagged, did not touch**: `apps/chat/migrations/0002_seed_dummy_admin_user.py` seeds a permanent `username=admin` dev-fallback user into every migrated database (dev AND prod) — its own docstring says "remove this migration... once the real authentication service replaces LoginView", which happened weeks ago. Left as-is pending a decision (see below) — it does NOT break `bootstrap_admin` today only because that command checks `is_staff=True` count, and the dummy user is never staff | ✅ done | `code` + `local-test` (506, incl. 38 new) + `manage.py check` | `apps/access_management/services/bootstrap.py`, `admin_guard.py`, `apps/authentication/password_validators.py`, `tests/test_admin_bootstrap.py` |
| T28 | 2026-08-07 | **Dropped the dead `MSG_*` indirection layer** flagged by the user as a readability mess: `CODE_ROLE_NOT_FOUND = "..."` / `MSG_ROLE_NOT_FOUND = MESSAGES[...]` / `message = MSG_ROLE_NOT_FOUND`. Checked usage first — `CODE_*` constants have real external consumers (imported by tests, re-exported via `services/__init__.py`) so they stay; `MSG_*` had none beyond their own one-line class-attribute assignment, pure dead indirection. Inlined `message = MESSAGES["domain"]["key"]` directly into every exception class across 6 files (`roles/users/permissions/catalog/grants` services + `authentication/services.py`); removed 11 now-dead `MSG_*` names from `services/__init__.py`'s imports and `__all__`. `chat/services.py` left untouched — its `MSG_MODEL_ERROR` IS imported by `views.py`, a real cross-module contract | ✅ done | `code` + `local-test` (468) + `manage.py check` | `apps/access_management/services/*.py`, `apps/authentication/services.py` |
| T27 | 2026-08-06 | **De-duplicated hardcoded strings, both layers** (user flagged the first pass as incomplete — services.py error messages were still literal). New `apps/access_management/codes.py::PermissionCode` (8 constants matching migration 0004) replaces 19 raw `required_permission = "..."` literals across every access-management view. New `apps/core/messages.py::MESSAGES` (single nested dict) is now the ONE home for every success/failure string on both sides of the request: the ~24 `api.success(...)` copy strings in views AND the `MSG_*` constants each `services.py` attaches to its typed exceptions (`RoleNotFound`, `InvalidCredentials`, etc.) — covers `access_management` (all 5 service files), `authentication/services.py` and `chat/services.py`. A typo in either now raises `AttributeError`/`KeyError` at the call site instead of silently never matching. Also caught that **dev DB migrations had never been applied** — ran them, confirmed the seeded 8 permissions are real rows now | ✅ done | `code` + `local-test` (468) + `manage.py check` | `apps/access_management/codes.py`, `apps/core/messages.py` |
| T26 | 2026-08-06 | **Backlog broadened to the whole identity programme** — was auth-only, now carries an `R`-series for `apps/access_management` (filename kept so all cross-refs survive). Added **R1** bulk grant `sync` endpoint (a picker UI cannot save atomically today — 15 checkboxes = 15 non-atomic calls), **R2** `catalog/list?for_role_id` so the tree shows effective state server-side instead of the client re-implementing prefix inheritance, **R3** the silent-no-op filters, plus R4–R7 from audit #2 and the delegated-admin gap | ✅ done | `code` | `AUTH_ISSUES_BACKLOG.md` §R |
| T25 | 2026-08-06 | **Verified resource-permission behaviour end-to-end across connectors** (live probe, not prose). Found that **ADR §3.3 showed an unproducible path**: `files:contracts_s3:msa_2024.pdf`. `document.py` emits no `RawTable`, so document sources project to **one** resource — relational/datalake/tabular reach source→table→column. Corrected the ADR with a new §3.3a mapping connector → achievable depth, and recorded the product decision that a document source is granted **whole**. Also probed all 6 list endpoints: pagination/caps/ordering correct everywhere, but **`search` and `is_active` are silent no-ops on the two grant list endpoints** | ✅ done | `probed` | `docs/adr/0001-rbac-resource-path.md` §3.3a |
| T24 | 2026-08-06 | **Implementation audit #2** — **6.5/10** (was 6.0). **~64% of the phase programme, still 0% data protection.** Four new findings: **A2** `AuthService` is a god class (measured 21 methods vs 6–7 everywhere else), **A3** `_log_context` duplicated 4×, a boolean-short-circuit-with-side-effect at `gate.py:95`, and the test blind spot that let Q1 survive — 401/403/429 **bodies** are still never asserted, now at 468 tests. Recommends **Gate 1 Phase A** (source-level authorization on `/api/v1/query`), not the cache | ✅ done | `probed` | `VEDA_RBAC_AUDIT_2.md` |
| T23 | 2026-08-06 | **Gate 2 — API authorization** (Phase 8, reordered ahead of the cache per the brief's own "cache only after correctness"). `RequiresPermission` DRF class + **`VEDA_RBAC_MODE` = off / shadow / enforce**, default **off**. Wired onto all 19 admin endpoints **alongside** `IsAdminUser`, never instead of it — so enforcement is strictly narrower than staff alone and no configuration can grant what was previously refused. Shadow mode logs only what it *would* refuse, so the exception list is greppable before flipping. Fail-closed on a view that declares nothing; fail-**open** on an unrecognised mode string (a typo must not take a deployment offline) | ✅ done | `code` + `local-test` (23) | `RBAC_PROGRESS_LOG.md` §8 |
| T22 | 2026-08-06 | **Permission resolver** (Phase 5) — `resolve(user) → EffectivePermissions`, frozen/immutable, **one query** for the whole graph and **zero queries** per decision. Implements ADR §3.5: DENY unpierceable at any depth, absence-of-grant = denial, inactive user/role/permission contributes nothing, and a blank-path grant does **NOT** cover concrete resources. Plus `POST users/permissions/effective` (Phase 6's "List Effective Permission") which answers a specific question server-side so clients never re-implement prefix inheritance. **Resolves only — still enforces nothing** | ✅ done | `code` + `local-test` (35) | `RBAC_PROGRESS_LOG.md` §8 |
| T21 | 2026-08-06 | **Documentation sync + tracker audit.** `ACCESS_MANAGEMENT_API_CONTRACT.md` was **two features stale** — added §7 (catalog + resource-path grammar) and §8 (grants), a top-of-file ⚠️ that nothing is enforced, TS types, and a rewritten "not built yet". Added the two missing `RBAC_PROGRESS_LOG.md` work-log entries (catalog, grants) and refreshed its snapshot; fixed an orphaned `M9` row sitting outside the §6a table. Audited PM_LOG against disk and found three unlogged items — the contract's own creation, `admin.py`, and this sync | ✅ done | `code` | this file · `ACCESS_MANAGEMENT_API_CONTRACT.md` §7-§8 |
| T20 | 2026-08-06 | **Grants — `UserRole` + `RolePermission`** (migration 0006) + `users/roles/{assign,revoke,list}` and `roles/permissions/{grant,revoke,list}`. Idempotent (201 new / 200 existing), **one decision per `(role, permission, resource)` triple** so ALLOW↔DENY updates rather than contradicting, per-edge `granted_by` audit, cascade rules chosen individually. No approval workflow (confirmed out of scope). **The RBAC graph is now complete — and still enforces nothing** | ✅ done | `code` + `local-test` (44) | `ACCESS_MANAGEMENT_API_CONTRACT.md` §8 |
| T19 | 2026-08-06 | **Catalog addressing implemented** (ADR-0001) — `resource_path.py` (pure, segment-boundary matching), `CatalogResource` + discovery/reconciliation, `catalog/{list,detail}` read-only, `manage.py sync_catalog`. Self-review caught **`bulk_update` does not call `pre_save`**, so reactivation was writing back a stale `updated_at`; plus a `parent_path` docstring that contradicted its own code | ✅ done | `code` + `local-test` (68) | `docs/adr/0001-rbac-resource-path.md` |
| T18 | 2026-08-06 | **ADR-0001 — RBAC resource path spec** (Phase 2), now **ACCEPTED**. Inspection contradicted the proposed path shape on two blocking points: VEDA has **no schema level** (`relational.py:117` scans one schema, stores bare table names) and `postgres` is a *dialect*, not a unique instance. Proposed `<kind>:<source>[:<segment>]*` derived from the existing `Source.dialect` taxonomy. §7 tenancy **DECIDED (option 1)**. Then **STOPPED per the FINAL RULE**: my own §3.7 FK design would have destroyed grants — `writer.py:137-139` deletes and recreates the whole substrate on every re-ingestion, so CASCADE wipes the catalog + grants and PROTECT breaks ingestion. Amended to no-FK + reconciliation | ✅ approved | `probed` | `docs/adr/0001-rbac-resource-path.md` §3.7 |
| T17 | 2026-08-06 | **Formal architecture + security audit** of the whole identity/RBAC programme. Scored **6/10** overall; **~30% of the phase programme, 0% of the outcome** — nothing is authorized yet. Found **Q1**: 401/403/429 escape the platform envelope (DRF `{"detail":...}`), which 338 tests missed because they asserted status codes only. Flagged **A4**: multi-tenancy would force a redesign, not an extension. Next: catalog addressing, not the resolver | ✅ done | `probed` | `VEDA_RBAC_ARCHITECTURE_REVIEW.md` |
| T16 | 2026-08-06 | **Permission catalogue** — `access_management_permission` table + 8 seeded permissions (migration 0004) + `POST permissions/{list,detail}`. **Read-only on purpose**: only code can enforce a permission, so no create/update/delete, guarded by tests. Inspection finding: `CatalogResource` would DUPLICATE the existing `Source`/`SchemaTable`/`SchemaColumn` catalog — deferred to the grant phase as a thin registry, not a copy | ✅ done | `code` + `local-test` (38) | `ACCESS_MANAGEMENT_API_CONTRACT.md` §6 |
| T15 | 2026-08-06 | **Role management** — `access_management_role` table (custom model, not `auth.Group`) + `POST roles/{create,detail,list,update}`. No delete: retirement is `update {is_active:false}`. Extracted the shared paging contract (`PaginatedListSerializer`, `paginate()`, `pagination_payload()`) now that roles is the second consumer, and replaced the per-class error→status registry with semantic `ConflictError`/`NotFoundError` bases after the tests caught it silently 400-ing every role conflict. `apps/access_management/admin.py` starts here; each later domain adds its own read-only `ModelAdmin` | ✅ done | `code` + `local-test` (77) | `ACCESS_MANAGEMENT_API_CONTRACT.md` §5 |
| T14 | 2026-08-05 | **`POST users/detail`** + **`apps/access_management` restructured into packages** (`serializers/`, `services/`, `views/`, `urls/` — one module per domain, shared pieces in `base.py`, public names re-exported from each `__init__`) so roles/permissions can land beside users. No endpoint behaviour changed | ✅ done | `code` + `local-test` (104) | `ACCESS_MANAGEMENT_API_CONTRACT.md` §2 |
| T13 | 2026-08-05 | **User list + update** — `POST users/list` (paginated, capped page_size, allowlisted ordering, stable tiebreak, search, is_active tri-state) and `POST users/update` (partial, 404/409, username+password+flags refused). **All endpoints moved to `POST <resource>/<action>`** to match `apps/chat`; user object unified across create/list/update. Test suite 171s → 13s via a fast test hasher | ✅ done | `code` + `local-test` (90) | `ACCESS_MANAGEMENT_API_CONTRACT.md` §2-§3 |
| T12 | 2026-08-05 | **User creation foundation** — new bounded context `apps/access_management` (RBAC home) + `POST /api/v1/users/create` (staff-only, atomic, DB-enforced case-insensitive email uniqueness). Envelope de-duplicated into `apps/core/api.py` and `apps/chat` + `apps/authentication` refactored onto it. Caught a migration that applied with **no index actually created** (sqlite table rebuild), and two bugs of my own in self-review. Also **created `ACCESS_MANAGEMENT_API_CONTRACT.md`**, split out of `AUTH_API_CONTRACT.md` §7 so verification and administration have separate frontend contracts | ✅ done | `code` + `local-test` (51) + `probed` | `RBAC_PROGRESS_LOG.md` §8 · `ACCESS_MANAGEMENT_API_CONTRACT.md` |
| T11 | 2026-08-05 | **`AUTH_ISSUES_BACKLOG.md`** — every open auth finding written up for later: fix sketch, files, effort, acceptance criterion each, plus 4 suggested batches. `RBAC_PROGRESS_LOG.md` §6a reduced to history + a pointer so the lists cannot drift. Found while writing it: **L3 was already fixed** incidentally by C2 | ✅ done | `code` | `AUTH_ISSUES_BACKLOG.md` |
| T10 | 2026-08-05 | **Re-review after Phase 1.1** — confirmed C1/C2/C3/H1/H4 closed by independent probe; found 5 new minor issues (N1–N5) incl. the residual distributed-brute-force trade-off C1's fix accepts | ✅ done | `probed` | Verdict: ❌ CHANGES REQUIRED — *merge-safe (flag off), not safe to enable*. `RBAC_PROGRESS_LOG.md` §6a |
| T9 | 2026-08-05 | **Phase 1.1 fixes**: C1 lockout DoS (two-tier counter), C2 fail-open break, C3 password-change revocation, H1 access-token test gap. Bonus: H4 log flooding; found+fixed **M9**, a pre-existing DRF throttle bypass (`NUM_PROXIES` unset) | ✅ done | `code` + `local-test` (79) + `probed` | `RBAC_PROGRESS_LOG.md` §8 (2026-08-05 Phase 1.1) |
| T8 | 2026-08-05 | **`AUTH_API_CONTRACT.md`** — standalone frontend contract; RBAC endpoints listed §7 as not-built with a definition-of-done. De-duplicated: auth detail removed from `docs/QUERY_API_FRONTEND_CONTRACT.md`, pointer added there and in `CHAT_API_CONTRACT.md` | ✅ done | `code` | `AUTH_API_CONTRACT.md` |
| T7 | 2026-08-05 | **Production-readiness review #1** of the auth module — 3 criticals, 4 high, 8 medium. Two of my own claims disproved by measurement (long-password DoS not real; deactivated access tokens *are* rejected) | ✅ done | `probed` | Verdict: ❌ CHANGES REQUIRED. `RBAC_PROGRESS_LOG.md` §6a |
| T6 | 2026-08-05 | **`RBAC_PROGRESS_LOG.md`** created as the auth/RBAC living doc (kept out of `VEDA_PROGRESS_LOG.md` per user's call) | ✅ done | `code` | `RBAC_PROGRESS_LOG.md` |
| T5 | 2026-08-05 | **Auth test suite** — 59 tests: unit, integration, negative, security (enumeration parity, lockout, cache fail-open), deterministic concurrency, edge cases. Self-bootstrapping sqlite harness, no repo-wide `pytest.ini` | ✅ done | `local-test` | `tests/test_authentication.py` |
| T4 | 2026-08-05 | **`POST /api/v1/auth/logout`** — idempotent, always 200, per-session, ungated by the rollout flag (revocation never grants) | ✅ done | `code` + `local-test` | `RBAC_PROGRESS_LOG.md` §2 |
| T3 | 2026-08-05 | **`POST /api/v1/auth/refresh`** — single-use rotation, race-safe via blacklist-INSERT-as-arbiter (closes simplejwt's own TOCTOU window), replay ⇒ account-wide revocation, verify-before-mutate | ✅ done | `code` + `local-test` | `RBAC_PROGRESS_LOG.md` §2 |
| T2 | 2026-08-05 | **`POST /api/v1/auth/login`** — active users only, single generic 401 (no enumeration), access + rotating refresh, per-account lockout. Dummy login **moved** out of `apps/chat` (−71 lines), URL unchanged | ✅ done | `code` + `local-test` | `RBAC_PROGRESS_LOG.md` §2 |
| T1 | 2026-08-05 | **Auth codebase analysis + gap analysis + implementation plan** — established that no JWT existed anywhere and the only login was a dummy returning `"dummy_access_token"`; 4 decisions taken with the user (simplejwt · new app · `/api/v1/` prefix · flag-gated default-OFF) | ✅ done | `code` | `RBAC_PROGRESS_LOG.md` §4 decisions register |

---

## Notes

- Branch: `feat/rbac-veda` (HEAD `baaac67`, PR #24 merge). **Every identity/RBAC change
  in this log is an uncommitted working-tree change** — ~205 modified/untracked paths,
  nothing committed or pushed.
- `VEDA_JWT_AUTH` is **default `0`**, so none of the above changes production
  behaviour yet. Login still returns the legacy placeholder payload until it flips.
- **Current surface**: **22 endpoints** (auth 3, users 4, roles 4, permissions 2,
  catalog 2, grants 6, effective-permissions 1), 1 management command, 6 migrations,
  **604 tests** (up from 468 as of T24 — Gates 1/2, T29-T44's endpoint work, and
  T45-T49's fixes each added their own coverage). Superseded by T45-T50: this
  round's fixes and the admin/role/user endpoints have now also been run **`live`**
  against the real docker-compose stack (not just `local-test`) — still **not
  `frontend`**.
- **What all of it does to a request today**: `VEDA_RBAC_MODE` defaults `off` in
  code but was live-verified `enforce` on the running dev stack this session
  (T42's fix). The data path (`/api/v1/query`, `conversations/query`) is gated —
  Gate 1 (T35-T41) — and admin endpoints are gated — Gate 2 (T23) — both confirmed
  live in T50's testing round, not merely by `local-test`.
| 2026-08-27 | Source isolation (datalake-only SM) | live-verified: isolates clean-named datalake queries + homzhub parity (7,107), but avg/filter/group still fail (graph-expand leak + aggregate-recognition, 2 more layers) — flag KEEP OFF | SOURCE_ISOLATION_REPORT.md |
| 2026-08-27 | Shared aggregate-measure fix (qualifier gate + evidence guard) | root-caused "average→clarify" as 2 shared gates (qualifier_completeness treats AVG-verb as missing token; evidence-guard demotes metric measure pick); both fixed flag-gated (AGG_VERB_COMPLETENESS_ENABLED); live homzhub avg rent=47927.60 + avg carpet=1.73M via fast-path, no SLM; 4/4 new tests + full regression green; datalake still needs metric registration (follow-up) | AGGREGATE_COMPLETENESS_FIX.md |
| 2026-08-27 | Real-data smoke A/B: AGG_VERB_COMPLETENESS on homzhub prod | aggregates 0/3->3/3 answered (avg carpet/rent/total); counts byte-identical off/on (7107/211); total-carpet OFF gave silent-wrong SQL, ON correct SUM; real-data validated + prod-safe -> ready to enable | AGGREGATE_COMPLETENESS_FIX.md |
| 2026-08-27 | Real-data reliability measurement (20 homzhub queries) | 30% correct, 30% SILENT-WRONG, 40% safe-refuse/error; only count/SUM/AVG deterministic core reliable; refuse-over-guess LEAKING (filter-drop, group mis-project, superlative mis-anchor return confident-wrong); priority = make wrong->refuse not add coverage | REAL_RELIABILITY_MEASUREMENT.md |
| 2026-08-27 | Fast-path live dimension-value grounding | filtered-count "how many <entity> in <value>" now deterministic COUNT(*) WHERE col IN(casings) via live exact-match probe + dominance disambiguation; root=value-store miss->Tier-1 projection+LIMIT (Nagpur 100 vs true 2012); live-verified Mumbai81/Nagpur2012/Pune1642/Bangalore17 + parity 7107 + tie/absent refuse; 5/5 tests + 41 regression; flag FASTPATH_LIVE_DIM_GROUNDING_ENABLED default-OFF | FASTPATH_LIVE_DIM_GROUNDING.md |
| 2026-08-27 | P0 coverage measurement (14 homzhub core questions) | 43% correct (count/filt-count/avg incl A2,C2 from this session fixes), 14% silent-wrong (GROUP-BY A4/D2), 43% safe refuse/error; measured priority backlog: GROUP-BY #1 then RATIO then total/agg-filter; projects ~75-80% if fixed | COVERAGE_MEASUREMENT_P0.md + HOMZHUB_QUESTION_INVENTORY.md |
| 2026-08-27 | Deterministic edge-driven cross-source MULTI | cross-source JOIN queries (assets+maintenance) mis-routed SINGLE/INVALID_SLM->refuse; fix: query names both HIGH cross_source_fk edge endpoints -> override to federated MULTI (tuned decide untouched, retrieval-noise-independent); live X1/X2 refuse->MULTI+answer, X3 now deterministic, single-source parity intact; 5/5 tests + 50/50 routing regression; flag CROSS_SOURCE_EDGE_MULTI_ENABLED default-OFF | CROSS_SOURCE_EDGE_MULTI.md |
| 2026-08-27 | Extend thinking/progress events to multi-source coordinator | new coordinator path emitted no live "thinking" (routing/dispatch silent until answer); added _emit route-phase events: "Deciding which source", "Routing to the <type> source" (SINGLE), "Combining data across N sources" (MULTI), "Joining data across sources" (federated); additive, no-op when non-streaming, only in already-gated MULTISOURCE path; verified events fire SINGLE+CROSS; 26/26 routing/merge regression | veda_hybrid.py |
| 2026-08-27 | Routing benchmark: source-agent selection (NEW vs BASELINE) | 11 labeled queries by source; BASELINE 9/11 (82%): single-source perfect 8/8, cross-source 1/3; NEW 11/11 (100%): cross-source 3/3 via edge-MULTI, ZERO single-source regression; each agent (relational/datalake/cross) picks correctly | ROUTING_BENCHMARK.md |
| 2026-08-27 | 50-question routing benchmark (datalake+filesystem+collisions) | 44/50 (88%): homzhub control 12/12, DL 13/15, FS 9/12, SIM 10/11; 3 WRONG-homzhub grabs (amenities-collision #13, doc HR #21/#27) + 3 safe-refuse; root=concept-collision + weak doc-vs-DB tiering; homzhub mostly stays in lane (6% leak) | ROUTING_BENCHMARK_50.md |
| 2026-08-27 | Source-description prior (flag-gated) implemented + A/B | additive max-blend of query<->sources_source.description into aboutness signal; SOURCE_DESC_PRIOR_ENABLED default-OFF, tuned policy untouched, 9/9 tests + routing regression green; A/B 44->45 (fixed #13 amenities-collision, no regression) BUT target doc-HR #21/#27 STILL wrong-homzhub (homzhub item-prior volume-bias 0.418 > doc desc-prior 0.354, max-blend cant overcome); RECOMMENDATION: DO NOT enable — target metric FS 9/12 unchanged; real fix needs item-prior volume-normalization (follow-up) | SOURCE_DESCRIPTION_PRIOR_IMPLEMENTATION.md |
| 2026-08-27 | Chunk-into-tier blend (flag-gated) — doc-routing fix | ROOT: _dominance_retier tiers on item-prior, DISCARDS clean chunk evidence (doc chunk 0.594 > homzhub col 0.470 for every doc query); FIX: blend max(item_prior,chunk) pre-tier (column NOT blended-noisy); A/B FILESYSTEM 9/12->12/12 (target ACHIEVED, desc-prior failed this), net 44->45, cost -2 borderline (props-in-Pune, maint-charges: doc chunk legit co-leads->SLM); 4/4 tests + regression green; CHUNK_TIER_BLEND_ENABLED default-OFF; normalization diagnosed WORSE (max 7/10 vs z 3/10) | CHUNK_TIER_BLEND.md |
| 2026-08-27 | Refined chunk-blend (dominant-only) + confident-SINGLE SLM-skip | root: complex homzhub queries NONE because boundary SLM fired on confident-SINGLE (weak doc chunk close in raw top_score) + 7B SLM garbled to NONE/invalid (independent of chunk-blend); fix1 chunk promotes tier only if > best column (doc-HR keeps winning, weak doc no false co-leader); fix2 CONFIDENT_SINGLE_SKIP_SLM (1 STRONG -> skip SLM); 31-homzhub NONE 8->1, homzhub 20->28, properties 9.18s->0.8s; 6/6+4/4 tests + 50 regression green; both flag-OFF | CONFIDENT_SINGLE_SKIP.md |
| 2026-08-27 | MASTER read-only project audit (21-part) | code-verified truth: routing SPINE DONE+working (coordinator/agents/DB-single/FS-RAG/evidence/orchestration/safety) + session fixes ~98% homzhub; PARTIAL: datalake answer-quality (no datalake semantic layer), cross-source (semi-join works, enrich-intent weak, SYNTHETIC only), item-prior volume-bias, desc-prior REJECTED; MISSING: datalake semantic layer, DEPENDENT staged, real multi-source data; CRITICAL: all flags default-OFF (SHADOW ON) -> prod still legacy keyword path, nothing shipped | MASTER_PROJECT_AUDIT.md |
| 2026-08-27 | Grouped-intent shape guard (answer-safety, Prong A) | silent-wrong root: "how many X by Y" -> SLM PURE PROJECTION (no GROUP BY) -> summariser fabricates distribution ("62% Singapore"); fix: grouped_shape_ok() refuses grouped-intent + pure-projection (GROUP BY/aggregate/WHERE present -> pass, so filters not mis-guarded); live "properties by city" hallucination->REFUSE, controls unchanged; 6/6 tests; GROUPED_SHAPE_GUARD_ENABLED default-OFF | (answer-quality Prong A) |
| 2026-08-31 | Distinct/uniqueness shape guard (answer-safety, Prong A) | silent-wrong root: "how many UNIQUE/DISTINCT/DIFFERENT X" -> SLM drops DISTINCT -> plain COUNT(*) counts total rows (not distinct values), summariser reports total as unique count; fix: distinct_shape_ok() refuses uniqueness-intent when SQL has neither DISTINCT nor GROUP BY (COUNT(DISTINCT)/SELECT DISTINCT/GROUP BY -> pass); mirror of grouped guard (same _llm_sql wiring, grammar-signal+AST, no hardcode); 6/6 unit + grouped regression 6/6, live over-refusal-safe (count/agg/list unchanged); DISTINCT_SHAPE_GUARD_ENABLED default-OFF | (answer-quality Prong A) |
| 2026-08-31 | Federated query reliability — transient-retry wiring + configurable repair (Task 5) | GAP: federated path LABELS failures retryable/failure_class but NEVER retried (single/independent get execute_reliably, federated was the one branch skipped) -> transient infra blip (DuckDB ATTACH timeout/postgres reset) failed federated permanently; FIX1 execute_federated_reliably() consumes the payload's own retryable label, bounded transient-only retry, wired at BOTH call sites (coordinator._federated_delegate + veda_hybrid direct); FIX2 self-repair depth configurable (FEDERATED_REPAIR_MAX_ATTEMPTS default 1 = historical one-shot, byte-identical); FIX3 retry_attempts/repair_attempts diagnostic on payload; flags FEDERATED_TRANSIENT_RETRY_ENABLED + FEDERATED_MAX_RETRIES default-OFF; 9/9 new tests + execution_and_merge 13/13 + semi_join 14/14 + source_agents 8/8 green; DEFERRED: real-data validation (needs 2nd real relational source), per-metric federated isolation, full trace/metric observability | (federated reliability) |
| 2026-08-31 | Unified cross-source Operation Classifier (routing Phase 2/3) | GOAL: reduce 7B to bounded choice — MULTI query classified into ONE of closed set {SEMI_JOIN, AGGREGATE_AFTER_JOIN, SET_INTERSECTION, LOOKUP_ENRICH, UNSUPPORTED}, SLM writes NO SQL/table/column; deterministic feasibility-gate (relationship/docs/source-count) downgrades infeasible/out-of-set/unparseable -> UNSUPPORTED; CODE dispatches to EXISTING planners (semi_join_planner / structured-plan reused, not rewritten); UNSUPPORTED/unimplemented/planner-fail -> controlled refuse, free-form SLM-SQL chain NEVER reached when ON; new query/operation_classifier.py (typed OperationContext/OperationDecision) + wired in run_federated (post-context, pre-semi_join, covers both callers); OPERATION_CLASSIFIER_ENABLED default-OFF byte-identical; 12/12 unit + real-data 6/6 correct on live qwen2.5:7b-instruct (SEMI_JOIN/AGG/LOOKUP_ENRICH/UNSUPPORTED all right, feasibility-gate overrode SLM on no-relationship); federated regression 11/11+14/14+13/13+8/8 green; DEFERRED: LOOKUP_ENRICH planner (Phase 4), SET_INTERSECTION dedicated template, full E2E dispatch benchmark | (operation classifier) |
| 2026-08-31 | Operation-classifier expressiveness guard (silent-wrong -> refuse) | adversarial test found 2 silent-wrong-but-plannable shapes the closed op-set cannot express: negation ("assets that do NOT have tickets" -> semi-join INVERTS) + count-threshold ("more than 5 tickets" -> semi-join DROPS >5), root cause = taxonomy has no anti-join/HAVING template; FIX: _expressible() deterministic post-classification guard in operation_classifier -> negation/count-threshold grammar-signal + planner-limits => UNSUPPORTED (refuse, never route), same discipline as grouped/distinct guards; part of classifier's deterministic validation (no separate flag - classifier already opt-in); 16/16 unit + real-7B re-verify (2 silent-wrong -> UNSUPPORTED, 8 legit unchanged no over-refuse); relax when anti-join/HAVING templates added | (operation classifier) |
| 2026-08-31 | LOOKUP_ENRICH planner (Phase 4) via structured-plan reuse | insight: "vendors along with number of assets" is structurally identical to AGGREGATE ("SELECT entity, AGG(related) GROUP BY entity") -> LOOKUP_ENRICH REUSES the structured-plan planner (BFS joins, SLM picks fields only), like SEMI_JOIN/SET_INTERSECTION share the semi-join executor; aggregate-enrichment answered correctly, pure-scalar lookup (no agg) -> no valid metric -> safe refuse (never free-form); dispatch OP_LOOKUP_ENRICH -> structured-plan in _dispatch_classified_operation; 16/16 unit (incl LOOKUP_ENRICH->structured stub->refuse) + federated regression 11/11+14/14+13/13 green; taxonomy now: SEMI_JOIN+SET_INTERSECTION (semi-join exec), AGG+LOOKUP_ENRICH (structured-plan exec) all wired; DEFERRED: pure-scalar-lookup projection template, full E2E dispatch benchmark (retrieval slow) | (operation classifier) |
| 2026-09-01 | Federated postgres SCHEMA discovery — the real catalog-bug root cause + FIX (federation now ANSWERS) | ROOT CAUSE (definitive): federated SQL qualified tables as src_2.public.assets_asset but homzhub_prod keeps its 308 tables under the 'homzhub' schema (only 24 under public) -> every federated query failed "Catalog Error: Table assets_asset does not exist"; earlier "free-form mis-qualifies catalog" diagnosis was WRONG (both free-form AND deterministic semi-join qualified correctly, just wrong SCHEMA); FIX: cross_source_composer.resolve_pg_schema() discovers the schema from the source's own DB (non-system schema with most base tables), data-driven no hardcode, cached per source_id; federated_route._catalog_table + semi_join_planner._pg_schema both delegate to it; FEDERATED_SCHEMA_DISCOVERY_ENABLED default-OFF ('public', byte-identical); PAYOFF: E2E real-data with classifier+schema-discovery ON -> SEMI_JOIN "20 assets have maintenance tickets" status=ok, AGGREGATE "avg maint cost per city 2622.50, min 640 Pune max 5950 Thane" status=ok -- first correct cross-source answers on real data (43s/32s); regression semi_join 14/14 + reliability 11/11 + classifier 16/16 + exec_merge 13/13 green | (federated schema fix) |
| 2026-09-01 | Planner bounded-retry + real-data federated validation suite | FIX: classifier-dispatched planners (semi-join / structured-plan) wrapped in bounded retry over their variance-prone SLM pick (FEDERATED_PLANNER_MAX_ATTEMPTS default 2, opt-in classifier only, deterministic assembly unchanged); VALIDATION (10 real cross-source queries, classifier+schema-discovery+retry ON): answer=4 (SEMI_JOIN x3 incl "cities with both assets+tickets" via retry, AGG x1), safe-refuse=6, WRONG=0, ERROR=0; retry lifted answer 3->4 (semi-join case); remaining 3 misses (avg/total AGG, vendor LOOKUP_ENRICH) are DEEPER retrieval-completeness (full-pipeline retrieval doesn't surface the assemblable columns for those phrasings consistently — "total spend per city" assembles 3/3 in isolation but refuses in full pipeline), NOT silent-wrong; regression classifier 16/16 + reliability 11/11 + semi_join 14/14 + exec_merge 13/13 green; KEY: safety 100% (0 wrong), answer-rate ~4/7 answerable and retrieval-limited | (planner completion) |
| 2026-09-01 | Intent↔SQL referent-alignment guard (Option B increment 1: temporal + entity-anchor) | GENERALIZED comparator (not one-off guards) reusing extract_sql_facts + semantic_type/analytics_role metadata, NO LLM, no hardcode; A-TEMPORAL: "per <time-unit>"/"<unit>ly" breakdown intent + SQL groups by non-temporal col -> refuse (#13 "leads per month" grouped by lead_stage); B-ENTITY-ANCHOR: query names a MEASURE column on table T but SQL measures/orders by a col not on T -> refuse (#14 "highest carpet area" -> assets_carpetareaunit); new veda/intent_sql_alignment.py (temporal_alignment_ok/entity_anchor_ok/alignment_ok) + wired in pipeline; INTENT_SQL_ALIGNMENT_ENABLED default-OFF; DISCOVERY: guards were gated on _llm_sql so verified-CACHE replay (#14) AND deterministic join-planner (#13 groups by lead_stage) BYPASSED them -> "deterministic aligned by construction" DISPROVEN -> guard now runs on EVERY produced SQL (no-op when off/aligned); 9/9 unit + real-data #13+#14 -> REFUSE, 4/4 correct controls stay ok (0 over-refusal, incl "total carpet area" naming the measure but aligned), flag-OFF byte-identical | (intent-sql alignment) |
| 2026-09-01 | Intent↔SQL alignment increment 2: DIMENSION referent alignment + ambiguity | extends shared comparator (veda/intent_sql_alignment.py::dimension_alignment) — 4-outcome (ALIGNED/REFUSE/CLARIFY/NOT_APPLICABLE); ACCEPTABLE_CANDIDATES = CATEGORY/DIMENSION cols on the SQL's tables whose name contains the requested dimension phrase ("by status"->{furnishing_status,loe_status}); rules: sole candidate grouped->ALIGN, grouped outside family->REFUSE ("by city"->country), >=2 candidates->CLARIFY (never arbitrary), empty candidate set (synonym)->NOT_APPLICABLE (no false refuse); reuses extract_sql_facts+semantic_type/analytics_role+grammar token, NO LLM/threshold/hardcode; wired in pipeline runs on EVERY SQL path; INTENT_SQL_DIMENSION_ALIGNMENT_ENABLED default-OFF; 17/17 unit (8 new) + real-data: leads-by-status->CLARIFY (furnishing/loe), assets-by-city grouped->ALIGNED, grouped-by-country->REFUSE, controls stay ok; primary #7 converted from arbitrary-both-columns execution to CLARIFY; honest: pipeline firing depends on SLM producing the ambiguous grouped SQL (variance) — when it does, CLARIFY fires | (intent-sql alignment inc2) |
| 2026-09-01 | Alignment coverage benchmark (64q) + temporal-guard FALSE-POSITIVE fix | expanded benchmark (test.py homzhub banks, 64 curated across count/agg/grouped/temporal/ranking/superlative/filter/dimlist): 47 ok/13 refused/4 error, 0 MISSES (100% target wrong-SQL surface covered — no misaligned SQL answered; catches #5 temporal, #16 dim-CLARIFY isolation-proven), BUT false-positive ~4.3% (2/47) — CRITICAL: temporal guard false-fired on a time-adverb inside a MEASURE NAME ("highest monthly rent"/"total monthly rent" -> "monthly" matched _wants_time_bucket -> REFUSED_TEMPORAL, over-refusing legit ranking/aggregate); entity-anchor+dimension guards clean (0 FP); FIX: _adverb_modifies_measure(query,adverb,sm) — a bare <unit>ly adverb is a breakdown ONLY when not part of a MEASURE column name (data-driven: adverb in a MEASURE col's words + another of its words in query -> suppress); "per/each/by <unit>" + trend/over-time signals unchanged; 19/19 unit (2 new) + real-homzhub-sm verified (FP gone, #13/trend still caught); flag-OFF byte-identical | (alignment coverage+FP fix) |
| 2026-09-02 | Canonical-QueryIntent Phase-1 SHADOW spike (measurement-only) — REFUTES easy preserve-on-decline win | built observe-only instrumentation: _FASTPATH_INTENT_QV ContextVar (fast_path, reset at try_fast_path entry, capture in _finalize on VALIDATION_DECLINE/QUALIFIER_GROUNDING_DECLINE/FASTPATH_SUCCESS) + veda/canonical_intent_shadow.py (field-level agreement comparator MATCH/MISMATCH/NA/UNKNOWN vs extract_sql_facts + structured log) + pipeline observe-only call on fp-None; CANONICAL_INTENT_SHADOW_ENABLED default-OFF byte-identical; 9/9 unit; KEY MEASURED FINDING [RUNTIME VERIFIED]: capture fires on _finalize (SUCCESS proven) but REAL fast-path DECLINES happen in branch logic BEFORE QueryIntent is constructed (amenities/waived/unknown-value -> 0 captured) + entitygroup/superlative SUCCESS routes bypass _finalize -> preserve-on-decline yields ~nothing on actual declines; REFUTES Phase-0 assumption "a fully-built QueryIntent exists on decline"; VERDICT: do NOT proceed to Phase-2 threading — canonicalizing QueryIntent needs restructuring intent-construction to be uniform across fast-path branches (bigger than a preserve side-channel), scope separately; observe-only spike saved a wrong architecture bet | (canonical-intent phase1) |
| 2026-09-02 | Amenities "list all <entity>" fast-path DECLINE bug — forensic audit + targeted flag-gated fix | REAL reproducible silent-wrong: "list all amenities" DECLINED the fast-path (list-verb token "list" collides with a values-catalog concept named with "list" -> _single_entity reads a false 2nd entity -> None; and the dimension_list branch demanded a distinctive dimension token beyond the entity name) -> SLM free-formed WRONG junction table assets_assetamenitygroup (11384 rows, answered "5") instead of assets_amenity (32 real amenities); ROOT-CAUSE VERIFIED against DB; FIX (fast_path.py, FASTPATH_LIST_ALL_ENTITY_ENABLED default-OFF): entity-only fallback in the LIST branch — retries _single_entity with list-verb tokens stripped (_LIST_VERB_TOKENS), _residual_is_filler gate prevents dropping real filters, lists the governed display column via _resolve_display_column; RESULT: flag ON "list all amenities" -> SELECT DISTINCT "name" FROM "assets_amenity" (correct table, 32); flag OFF byte-identical (still DECLINE); SAFETY "list properties in Mumbai" does NOT fire (residual guard -> no filter-drop); count queries unchanged; E2E via run_hybrid_query now hits assets_amenity (was assets_assetamenitygroup); 3/3 new integration test (tests/test_list_all_entity_fastpath.py, needs CWD=/app/veda_core); HONEST: "list all asset types" still declines (asset-type entity resolution differs — separate nuance, not a regression) + NL summariser narrates the name-list oddly (separate pre-existing answer-phrasing quirk, SQL/data correct) | (amenities fastpath fix) |
| 2026-09-02 | Step 2 — guard review vs benchmark (isolation, no default flip) | REVIEWED temporal/entity-anchor/dimension + 3A agg-presence guards on real full-pipeline runs (vedademo endpoints) via OFF-vs-ON + per-sub-guard isolation (SLM-variance-safe attribution); METHOD TRAP caught+fixed: first run used unreachable container LAN endpoints (metal No-route-to-host) -> ALL refused incl known-good "total rent" -> invalid, re-ran pinned; RESULT (6q isolation): 0 FALSE POSITIVES, 3 legitimate silent-wrong->refuse catches (#13 per-month fake bucket=temporal, #14 HECTARES reference-table superlative=entity-anchor, #30 LIMIT-100 fake-total=3A), 0 correct-regression; KEY CORRECTION: initially mislabelled #30 as a residual temporal FP -> pulled the cached SQL (SELECT expected_monthly_rent...FROM assets_leaselisting LIMIT 100, AGG=[]) -> it is a TRUE 3A catch (query wants SUM across ALL, SQL returns 100 raw rows + summariser fabricates "950,000 total"); temporal-FP fix (#20 ranking "monthly rent") HOLDS (answers, not refused); SEPARATE FINDING: that wrong LIMIT-100 no-agg SQL was a verified-CACHE hit (sim=1.0) -> unverified-aggregate SQL entered the verified cache, 3A nets it on replay; VERDICT: safe to KEEP enabled for validation, do NOT blind-flip defaults -> honest bar = post-fix full 64q re-run (the 64q that exposed the temporal FP predates the _adverb_modifies_measure fix); doc GUARD_REVIEW_STEP2.md | (step2 guard review) |
| 2026-09-02 | Step 3 — cross-session instability ROOT CAUSE IDENTIFIED (verified-cache poisoning) — measured, not fixed | IDENTIFICATION-only per "do NOT cache-fix yet": the exact cross-session state delta is the VERIFIED-QUERY CACHE (substrate_verifiedquerycache, pg+pgvector); MECHANISM: save gate (pipeline.py ~L2024 `if not from_cache and fp is None and rows...`) uses `rows` (non-empty result) as the ONLY success signal — NO correctness/aggregate/alignment check -> a one-shot SLM-variance wrong-but-executable SQL returns rows -> saved as "verified" -> replayed deterministically wrong across ALL future sessions until evicted; EXPLAINS the standing "SLM deterministic within-session but unstable cross-session" puzzle (cache IS the cross-session state, accumulates variance-errors permanently); EVIDENCE [DB VERIFIED]: 91 cache entries, 14 (~15%) POISONED = aggregate-intent queries ("total X"/"how many X"/"average X") whose cached SQL has no count|sum|avg|max|min|group by (e.g. "total users"->SELECT first_name,last_name; "what is the total carpet area"->SELECT name,id,title WRONG cols; "how many properties in Bangalore"/"how many assets by city"->raw projection) — these are the SAME recurring reliability-benchmark silent-wrongs, recurring because CACHED not because SLM re-fails; STEP-2 LINK: guards run on the replay path so guards-ON already REFUSES these 14 at replay (safety net) but does NOT stop the WRITE; PROPOSED (not implemented): (1) guard the cache WRITE with the same alignment/3A checks, flag VERIFIED_CACHE_GUARD_ON_WRITE default-OFF -> "never-poison" instead of "refuse-on-replay"; (2) one-time scoped eviction of the 14 poisoned rows; channels ruled out: SLM within-session (0% flip), sm registry (re-ingest only), DB drift (value not structural), retrieval (bypassed on cache hit); doc STEP3_CROSS_SESSION_INSTABILITY.md | (step3 instability rootcause) |
| 2026-09-02 | Aggregate SUM-omission fast-path fix (10 "total X across all Y" silent-wrongs) | DB-verified silent-wrong: "total <measure> across all <entity>" produced a raw projection + LIMIT 100 (no SUM) → summariser fabricated a fake total; ROOT CAUSE (traced): the measure NAME's time-adverb ("expected MONTHLY rent") tripped _trend_signal, AND "across" counts as both a group-prep (_group_signal) and a join-hint (_JOIN_HINTS) → the scalar measure query was hijacked into the count/trend branch → declined → SLM free-formed the raw projection (the SUM/AVG branch was never reached, though it builds correct SUM when reached); FIX (fast_path.py, flag FASTPATH_SUM_MEASURE_ENABLED default-OFF): a _scalar_measure_bypass (sum/avg verb + measure-metric match + NOT count + NO real group-word + NO explicit trend/per-unit) skips the count/trend branch so control reaches the SUM/AVG branch, and inside that branch an "across"-only join-hint is ignored for the bypass case; RESULT flag ON: all 6 tested "total" queries → correct full-table SUM() (total expected monthly rent/security deposit/expected price/booking amount/expected amount, + total rent across lease TRANSACTIONS → SUM(rent) FROM assets_leasetransaction i.e. correct table too), + bonus fixes the "average monthly rent" AVG that was also adverb-hijacked; flag OFF → byte-identical (all decline as before), controls unchanged (real monthly-trend → DATE_TRUNC, grouped-by-furnishing → existing path, count → COUNT); 4/4 new test (tests/test_sum_measure_fastpath.py) + 3/3 list-all regression green; NOTE: E2E answer is now a genuine SUM (not LIMIT-100 fake) though its VALUE differs from my direct DB query (2.12B vs 1.53B) = the separate pipeline-reads-a-different-DB-snapshot infra issue (same as properties 7425 vs 6905), NOT this fix | (sum-measure fastpath fix) |
| 2026-09-02 | Count "do we have?" fix + entity-table anchor (Bugs 2 & 3) | BUG 2 (count via LIMIT not COUNT): "How many projects do we have?" answered "100" (LIMIT-100 raw projection, no COUNT); ROOT CAUSE: trailing "have" is a _JOIN_HINTS token → count fast-path treats it as a relationship/join and bows out → SLM free-forms projection; FIX (fast_path.py, FASTPATH_COUNT_HAVE_FILLER_ENABLED default-OFF): a TRAILING have/has/having (only filler/punct after), when it is the ONLY join-hint token, is not counted as a join → count path fires; flag ON: "how many projects/properties do we have?" → COUNT(DISTINCT id) on correct table; controls unaffected (real "with X" join, "have power backup" filter still decline the bare count); 3/3 test. BUG 3 (wrong-table routing): "average security deposit of lease TRANSACTIONS" → assets_leaselisting (wrong, ~1000x off); ROOT CAUSE: "average security deposit" is a metric LABEL on BOTH assets_leaselisting AND assets_leasetransaction, match_metric_labels returns both tied on label-token count, fast-path takes whichever is first; FIX (fast_path.py, ENTITY_TABLE_ANCHOR_ENABLED default-OFF): _rank_metrics_by_named_table re-ranks tied candidates by how many query tokens (len>=4) are substrings of the metric's source_table → "lease transactions" prefers assets_leasetransaction; applied at BOTH SUM/AVG and MAX/MIN branch metric loops; data-driven (table-name token overlap, no hardcoded table/entity names); flag ON: "of lease transactions" → leasetransaction, "of lease listings" control → leaselisting; flag OFF byte-identical; 2/2 test. ALL 4 fast-path test suites green (entity-anchor 2/2 + sum 4/4 + count-have 3/3 + list-all 3/3 = 12). Note: 3 bugs found by parallel agents attempt (rate-limited out) then implemented directly | (count+anchor fixes) |
| 2026-09-02 | ENABLE 4 verified correctness fixes by default (config default 0->1) | User approved enabling the verified-good fixes: flipped config.py defaults "0"->"1" for FASTPATH_SUM_MEASURE_ENABLED, FASTPATH_COUNT_HAVE_FILLER_ENABLED, ENTITY_TABLE_ANCHOR_ENABLED, FASTPATH_LIST_ALL_ENTITY_ENABLED (docker-compose.yml UNTOUCHED per rule); verified: all 4 read True with NO env set + all 4 fixes fire (SUM/COUNT/AVG-on-correct-table/DISTINCT-amenity), 12/12 unit tests green with new defaults, rollback confirmed (env=0 overrides default -> False); guards (INTENT_SQL_*/GROUPED/DISTINCT) + all cross-source/federated flags remain OFF (separate decisions); DEPLOYMENT_PLAN.md updated (Step 0 now = image rebuild required to persist defaults; rollback = env=0); benchmark impact when live: 117-set correct 68%->79%, silent-wrong 17->3; NOTE hot-patched running container for test but image REBUILD from feat/multisource-routing needed for restart-persistence | (enable correctness fixes) |
| 2026-09-02 | ENABLE multi-source tasklist (14) + guards (6) by default — user-approved, with honest residual-risk finding | Per user's multi-source+intent tasklist (Task 1-6) + their "wrong->refuse" priority: flipped config.py defaults "0"->"1" for 14 routing/federated flags (MULTISOURCE_ROUTING, CROSS_SOURCE_EDGE_MULTI, OPERATION_CLASSIFIER, FEDERATED_SCHEMA_DISCOVERY/TRANSIENT_RETRY/SEMI_JOIN_STRUCTURED, FEDERATE_SLM_MULTI, ROUTING_SLM_MULTI_FEWSHOT/AGENT_RETRY, REQUIRED_SOURCE_ESCALATION, SOURCE_DESC_PRIOR/ISOLATED_RETRIEVAL, DOC_DATA_GROUNDING, DATALAKE_VALUE_GROUNDING) + 6 guards (INTENT_SQL_ALIGNMENT/DIMENSION/AGG_PRESENCE, GROUPED/DISTINCT_SHAPE_GUARD, AGG_VERB_COMPLETENESS); docker-compose.yml UNTOUCHED; VERIFIED: config imports, all 20 read True (no env), 12/12 fast-path tests green, single-source E2E smoke ok (COUNT+SUM correct), no crash; total flags now: 24 ON / 15 OFF; HONEST RESIDUAL-RISK FINDING [RUNTIME]: guards do NOT catch cross-source semi-join count errors — smoke "which cities have both assets and vendors" still answered "2: Kochi" (truth 5) with NO refuse, because guards were verified on SINGLE-source shapes, not cross-source combine numbers; so multi-source is LIVE but cross-source exact numbers remain unreliable AND unprotected by the guards -> demo cross-source qualitatively (routing/combine works) not for exact numbers; rollback = per-flag env=0; NOTE image rebuild needed to persist defaults across container restart | (enable multisource+guards) |
| 2026-09-02 | Doc-intent evidence routing fix — source-3 doc questions mis-routed to SQL (ENABLED default) | BUG (doc benchmark, 57 grounded queries): 35/49 document questions routed to the SQL/tabular head and errored/refused (pure-HR "grievance redressal"/"paternity leave", maintenance-fee, site-notes questions); ROOT CAUSE (isolated): the sql-vs-rag intent is decided in classify() by a FIXED word-list regex (_DOC_REF_RE) then the SLM router — NEITHER consults the source-coordinator's retrieval evidence; verified plan_route DOES tier source 3 STRONG for a doc query (chunk cosine 0.679, item-prior 0.476 dominant) but that evidence never reached the sql/rag decision, and the SLM router guessed "sql"; FIX (veda_hybrid.py classify() + _doc_intent_by_evidence, flag DOC_INTENT_EVIDENCE_ENABLED default-ON): after the regex override, reuse the coordinator's OWN evidence (_default_evidence_provider + group_evidence_by_source + _apply_item_prior + _dominance_retier) and route to the RAG lane when a chunk-backed source tiers STRONG and dominant; data-driven (BGE-M3 cosine, no keywords); RESULT flag ON: 4/4 doc queries (grievance/society-fee/late-fee/paternity) -> rag, SQL queries (properties count / total monthly rent) STAY sql (0 over-routing); E2E grievance -> ok, cited Samta-Employee Handbook, correct open-door-policy answer; flag OFF byte-identical (docs still mislabel sql); 3/3 test (tests/test_doc_intent_evidence.py); NOTE some doc queries now reach the RAG lane but still error there (e.g. society-charges-fee) = separate RAG retrieval/synthesis issue, not routing; flags now 25 ON / OFF remainder | (doc-intent routing fix) |
| 2026-09-03 | Source-routing dominance-tier fix — doc questions refused by coordinator (ENABLED default) | DEEPER root cause of doc-routing (after DOC_INTENT_EVIDENCE): with MULTISOURCE_ROUTING ON, the coordinator (_run_coordinator->plan_route) is the routing authority BEFORE classify(), so classify's doc-intent fix was bypassed; traced step-by-step: for "paternity leave" the doc source 3 has a dominant CHUNK cosine 0.758 but _dominance_retier PREFERS the item-description prior (src2=0.385, src3=0.364, src5=0.358 all within gap) -> 3 sources tie STRONG -> decide()=AMBIGUOUS_SOURCE_SELECTION -> SLM boundary resolver -> NO_MATCH -> refusal (chunk 0.758 signal ignored); FIX (source_coordinator._dominance_retier, flag ROUTING_TIER_MAX_SIGNAL_ENABLED default-ON): tiering signal = max(item, column, chunk) so a strong raw cosine is never suppressed by a weaker item prior; VERIFIED: paternity + grievance -> plan_route SINGLE src=[3] -> E2E ok/doc (were refused/sql); "how many properties" control -> SINGLE src=[2] -> ok/sql (no regression); env=0 restores item-prior-preferred; re-running 57-doc benchmark to measure routing %; TWO doc-routing fixes now default-ON: DOC_INTENT_EVIDENCE_ENABLED (classify fallback) + ROUTING_TIER_MAX_SIGNAL_ENABLED (coordinator, the effective one) | (dominance-tier routing fix) |
| 2026-09-03 | Doc-boundary-preference fix — ambiguous doc+tabular queries (SLM mis-picked SQL) — ENABLED default | THIRD doc-routing fix (after max-signal tiering): remaining ~20 mis-routes were fee/rent/asset/city questions where doc words overlap homzhub schema -> BOTH sources tier STRONG (close) -> decide()=AMBIGUOUS -> SLM boundary resolver -> SLM mis-picked the TABULAR source even though the doc source had strictly-higher signal (verified deterministic 4/4: "amenities inspected" item src3=0.612>src2=0.568, chunk src3=0.622, yet SLM->src2); FIX (source_coordinator plan_route boundary, flag ROUTING_DOC_BOUNDARY_PREF_ENABLED default-ON): _doc_boundary_winner routes SINGLE to a DOCUMENT source when it is the STRICT top-signal candidate at the boundary (structural source_type + top_score, no keywords), skipping the SLM; RESULT: ambiguous doc-questions 0/7->6/7 route to docs, SQL controls (properties count / total monthly rent) stay ['2'] SQL in BOTH flag states (0 regression); E2E "Taxes fee" -> ok, cited maintenance_policy.docx, "800 charged annually" (exact ground-truth); THREE doc-routing fixes now default-ON: ROUTING_TIER_MAX_SIGNAL + ROUTING_DOC_BOUNDARY_PREF (coordinator) + DOC_INTENT_EVIDENCE (classify fallback); re-running 57-doc benchmark to measure final routing % | (doc-boundary-pref fix) |
| 2026-09-06 | Phase E — first END-TO-END pipeline truth audit (182 q, real front door + real DB) + 2 fixes | METHOD: D4-D11 each audited ONE signal in isolation and shipped nothing; this ran all 182 benchmark queries through veda_hybrid.run_hybrid_query against the real DB capturing SQL+rows+answer, classified final outcome. BLOCKER FOUND+FIXED (active, not flag-gated): storage_adapters/reader.py::source_connection() never SELECTed schema_filter so runtime._pg()/execute_sql()'s SET search_path could never fire -> EVERY single-source query failed "relation does not exist"; +set sources_source.schema_filter='homzhub'. MEASURED: 113 correct(62%)/39 safe-refusal/20 timeout/10 wrong(5%); deeper filter-column check found 7 more defects inside "correct" => honest ~106 correct/~17 wrong(9%), and 106 is an UPPER bound (values/JOINs never verified). STRUCTURAL FINDING invalidating D6-D11's premise: metric/aggregate queries are decided by the fast-path metric registry (no retrieval, no LLM) — typed_anchor_evidence AND embedding retrieval both BYPASSED, which is why D6b Option E and a NAME_COVERAGE_WEIGHTING experiment each moved exactly +1 query. FIX THAT WORKED: fast_path._rank_metrics_by_named_table broke ties on SUBSTRING match crossing word boundaries ('transactions' matches ...paymenttransactionSETTLEMENT because the plural s comes from 'settlement', not the correct singular table) -> wrong sibling won 2-1 on every payment query; replaced with schema's own segmented table_tokens (singular/plural aware) + coverage tie-break, verified bidirectional; A/B 182 q: CORRECT 113->123 (+10), WRONG_TABLE 8->2 (-6), REGRESSED 0, aggregate category 81%->100%, AGGR-23 answer 25.000 -> 29,430,686.36; full suite 36 failing before/after (zero new), 17 new tests pass; flag METRIC_TABLE_TOKEN_RANKING_ENABLED default-OFF. NO-GO: NAME_COVERAGE_WEIGHTING_ENABLED (+1 query, wrong module - consider deleting). OPEN: boolean-column confusion (4 q), EXISTS count returns 6 for a 466-row table (2 q), over-federation on amenities (5 q, = D2's unfixed finding), NL number formatting, 18 timeouts, analytical_multitable 26% correct/55% refused. HARNESS CAVEATS (mine): >3 parallel workers collapse the remote DO host's DNS (42 false failures); ORDER BY ranking queries were initially mis-scored as WRONG_AGGREGATE | docs/architecture/VEDA_PHASE_E_PIPELINE_TRUTH_AUDIT.md + VEDA_PROGRESS_LOG.md §5 |
| 2026-09-06 | Phase F — multi-source re-run of the SAME 182 queries (the gap Phase E left open) | SCOPE FIX: Phase E had PINNED source_id=2 so routing/coordinator were never tested (I overstated it as "full pipeline"; corrected in all docs). Phase F re-ran the same 182 q with source_ids=(2,3,4,5), metric fix ON in both, only scoping differs. RESULT — multi-source is worse on every axis: CORRECT 121->80 (-41), TOTAL WRONG 6->38 (+32), SAFE_REFUSAL 37->14, EXEC_FAIL 18->50, median latency 11.1s->58.8s (~5x); 52 queries correct single-source BREAK under multi-source, 11 improve. SEVERE: silent-wrong (status=ok + fluent answer rendered + wrong source) 3% -> 21% — e.g. "distribution of lease listings by status" AND "distribution of verification documents by status" both got the SAME 7 rows from src_4."maintenance" (a maintenance CSV) each relabelled as the asked-about entity; "which lease listings have the highest expected monthly rent" answered '"Squash" at 500' — an AMENITY name + its monthly_fee from src_5 — while single-source answered it correctly. ROOT CAUSE = the ROUTING DECISION, not the engine: deterministic route 73 q / 78% correct / 0 fail; federated route 100 q (55% of benchmark) / 23% correct / 33 wrong / 44 fail; hybrid 3 q / 0% correct. Trivially single-table queries ("How many lease listings exist?") are sent federated then refused as "cross-source shape not supported". LATENCY IS NOT THE SLM: 1 SLM timeout in 182 q; deterministic median 79.9s is SLOWER than federated 44.1s (federated aborts early); not one-time warm-up (repeat query, no index rebuild, still ~103s) — it is per-query multi-source setup. IMPLICATION: routing is a far bigger correctness lever than anchor selection (8 phases D4-D11) — it turns a 3%-wrong system into a 21%-wrong one. Nothing enabled; no code changed in this phase | docs/architecture/VEDA_PHASE_E_PIPELINE_TRUTH_AUDIT.md (Phase F section) + VEDA_PROGRESS_LOG.md §5, F20/F21 |
| 2026-09-06 | Phase G — repo's own test.py 50 business questions through the multi-source pipeline (3 parallel workers + 3 independent reviewers) | HARDER SET than the 182-q benchmark: real analytical business questions (conditionals, filtered aggregates, derived metrics). No ground truth exists, so every answered query's SQL+rows+prose was captured verbatim and reviewed by 3 independent reviewers. RESULT: 3 CORRECT (6%) / 23 CONFIDENT WRONG (46%) / 10 failed / 9 refused / 5 unsure(doc-RAG). Only correct ones were simple unfiltered lookups (assets under maintenance, amenity list, MAX(rating) GROUP BY city). SINGLE MECHANISM behind nearly all 23 wrong — all three reviewers converged independently: the generated SQL DROPS THE QUESTION'S FILTER and degenerates into a bare GROUP BY over one table, then the summariser narrates whichever groups came back as the answer ("average amount of REPAIR requests" -> SUM over the WHOLE maintenance table; "vendors rated 4.0-4.5" -> no WHERE on rating, counts all groups 3.7-4.8, reports 6). SECONDARY: summariser fabricates statistics absent from the SQL ("average asset of 13.29" = mean of asset_id presented as a metric; "1.4 services on average include intercom maintenance"; "88% of total"). TERTIARY: conditional questions never executed ("if a 10% surcharge is applied" asserts a revised amount without computing it). ALSO: fabricated cross-source join (Q9) linking value_bundle_pricing_id->asset_country_id=maintenance.asset_id, reporting MAX(an ID) as "financial impact 136.0". CONTEXT: 182-q benchmark = 62% single-source / 42% multi-source because it is dominated by simple direct aggregates; on the questions a business user actually asks the system scores 6% and fails SILENTLY (23 wrong vs 9 refusals) => the refuse-over-guess contract does NOT hold once a question carries a filter or condition. Latency median 47s. Nothing changed; audit only | docs/architecture/VEDA_PHASE_E_PIPELINE_TRUTH_AUDIT.md (Phase G section) + VEDA_PROGRESS_LOG.md §5, F22/F23/F24 |
| 2026-09-06 | Data Lake implementation audit (read-only, no code) | Full inventory of the file-backed "Data Lake": local filesystem + in-process DuckDB, NO object store (no boto3/s3fs/fsspec anywhere). TWO file connectors exist and the `datalake:*`-registered one (connectors/datalake.py, random uuid4, no sampling/materialization) is DEAD for every live source — `_dispatch_datalake` uses connectors/tabular_files.py (deterministic uuid5) instead; only its `_normalise_duckdb_type` is live. KEY GAP: lake datasets get types+embeddings+graph but NO semantic layer at all — `semantic_layer_v2` is never called on the lake path (`_run_schema_pipeline` step list omits it), and `l3_enrich.py:35` couldn't do it anyway (`get_real_schema()` resolves the PRIMARY relational source and ignores `ctx`). Also MISSING: change detection of any kind (no mtime/checksum/schema-diff/tombstones; `beat` runs with no `beat_schedule` defined), lake-side profiling (data_profiler is psycopg2-only), partitions, nested types, JSON/Iceberg readers, lineage, ownership. Resolved 1 unknown live: `source_item_embeddings` DOES exist in the engine db. Verdict: execution+routing plumbing is real, the metadata/semantics feeding it are not | docs/architecture/VEDA_DATALAKE_IMPLEMENTATION_AUDIT.md |
| 2026-09-06 | Cross-dataset relationship detection for the Data Lake — verdict DEFER (no code) | Premise was FALSE: cross-source relationship detection already exists and runs — `ingestion/cross_source_graph.py::discover_edges` (MinHash + exact value-hash containment + name affinity + HIGH/MEDIUM tiers), wired for the lake at `source_dispatcher.py:386`, with **20 live `cross_source_fk` edges** in graph_edges. Only INTRA-lake pairs are missing, blocked by ONE line (`if a.source_id == b.source_id: continue`). MEASURED by running the project's own `_exact_containment`/`_name_affinity`/`_tier` over the live sketches with that guard relaxed: exactly **1** edge, the correct one (`vendors.ticket_id -> maintenance.ticket_id`, HIGH, containment 1.000), **0 false positives**. Verdict 🔴 DEFER anyway: only 1 relationship exists in the whole registered lake (3 datasets / 21 rows), **zero** benchmark queries need it (54+50+multisource checked), and an edge would be INERT — fk_adjacency has 0 lake rows, `_datalake_isolated_sm` carries no join structure, `relationship_graph.py` is psycopg2-only, and `routing_policy._edge_connected_subset` structurally ignores same-source edges. Also found: `run_data_graph` is a confirmed SILENT NO-OP for the lake (psycopg2-only) while its call-site comment claims "value-overlap FK discovery within the files". Trigger to revisit: a lake source with >3 interrelated datasets, or a real query needing an intra-lake join | docs/architecture/VEDA_DATALAKE_RELATIONSHIP_DETECTION_INVESTIGATION.md |
| 2026-09-06 | Cross-source edge reliability — name-affinity HIGH-tier floor (FLAG ENABLED? NO, default OFF) | BUG: `_tier()`'s `or affine` clause promotes a high-containment edge to HIGH below `CROSS_SOURCE_FK_HIGH_MIN_DISTINCT=25` with NO lower bound, so 1-2 value enum columns reached HIGH: `assets_closurereason.asset_country_id` (values {1,4}) and `services_valuebundlepricing.asset_country_id` ({1}) both HIGH against the lake's `maintenance.asset_id` ({1,4,6,19,20,21,22}) by pure coincidence. HIGH is execution-grade — `federated_route._join_hints` drops every MEDIUM when any HIGH exists — so a bogus HIGH becomes a JOIN KEY in generated federated SQL (measured: `maintenance.asset_id = assets_closurereason.asset_country_id`, a property id joined to a country id). Audited all 20 live edges against real values: 8 VALID / 12 FALSE POSITIVE, 3 of the 8 HIGH bogus. FIX (config.py + cross_source_graph._tier, flag CROSS_SOURCE_FK_AFFINITY_FLOOR_ENABLED default OFF): an absolute floor the affinity promotion may not cross, DERIVED not tuned — `ceil(1/(1-HIGH_CONTAINMENT))` = 5, because below that "containment >= 0.8" degenerates into "== 1.0" and cannot discriminate. Float-precision guard is load-bearing: naive `ceil(1/(1-0.8))` = 6, which would have demoted the genuine 5-value `vendors.city` edge (caught by the A/B, not by reading code). MEASURED on live sketches: HIGH 8->6, total edges 20->20 (downgrade never drop), all 8 valid edges preserved incl. the n=5 boundary, federated join keys offered 4->2. 7 new tests, 5 existing suites identical in both flag states. HONEST LIMITS: (1) routing UNCHANGED — `_default_edge_provider` has no tier filter and `_edge_multi_pair` fires 3/182 before AND after (0 changed); (2) one HIGH FP remains (`maintenance.asset_id -> asset_assignment.id`, a table's own identity PK accepted by `_affine_target_ok`) — separate root cause, not fixed | docs/architecture/VEDA_CROSS_SOURCE_EDGE_RELIABILITY_FIX.md + tests/test_cross_source_tier_floor.py |
| 2026-09-06 | MULTI-routing root cause audit (read-only, no code) | Tested and FALSIFIED the assumption that false graph edges cause over-MULTI (0/182 change, both tier-consuming call sites). REAL root cause: **`MULTISOURCE_ROUTING_SHADOW` defaults to "1"** (config.py:981, nothing overrides it), so `_run_coordinator` computes the routing decision, traces it, and `return None`s — then `veda_hybrid.py:1054` unconditionally calls `_maybe_federated`, and federation is decided solely by `should_federate(cols)` = "did the RETRIEVED columns span >=2 sources". A PRESENCE test, not a relevance test. MEASURED (probe over the real `select_retrieval`+`should_federate`, all 182, 0 errors): **182/182 federated, all wrong** (ground truth is homzhub-only for all 182); source 5 `amenities_catalog` (4 columns) present in **100%** of selected column sets, source 4 in 26%. Mechanism measured: selection is top-K by COUNT with no score floor (mean 59 cols, median 39, of 1915), lake columns score 0.28-0.36 vs a homzhub top of ~0.63, and `pk_inject_v2`/`graph_supplement_v2` then expand the table to all 4 columns (hence never partially present). SECOND root cause for shadow=0, also measured: deterministic `decide()` is RIGHT (SINGLE 177/182, MULTI 0) but **182/182 are escalated to the SLM anyway**, 68% by Required-Source Escalation firing on clearly-dominant decisions (mean gap 0.169 vs threshold 0.10) because `_required_secondary`'s two signals are vacuous here (permanent edge connectivity + a cosine that is always >0). Verdict: **A — Candidate Retrieval**, E secondary. System never distinguishes "relevant" from "required" — necessity is computed nowhere. BLOCKER noted: routing SLM unreachable (`host.docker.internal:11434` -> HTTP 500); local `ollama:11434` works | docs/architecture/VEDA_MULTI_ROUTING_ROOT_CAUSE_AUDIT.md |
| 2026-09-06 | Federated per-source relevance qualification gate — ENABLED default ON | Acts on the MULTI-routing audit's verdict. `should_federate` is a PRESENCE test with no score floor anywhere before it. FIX (cross_source_composer.qualified_source_ids/qualify_columns + one call in federated_route.run_federated before `should_federate`, flag FEDERATION_SOURCE_QUALIFICATION_ENABLED default **ON**): drop the selected columns of sources that are not COMPETING for this query. **NO new threshold** — reuses `ROUTING_COMPETE_WINDOW` (0.08), whose documented meaning is already "how close a runner-up must be to count as genuine competition". NON-OBVIOUS: the gate CANNOT use `select_retrieval`'s column score — measured, on "total carpet area" source 2's best reads **0.0739** and source 5's **0.3646** (the reranked value collapses; `_default_evidence_provider`'s own docstring records the same finding), so gating on it would drop the CORRECT source; the gate therefore qualifies on the routing layer's clean per-source cosine and reads `top_score` via `build_candidates` so that formula stays in one place. A/B MEASURED, 182 benchmark + 6 genuine cross-source, 0 errors: federated **182/182 -> 12/182** (170 fixed, 93%); 170 queries now select source 2 ALONE; aggregate/filter/grouped/ranking/temporal 100% fixed, simple 42/50, analytical_multitable 27/31. Residual 12 are all borderline INSIDE the 0.08 window (e.g. SMPL-01 0.5249 vs 0.4746) — gate fails by under-fixing, never by over-dropping. Cross-source regression: **5/6 preserved** and survivors keep the RIGHT partners (amenities_catalog dropped from maintenance/vendor joins); 1 ACCEPTED regression — "Which assets have maintenance tickets?" routes SINGLE to the datalake (homzhub side 0.117 below top). 8 new tests; 9 existing suites identical in both flag states (test_source_coordinator 29/32 pre-existing); latency 2.60s vs 3.12s baseline (no measurable cost). Default flipped 0->1 and live-verified after an `inference` restart; docker-compose.yml UNTOUCHED; rollback = FEDERATION_SOURCE_QUALIFICATION_ENABLED=0 | tests/test_federation_source_qualification.py + docs/architecture/VEDA_MULTI_ROUTING_ROOT_CAUSE_AUDIT.md |
| 2026-09-06 | RSE item-prior competitiveness guard — ENABLED default ON | Acts on the MULTI-routing audit's SECOND root cause. `_required_secondary`'s Signal 2 was `top_item_score > 0.0`, which is vacuous (a BGE-M3 cosine is positive for essentially any text pair), and Signal 1 (edge-connected to the top) is permanently true for any datalake source sharing a VALID cross_source_fk edge with the primary. MEASURED over 182 benchmark + 6 hand-written cross-source queries (probe replaying real candidate sets, 0 errors): RSE fired on **123/182 (68%)** — every one a decision the policy itself had already called "clearly dominant" (top-vs-runner gap mean 0.169 vs ROUTING_DOMINANT_GAP 0.10) — and on **0/6** genuine cross-source queries, which all reach the boundary through the two_strong / close_runner / ambiguous branches instead (XSRC-2 deterministic MULTI, XSRC-3/4 AMBIGUOUS, XSRC-1/5/6 gap < 0.10). FIX (config.py + source_coordinator._rse_item_window/_required_secondary, flag RSE_ITEM_COMPETITIVENESS_ENABLED default **ON**): Signal 2 now requires the secondary's item prior to be COMPETITIVE with the top's, not merely non-zero. **NO new threshold** — reuses ROUTING_COMPETE_WINDOW (0.08), the same constant the decision boundary and the federation gate use. A/B on the SAME 188 items: RSE escalations **123 -> 13** (89% blocked), cross-source **0 -> 0** (untouched). The 13 kept are exactly the competitive cases — item gaps from **-0.0147** (secondary scoring HIGHER than the top, FILT-04) up to +0.0764. 9 new tests; 8 existing suites identical in both flag states (test_source_coordinator 29/32, test_authoritative_routing 2/13, test_confident_single_skip 3/4 all pre-existing). Live-verified with no env set: 2 of 3 sample queries now `at_boundary=False` (no SLM call at all). HONEST SCOPE: with MULTISOURCE_ROUTING_SHADOW=1 (user's decision, kept) the routing DECISION is discarded, so this changes **no route** — `plan_route` runs BEFORE the shadow check, so the benefit is removing a wasted SLM boundary call + its latency/tokens on ~2 of every 3 queries. Also: the 6 cross-source queries are self-authored, not a validated set, so "RSE contributes 0 genuine escalations" is measured on this set only. Rollback = RSE_ITEM_COMPETITIVENESS_ENABLED=0 | tests/test_rse_item_competitiveness.py + docs/architecture/VEDA_MULTI_ROUTING_ROOT_CAUSE_AUDIT.md |
| 2026-09-08 | Traceability / explainability / RBAC-visibility inventory audit (read-only, no code) | INVENTORY ONLY — nothing implemented, nothing enabled, no architecture proposed. Traced the full lifecycle (api middleware -> scope/RBAC -> chatbot graph -> inference route -> run_hybrid_query -> coordinator/federated/Tier-1 -> validation -> execute_sql -> business_explain -> SSE -> ChatMessage/QueryLog) and inventoried every capability with file:line, per stage: what is generated, what metadata exists, what is emitted, what is persisted, what is transient, what is user-safe. VERIFIED LIVE by reading the last record of veda_core/logs/explain_trace.jsonl (trace_id dc96dcb1951f). KEY FINDINGS — (a) validation is the strongest stage (8/10): 4 named checks already carry plain-English labels and already ship to the frontend inside build_explain; (b) federation is the weakest (3/10): build_provenance, MergeResult (policy/conflict/winner) and the partial{failures,ok_count,complete} block are all fully computed then DISCARDED in memory, zero federation data reaches any log or the UI; (c) source execution 4/10 — there is NO per-source record at all (no name, no timestamp, no duration, no rowcount, no retry count); the engine only ever sees numeric source_id, Source.name is never joined back; (d) ExecutionPlan is built at execution_planner.py:48 and never traced; depends_on is always empty and MODE_PARALLEL is executed by a sequential for-loop (source_coordinator.py:848); (e) trace_id flows cleanly browser->api->inference->engine but `grep -rn trace_id apps/` returns ZERO — the chat tier drops it, so support cannot correlate a complaint to a trace; (f) chat path writes no QueryLog at all (only apps/query/views.py:201 does) and QueryLog has no user FK (tenant = username proxy). TWO CONFIRMED DEFECTS: explain.py:566-576 + pipeline.py:274-283 read explain_payload["datasets"]/["check_items"] but build_explain returns them nested as data_used.datasets / validation.checks -> the trace's explainability section records datasets=None, validation_passed=None on EVERY query (reproduced in the live record); and business_explain.py:377 hardcodes sql.enabled=True so the raw SQL is exposed to every end user with no flag. RBAC HALF: the model + resolver are strong (9/10) — 4 fail-closed rules with strict source-gated hierarchy, DB-enforced CI-unique constraints, granted_by provenance, inactive-chain grants nothing — and the admin read API ALREADY EXISTS and is wired (users/permissions/effective with an allowed/explicitly_denied/granted_on decision block; catalog/tree with per-node ALLOW/DENY overlay; roles/permissions/list filterable by resource_path; roles/list with users_count + connected_sources). So "kisko permission hai" is answerable TODAY for an admin with three existing endpoints, no backend change. But end-user visibility scores 1/10: every read endpoint sits behind IsAdminUser + user.manage, so a user cannot see their OWN access and cannot tell a permission refusal from a data refusal — no self-service endpoint exists in apps/authentication. Auditability 3/10: gate.py:104,119 denials are logger.warning lines only, there is no authorization audit model. HAZARD FLAGGED for any future permission UI: the hierarchy rule is implemented THREE times (resolver.allows / query.scope.permitted_source_ids / catalog._resolve_effect) and resolver.py:24-27 mandates changing all three together — drift here already caused one live bug (tree painted ALLOW while the query correctly denied). Env note: .env:104 VEDA_RBAC_MODE=enforce (code default is off) | VEDA_TRACEABILITY_AND_RBAC_AUDIT.md |
| 2026-09-08 | Query traceability Phase 1 — user-safe execution timeline, warning tier, per-source records, explain v2 (ALL FLAGS DEFAULT OFF) | Implements Phase 1 (Foundation) of the 28-part traceability/explainability spec; the spec message was TRUNCATED mid-Part-28 so the remaining phases are backlogged, not silently dropped (list in VEDA_TRACEABILITY_PHASE1.md §Not-in-this-phase). ARCHITECTURE: one recorded fact, three consumers — emit() writes to the internal ExplainTrace, fires the existing on_event SSE callback, and is projected into the final explainability payload; no layer re-derives "what happened" (the spec's Part-27 single-source-of-truth rule). 5 NEW veda_core modules: veda/lifecycle.py (10 business phases received→completed, 4 statuses, ambient ContextVar like explain.py, closed phase vocabulary so an unmapped internal stage emits NOTHING rather than leaking its name), veda/warnings.py (the missing tier between pass/fail and refuse — 7 stable codes, idempotent per code, streams live AND lands in the payload), veda/exec_records.py (SourceExecutionRecord: status/timestamps/duration/rows/retries/fallback; as_safe_dict() is the ONLY user-facing projection and drops engine/error/fallback_path), veda/source_names.py (display names via the profiles ContextVar; unknown source → generic label, NEVER the numeric id), veda/safe_projection.py (the ONE reader of the trace allowed to build user-facing output — retrieval/rrf/reranking/slm/sql_planning/rbac_filter sections are deliberately unreadable from it). WIRED: source_coordinator persists the ExecutionPlan + per-source records + MergeResult/conflict + partial-failure warnings (all four were computed-then-discarded per the 2026-09-08 audit); pipeline emits understanding/validation phases and passes the trace to build_explain; veda_hybrid owns the timeline+recorder for the request and emits received/source_selection/access_check/terminal. TWO BOUNDARY BUGS FIXED: X-Veda-Source-Profiles has been SENT by the api tier since the multi-source work but was NEVER READ by inference/main.py, so current_source_profiles() returned {} in every deployed request (canonical tie-break could not fire, and nothing could resolve a display name) — now bound, failing OPEN to {} since it is display/tie-break metadata not an authz input; and source_profiles_for() now carries `name`, the single controlled boundary for display names (no DB join spread through the pipeline). DEFECT 1 FIXED (from the audit): explain.py:566 + pipeline.py:274 read flat "datasets"/"check_items" that build_explain has NEVER emitted (it returns data_used.datasets / validation.checks), so EVERY trace recorded datasets=None validation_passed=None — one shared summarize_explain_payload() now reads the real nested shape, accepts the refusal payload too, verified against real payloads. DEFECT 2 NOT falsely fixed: MODE_PARALLEL still executes sequentially, so the trace stamps executed_mode="sequential" and the projection reports THAT — actual parallel execution (needs cancellation/timeout/RBAC-before-execute/deterministic merge) is explicitly backlogged rather than faked. ALSO: explain payload v2 (additive — every v1 key byte-identical, only `version` changes), SQL exposure moved behind EXPLAIN_EXPOSE_SQL (was a hardcoded True reaching every user; default True preserves existing behaviour), real DB-side timing + truncation warning in execution.py, version stamping (git_sha/pipeline/schema, admin-only — never in the safe projection), trace_id + structured timeline into ChatMessage.metadata (grep -rn trace_id apps/ previously returned ZERO), and GET /api/v1/access/me (self-service, IsAuthenticated, resolves request.user ONLY — no user_id param — returns role names + accessible source display names, never resource_paths/DENY grants/permission codes; reuses permitted_source_ids so it can never claim access the query path would refuse). TESTS: 39 new in tests/test_query_traceability.py, all pass, incl. leak assertions (raw psycopg2 error + host + password, engine names, "PARALLEL", candidate scores all asserted ABSENT from projections) and an explicit default-OFF contract test. REGRESSION: 17 suites run per-file — 16 clean; 2 failures proven PRE-EXISTING by diff (test_confident_single_skip::test_flag_on_one_strong_skips_slm hits SC._decision_boundary which my diff does not touch, and matches PM_LOG's own documented 3/4; test_data_scope::test_staff_bypasses_regardless_of_grants contradicts data_scope.py's own docstring lines 38/98 which state is_staff is NOT a bypass — that file is untouched). Nothing enabled, nothing committed | VEDA_TRACEABILITY_PHASE1.md + tests/test_query_traceability.py + VEDA_TRACEABILITY_AND_RBAC_AUDIT.md |
| 2026-09-08 | Traceability Phase 1 — LIVE-VERIFIED in the real inference container; 4 wiring gaps + 1 misleading label found and fixed | Ran the REAL pipeline (real Postgres, real retrieval, real front door) inside `inference` with the flags passed as ENV ONLY (`docker compose exec -e …`) — no default flipped, no .env touched, no container recreated. Query "How many sale negotiations are there?" -> answered 5, route=deterministic. FLAGS ON: explain came back version=2.0 with a real per-source record (duration_ms 479, rows_returned 1), the resolved display name "Homzhub Property DB" (PROVES the newly-bound X-Veda-Source-Profiles header works end-to-end — it had been sent-but-never-read in every deployed request), support.trace_id, and a 14-event streamed timeline. FLAGS OFF: byte-identical — only the 4 pre-existing legacy events, version=1.0, exactly the original 9 keys, same answer. FOUR WIRING GAPS THE LIVE RUN EXPOSED (unit tests structurally could not catch these — they are coverage facts, not logic errors): (1) the DETERMINISTIC path produced NO `execution` block at all because it bypasses source_coordinator.execute_decision, i.e. the most common path of all had zero per-source records — fixed by opening a record around execute_sql in pipeline.py; (2) `data_retrieval` never fired on that same path (same cause) — now fires and names the source; (3) `access_check` only fired on REFUSAL, so a successful query never showed "Verified your access" — now emitted on success from the already-RBAC-narrowed scope; (4) phase ORDER was wrong (source_selection completed before understanding even appeared, because routing runs at the front door and Tier-1 understanding runs later) — fixed by opening understanding as `started` at the front door, which is honest since reading the question genuinely is first. ONE COPY CORRECTION: validation completes BEFORE data_retrieval, which is correct pipeline behaviour (all 4 checks are pre-flight on the SQL), so the phase title went from "Checking the result" to "Checking the query" — the old wording described a result that did not exist yet. The spec's illustrative timeline had the opposite order; the timeline now reports the REAL order rather than the assumed one. ALSO: all 7 flags made env-overridable to match the repo's existing 73-flag `_os.environ.get(FLAG,"0")=="1"` convention (they were plain constants, unflippable without a code edit); fixed a stale self-reference in lifecycle.py's docstring to a `lifecycle_map.py` that was never created. Tests still 39/39; test_business_explain 23/23, test_source_coordinator 32/32, test_federated_reliability 11/11 after the changes. Scratch verification script + its output removed from the tree. Nothing enabled, nothing committed | VEDA_TRACEABILITY_PHASE1.md §6a |
| 2026-09-08 | Traceability Parts 3 / 23 / 10 — RBAC-narrowing warning, retry+fallback signals, cross-source join match counts (all flags default OFF) | PART 3 (RBAC narrowing was entirely SILENT): RESTRICTED_DATA now raised at two places, each reusing an EXISTING decision rather than re-deriving one — in pipeline.py where filter_retrieval_results actually CHANGED the candidate count (running the filter is not newsworthy; removing a relevant candidate is), and in pipeline._feedback when explain_failure returns ACCESS_DENIED_WHY (the refusal genuinely WAS an access denial). before/after counts stay internal and are NOT projected because the count of withheld items is itself a disclosure; live-verified against a real restricted data-scope with no rbac_filter / allowed_resources / restricted table name anywhere in the payload. PART 23: AgentResult.retry_count is now a real typed field stamped by reliability.execute_reliably (it previously only wrote a prose reason string "retried x2"); coordinator copies it to the per-source record and raises FALLBACK_USED once; execute_federated_reliably's retry_attempts gets the same treatment; the Tier-1 -> Tier-2 hand-off also raises FALLBACK_USED (a genuine alternate retrieval path); safe projection exposes retried:true / fallback_used:true but NEVER the attempt count, and the copy is asserted to name no internal component (no tier/slm/agent/rag). PART 10: implemented ONLY on the aggregate-pushdown path (execute_plan), where per-source aggregates are ALREADY materialized as DuckDB temp tables — _join_stats is two LOCAL counts (INNER vs FULL join cardinality), zero source access, zero extra federated round trip; deliberately NOT offered for plain execute() where the arbitrary caller-supplied join would cost 1-2 extra federated queries on a path whose measured median is already tens of seconds (a missing join block is honest, a guessed one is not). Verified on REAL DuckDB 1.5.4 in-container: 2 sources -> matched 2/unmatched 2, 3 sources -> matched 1/unmatched 3, single table -> None, flag off -> None, full chain renders "98.0% of relevant records were successfully matched" with raw counts stripped. TWO MORE HONESTY BUGS THE LIVE RUN CAUGHT, BOTH MINE: (1) "Verified your access" was emitted at scope-resolution time, so a query later refused on permission grounds showed the user a green access check followed by "you do not have permission" — a visible contradiction in the live stream; the front door now emits access_check as STARTED ("Checking your access") and it completes in pipeline.py only once validation has actually passed (the first moment we can honestly say narrowing did not block THIS query), with a real denial downgrading it to failed; (2) a REFUSED turn carried no warnings and no support reference because the v2 extension only reached the success path — build_refusal_explain now merges warnings/limitations/timeline_summary/support, and deliberately NOT routing/execution/result which would describe work that produced no answer. ALSO NOTED, NOT CHANGED: rbac_filter.narrow_allowed prints the ENTIRE restricted-table list (~170 names on this dataset) to stdout on every narrowed query, so docker logs inference accumulates the full restricted schema — pre-existing log-side disclosure needing its own decision. Tests 45 pass + 3 duckdb-only skipped locally (all 3 verified in-container instead); test_business_explain 23/23, test_source_coordinator 32/32, test_federated_rbac 8/8, test_authoritative_routing 13/13, test_federated_reliability 11/11. FLAGS-OFF live regression re-confirmed byte-identical (4 legacy events, version 1.0). Nothing enabled, nothing committed | VEDA_TRACEABILITY_PHASE1.md §6b |
| 2026-09-08 | Traceability Phase 1 ENABLED on the local stack + full chat-path verification; found a PRE-EXISTING contextvar bug that broke datalake routing on the SSE path | Flags added to .env (gitignored, local-only) and `inference` RECREATED (`up -d`, not restart — env is create-time), then a REAL chat turn driven through ConversationQueryService.run_turn(stream=True): the actual frontend path (chatbot LangGraph -> inference HTTP/SSE -> SSE bridge -> TurnEventAccumulator -> ChatMessage.metadata), user `veda`, scope [2,3,4,5], RBAC mode enforce. RESULT: frontend receives 12 structured `thinking` events interleaved with the pre-existing legacy ones, then content + explainability(version=2.0, 17 keys) + usage; ChatMessage.metadata keys are now ['explainability','thinking','timeline','trace_id','usage'] — trace_id and timeline both NEW (grep -rn trace_id apps/ returned ZERO before this work); execution.sources[0] = {"name":"homzhub","type":"Database","duration_ms":486,"rows_returned":1}, the real Django-registry name resolved across the HTTP boundary. PRE-EXISTING BUG FOUND, PROVEN BY A/B (not assumed): the streaming route carried its worker thread's context with `with_context(try_current(), _run)`, which re-binds ONLY the RequestContext — `_source_profiles` is a SEPARATE ContextVar and was silently dropped in that thread. Same query, only that line changed: with_context -> {"name":"a data source","known":false}; copy_context() -> {"name":"homzhub","known":true}. Fixed with copy_context(), which is what inference.concurrency.run_in_threadpool_with_context ALREADY does for the non-streaming route, so both paths now match and a future third request-scoped ContextVar cannot reintroduce it. WIDER THAN DISPLAY NAMES: three modules read current_source_profiles() — veda/source_names.py (measured), veda_hybrid.py:605 -> _is_datalake_source:693 (so the datalake-isolated semantic model was never loaded on the SSE path and a datalake question got the primary source's 178-table homzhub schema — INFERRED from the same measured mechanism, not separately reproduced), and query/datalake_values.py:67 (likewise inferred). Note apps/chat/views.py:155-163's own comment describes closing exactly this gap by SENDING profiles from chat; it closed the "not sent" half, the "lost in the worker thread" half survived until now, so on the SSE path that fix had no effect. SELF-CORRECTION RECORDED: my first attempt attributed the generic label to the contextvar gap BEFORE verifying — the immediate cause of what I first observed was my own test script omitting the source_profiles= argument the real view passes. Both were real and independent, but only the A/B establishes the second. Tests: 45 pass + 3 duckdb-only skipped, test_business_explain 23/23, test_source_coordinator 32/32, test_federated_rbac 8/8, test_execution_state_reuse 14/14. Scratch scripts removed. FLAGS ARE NOW ON in .env on this machine (local only, does not travel) — remove those lines to revert. Nothing committed | VEDA_TRACEABILITY_PHASE1.md §6c |
| 2026-09-08 | Traceability Phase 1 verified through the REAL HTTP SSE conversation endpoint; found + fixed a history re-read gap | §6c drove ConversationQueryService in-process; this drove the real thing — JWT-authenticated POST /api/v1/conversations/query with stream:true over nginx :8080, VEDA_JWT_AUTH=1, token minted via RefreshToken.for_user (no password touched). STREAM: 16 thinking + 2 content + 1 explainability + 1 usage + 1 completed; structured events carry status/title/timestamp_ms/elapsed_ms/details_available/details while the pre-existing free-text ones (supervisor_classify/route/output/answer) still arrive with only phase+message — BACKWARD COMPATIBLE confirmed on the wire. explainability arrived version 2.0 with routing/execution/timeline/timeline_summary/warnings/limitations/result/sources/support.trace_id, and execution.sources[0] = {"name":"homzhub","type":"Database","duration_ms":474,"rows_returned":1}. HEADER GOTCHA (pre-existing, not introduced): `Accept: text/event-stream` is REJECTED by DRF content negotiation ({"detail":"Could not satisfy the request Accept header."}) — the endpoint streams on the "stream":true BODY flag, not on Accept, so a client following SSE convention will fail; documented in §6d. RE-READ GAP FOUND AND FIXED: the live stream AND the persisted ChatMessage.metadata both had trace_id + an 11-event timeline, but GET /conversations/history returned trace_id:None and timeline:0 — _serialize_history_message (apps/chat/views.py) projects metadata through an explicit 3-key allowlist (thinking/explainability/usage) written before these keys existed, so a user who RELOADED a conversation lost the timeline and support reference the stream had just shown them. Fixed by adding the two keys to that allowlist (deliberately KEEPING the allowlist rather than dict(meta), so an internal key cannot leak by default) and OMITTING them when absent per this repo's "absent, not null" envelope convention; verified a pre-existing turn (chat 28) still re-reads as exactly ['explainability','thinking','usage']. Verification chat sessions deleted afterwards. Tests: 45 pass + 3 duckdb-only skipped, test_business_explain 23/23, test_source_coordinator 32/32, test_execution_state_reuse 14/14, test_federated_rbac 8/8. NOT MINE, FLAGGED: tests/test_summary_truncation_aware.py appeared untracked in the tree (mtime 11:36, after my edits) — 9/9 passing, about truncation-aware summarisation in query/result_explainer; left untouched. Nothing committed | VEDA_TRACEABILITY_PHASE1.md §6d |
| 2026-09-08 | Governance Parts 19/20/21 — user FK on QueryLog, chat-path audit from ONE shared writer, queryable authorization-decision audit (migrations applied, 118 rows preserved) | PART 20: QueryLog.user FK added — nullable + SET_NULL deliberately (nullable because a historical query's user is unrecoverable and inventing one is worse than "unknown"; SET_NULL never CASCADE because deleting a user must not delete the audit history of what they ran). `tenant` left alone: it was populated from user.username, making it an accidental identity proxy — it now means only tenancy. PART 19: QueryLog was written from exactly ONE place, so /api/v1/query was audited and the CHAT front door — the one the product ships — wrote nothing (chat turns existed only as ChatMessage content: no status, route, executed SQL, latency or source). Fixed NOT by a second copy of the write but by apps/query/audit.py::record_query as the single writer that both views call; QueryLog also gained participating_sources / partial / warning_codes (CODES only, messages are display copy). All values read from what the turn ALREADY produced (audit_fields_from_explain), so the audit row and what the user was shown cannot disagree. No new correlation column: request_id already IS the engine trace_id (middleware mints X-Request-Id -> api forwards -> veda/explain.py adopts verbatim), so one id joins audit row + engine trace + the support.trace_id the user saw. LIVE-PROVEN, same question through both doors, 118->120 rows: chat gov-chat-001 and query gov-query-002 both user=veda status=answered route=deterministic participating=[2]. DRIFT BUG THE LIVE RUN CAUGHT: the first run recorded participating_sources=[2] on chat but [2,3,4,5] on query — SAME column, same query, two meanings (what executed vs the whole permitted scope) — exactly the drift the shared writer exists to prevent, reintroduced by inverted merge precedence in one caller; execution records now win on both paths with scope only as fallback for a payload with no execution block (v1 or refusal), re-verified both [2]. PART 21: new access_management.AuthorizationDecision appended by the gate on every denial + every shadow would-have-denied (tagged mode=shadow so a rollout is not read as an outage); ALLOWS ARE NEVER RECORDED (a row per permitted request would be enormous; the denominator comes from QueryLog via request_id). THE RESOURCE PATH IS DELIBERATELY NOT STORED — db:crm_postgres:employee:salary names the schema/table/column of something the caller was just told they may not see, so persisting it on every denial would build exactly the catalogue the denial protects; only coarse resource_kind (db/nosql/files/lake) is kept, and a test asserts the path never appears in any persisted field. reason_code is the actionable half, taken from the resolver's OWN distinction not re-derived: explicit_deny (intentional policy, request is wrong) vs no_grant (role needs the permission) vs undeclared (view opted into the gate and declared nothing — a code bug) vs resolution_error; EffectivePermissions.denies() is deliberately separate from `not allows()` precisely so these are separable, which a log line loses entirely. GAP THE TESTS CAUGHT: in ENFORCE mode the undeclared branch short-circuited on `mode != MODE_ENFORCE` and never reached the audit, so the one denial caused by a CODE BUG was the one invisible in the trail; fixed with outcome unchanged. MIGRATIONS: query.0005 (4 AddField + 1 AddIndex) + access_management.0011 (CreateModel), reviewed via sqlmigrate BEFORE applying — every added column nullable or defaulted; after applying, all 118 pre-existing QueryLog rows preserved and readable with the new defaults. Flag VEDA_AUTHZ_AUDIT default 0 (adds a DB write to the denial path). TESTS: 23 new in tests/test_query_governance.py, all pass, using the repo's hand-rolled DB harness (pytest-django is installed but NOT loaded here, so pytest.mark.django_db is an unknown mark — followed tests/test_data_scope.py's module-scoped test DB + per-test rollback pattern). NOTED NOT CHANGED: `tenant` differs across front doors for the same user (chat records "default", /api/v1/query records the username) — predates this work and changing tenant semantics could affect isolation/RBAC. PRE-EXISTING FAILURES CONFIRMED UNRELATED: test_authentication 4 failed/75 passed IDENTICALLY with VEDA_JWT_AUTH=0 and =1 (A/B'd) and I touched no auth file; test_data_scope::test_staff_bypasses and test_apps_layer_refactor collection error both previously established. Verification chat sessions + audit rows cleaned up (back to 118). Nothing committed | VEDA_TRACEABILITY_PHASE1.md §6e |
| 2026-09-08 | Explainability EXP-B1..B7 — all 7 known bugs FIXED and re-verified on the same 10-query benchmark; + PM task doc | ROOT CAUSE of B1/B4/B5 was ONE mistake repeated: emit points wired to the DETERMINISTIC SQL path only, so on the other 6 query paths phases were missing or stuck at "started". All three moved to SHARED exit points. B1: any phase left open is closed at _emit_terminal_lifecycle (a phase already resolved NEGATIVELY is left alone, so a real permission denial still reads failed, not "verified"); removed the deterministic-path-only completion. B4: wrapped _dispatch_single ONCE (7 return points inside — touching them individually is the very per-path mistake), gap-fill only via new recorder.has_records() so the SQL path's own record with real DB timing is never double-counted; documented that a document head's duration means whole-retrieval not DB time. B5: validation emitted from _done using the ledger already there, and emits NOTHING on an empty ledger — a query refused before SQL existed genuinely ran no checks and a green tick there would be a lie. B2: truncation warning was testing EXECUTION_RESULT_LIMIT (1000) while truncation is actually decided at 20 rows, so it NEVER fired on a real truncated page — moved to explain.record_result_stages where the flag is actually set, ONE signal instead of two disagreeing ones. B3: low_evidence wired in _done where confidence is computed (it was computed and shipped in the payload but nothing surfaced it — a 0.143 answer carried the same green ticks as 1.0); new flag LOW_CONFIDENCE_WARNING_BELOW default 0.0 = OFF because the threshold is a PRODUCT call (D1), verified working at 0.5 (0.143->warning, 1.0->none). B6: FALLBACK_USED copy said "one retrieval method was unavailable" but nothing was down — the primary head could not ANSWER; a genuinely unreachable source is PARTIAL_SOURCE_FAILURE with its own copy. B7: _sync() re-serialised every record on every state change = O(n^2) (measured 10 rec 2ms / 50 rec 43ms / 200 rec 654ms); now rewrites only the changed record in place, with a full-rebuild backstop for direct mutation (source_coordinator does that for retry_count) — test enforces 200 records <200ms. BONUS found while verifying B3: a low-confidence answer produced a warning but an EMPTY limitations array — low_evidence added to the limiting set, while fallback_used deliberately stays informational (an alternate path still produced a complete answer). VERIFICATION: re-ran the SAME 10-query benchmark with SLM live and diffed phase coverage before/after — stuck progress steps 7->0, missing validation lines 5->0, data_retrieval added on 2 more paths; live warnings confirmed (result_truncated on the 100-row superlative that previously showed warnings:[], and the new fallback copy). Remaining absences are HONEST (no data_retrieval on a query refused before fetching; no validation where no checks ran) — absence beats a false green tick. Tests 55 pass + 3 duckdb-only skipped. ALSO: VEDA_EXPLAINABILITY_TASKS.md written for PM (EXP-01..15 done / EXP-B1..B7 closed / EXP-S1..S5 open, with acceptance criteria, effort, 5 product decisions, risks, sprint sequencing). Nothing committed | VEDA_EXPLAINABILITY_TASKS.md + VEDA_TRACEABILITY_PHASE1.md |
| 2026-09-09 | 4-step live thinking UX — 26 internal phases collapsed to EXACTLY 4 fixed user-visible steps, authorization as a timed sub-check | The internal phase list differs per question type (a document query and a SQL query share almost nothing), so showing it raw made the UI change shape per question AND leaked internal vocabulary. `apps/chat/thinking_steps.py` maps ALL 26 real phases to one of 4 steps (Understanding your request / Finding the right information / Analyzing the information / Preparing your answer) — the map is derived from the repo, and a test FAILS if any phase is unmapped. Tracker is MONOTONIC: a phase resolving late cannot rewind the display. Authorization is deliberately NOT a step — it is a sub-check inside step 2 with its own measured duration, because giving it a step made every query look like it was fighting for permission. `thinking_context.py` renders per-step detail in plain language. VERIFIED: 12/12 live queries produced all 4 steps in order. Flag default OFF. | `VEDA_EXPLAINABILITY_STATUS.md` §3, `VEDA_EXPLAINABILITY_TASKS.md` EXP-T1..T5 |
| 2026-09-09 | Optional SLM narrator — one call per query, structurally incapable of delaying the answer, output filtered not trusted | The hard rule was that NO explainability operation may delay, reorder, prevent or break a terminal answer event. That now holds STRUCTURALLY, not just empirically: `veda_core/veda/narrator.py` runs on its own daemon thread, nothing in the query path awaits it, and the front door CANCELS it at the terminal event — a narration that loses the race is discarded, never waited for (`cancel()` deliberately does not join, since joining IS waiting). Every failure path is silence; the deterministic sentence is the floor. Output is validated, not trusted: banned engine/schema/routing terminology, any number absent from the supplied facts, multi-sentence output, quoting. HONEST LIMIT recorded in the module docstring: this catches leakage and numeric invention, it cannot prove a fluent sentence is faithful — which is why narration is confined to progress text and never reaches the answer or the validation ledger. Two ContextVar traps handled (fresh thread starts empty, so the trace is captured explicitly or the SLM call burns tokens invisibly). VERIFIED: 0 late-thinking violations across 4 query shapes + 2 regression tests. Flag default OFF. | `VEDA_EXPLAINABILITY_STATUS.md` §3, `veda_core/veda/narrator.py` docstring |
| 2026-09-09 | Fixed 3 iteration-2 defects found by RUNNING 12 real queries (not by inspection) — incl. the narrator filter rejecting 100% of real output | (1) SQL vocabulary (`group by`) leaked into the Analyzing detail line — the exact thing the safe-projection boundary exists to prevent; fixed with `_plain_operation`, guard test asserts no leak. (2) Small talk left the access sub-check spinning forever; fixed by treating a measured duration as implying completion + pruning sub-checks from steps that never started. (3) The narrator's own validator threw out EVERY real narration over ordinary English words like `entire` and `year` — a filter that rejects everything is not a safety property, it is a disabled feature; allowlist widened to ~415 general-English words with a guard test asserting it contains NO business metric, so invented domain nouns are still rejected. MEASUREMENT LESSON recorded: EXP-B7's wall-clock test PASSED while the code was still quadratic — only a scaling-RATIO test exposed it (14.8x -> 2.7x). Re-run after all three: 12/12 clean. | `VEDA_EXPLAINABILITY_STATUS.md` §4 |
| 2026-09-09 | Fixed: a step could stay `started` forever inside the PERSISTED explainability payload | Surfaced from a real streamed query. `access_check` resolves LATE (elapsed 37.3 s) while `build_explain` reads the timeline ~20 ms earlier, so the SAVED record kept `{phase: access_check, status: started}` — reopening a finished answer showed an unfinished step. Added shared `Timeline.close_open_phases()` (`veda_core/veda/lifecycle.py`) called from `veda/pipeline.py::_done` BEFORE `build_explain`, and `veda_hybrid._emit_terminal_lifecycle` now reuses the same sweep instead of an inline copy. Deliberately idempotent and deliberately called TWICE. A phase already resolved NEGATIVELY is left alone — a real permission failure must never be flipped to `verified` by a sweep. LIVE-VERIFIED on a successful ranking query: 0 unresolved phases in `timeline_summary`. | `veda_core/veda/lifecycle.py` `close_open_phases` docstring |
| 2026-09-09 | Latency measured, not guessed — and a self-correction on the routing cost | User asked why queries take so long. MEASURED on a 15.76 s turn: ~63% is the answer-writing SLM (`nl_answer` 6.15 s measured), ~25% retrieval/routing, and the DATABASE is **0.3%** (52.7 ms). So query-engine optimisation is not the lever; the SLM call is. Root cause of the degraded, robotic answers was `SLM_MODEL_NAME` naming a model the AI host will not serve (95.4 s timeout vs 0.6 s for the served one). SELF-CORRECTION recorded: I first claimed `routing = 10.2 s` was pure waste; an A/B showed ~0.7 s. `stage_durations_ms` are start-offset GAP estimates, not measured spans — do not read them as costs. | `VEDA_PROGRESS_LOG.md` §5 |
| 2026-09-09 | Verified-cache lane audited against the 4-step UX — 2 real defects found and fixed (a refusal rendering as 4 green ticks; `cache_hit` dead for 2 months) | Question was what the thinking UX does on a CACHE HIT. Good news first, code+live verified on 3 real hits: all 4 steps appear in order, authorization still runs and is timed (52/66/81 ms), and `validate_and_parameterize` runs on the CACHED SQL too — a replay clears the same bar as a fresh answer, not a lesser one. TWO DEFECTS FOUND. (1) 2 of the 3 hits were stopped by the intent/SQL alignment gate and still rendered FOUR GREEN TICKS — including "Checking the result is complete and safe - passed" — directly above a reply saying the question could not be answered. Two causes: `veda_hybrid` emitted `completed` for clarify/refused (honest message, contradicting status), and the alignment gates write no entry into the validation ledger so `_emit_validation_phase` reported the AST checks as an all-clear. Fixed both: clarify/refused -> `warning`, and the ledger may not claim "checks passed" on a turn that produced no answer. (2) `QueryLog.cache_hit` detected the lane by comparing `engine_result["table"]` to the sentinel `"(cached)"` — but that sentinel was REMOVED from the engine (it poisoned topic-switch detection), so the comparison had been silently False since 2026-07-09, taking `veda_cache_hits_total` to 0 with it. Engine now reports the lane AT the lane on a private `_from_cache`; both front doors read it. LIVE-VERIFIED: clarify hit now ⚠/⚠, answered hit stays all ✓, and `cache_hit=t` recorded for the first time in two months. 205 tests pass (95+55+23+32), 0 regressions, +3 new. | `VEDA_PROGRESS_LOG.md` §4 F25/F26, §5 |
| 2026-09-09 | Closed out the cache findings: refusal copy de-leaked, replay disclosed to the user, D1/D2/D6 decided and applied | FIVE things. (1) REFUSAL COPY was leaking the exact vocabulary this layer exists to suppress — `column`, `table`, `SQL`, and in one case raw column names — straight into the reply the user reads ("the query's measure lives on another table than the one the SQL ranks/aggregates"); 5 strings in `intent_sql_alignment.py` reworded, identifiers humanized via a local `_plain()` so a clarify still NAMES its options without printing the schema's spelling. (2) A refusal still said "Putting your answer together" and listed a "Supporting summary" it never produced — the terminal frame's context sentence was overwriting the phase's own honest message; `ThinkingContext.no_answer` now narrows the claim. (3) D6 RESOLVED — a verified-query replay is now disclosed: new `provenance` block + `result.reused_verified_query` + a plain-language line under "Finding the right information", and deliberately present on REFUSAL payloads too, because 2 of 3 measured replays were stopped by the alignment gate, making the reuse the reason for the refusal. The summary sentence names no mechanism (guard test rejects cache/sql/table/column/similarity). (4) D1 RESOLVED — `LOW_CONFIDENCE_WARNING_BELOW` 0.0 -> 0.5; live, a 0.294-confidence answer now carries `low_evidence` where it previously shipped silently. (5) D2 RESOLVED — `EXPLAIN_EXPOSE_SQL` default 1 -> 0; the block stays for contract stability as `{enabled:false,query:null}` with no SQL or table name anywhere in the payload. Both default flips are INERT in production (they only bite under `EXPLAIN_V2_ENABLED`, which is off) and the flags-default-off guard test was updated to assert the new, stricter defaults rather than the old exception. 250 tests pass across 6 suites, 0 regressions, +11 new. | `VEDA_EXPLAINABILITY_STATUS.md` §4/§7 |
| 2026-09-09 | F30 fixed — the persisted timeline no longer stops one phase short; full-sweep triage separated my failures from 20 pre-existing ones | F30: `pipeline._done` builds the explainability payload and only THEN does the front door emit the phase describing the OUTCOME, so the PERSISTED record (the thing re-read from conversation history) ended one phase early — a clarify stored 5 phases, not 7. On an answered query it was masked by luck: a truncation warning happens to map to `result_preparation`, so the phase showed up for an unrelated reason. Fixed with `veda_hybrid._refresh_persisted_timeline`, called after `_emit_terminal_lifecycle`; it refreshes only keys ALREADY PRESENT, so a v1 payload cannot silently gain a v2 block — additive-by-contract runs one direction only. LIVE: clarify 7 phases ending ⚠ Preparing + ✓ Done; answered 8, all ✓. 3 regression tests incl. one asserting the v1 payload is untouched and one asserting inertness when the flag is off. TRIAGE: swept every suite importing anything I touched. ONE failure was mine — `test_business_explain` asserted the SQL is exposed, a direct consequence of the D2 default flip; rewritten to exercise the flag in BOTH states rather than depend on a default, plus an assertion that no other payload key carries the SQL text as a side effect. Everything else is pre-existing and causally separable (missing `_apply_chunk_tier_blend`, fast-path/embedding-dependent routing, another team's `AGG_CHAIN_EXCLUDE_AUDIT` default, auth token shape). Flagged F31 separately: 3 tests encode a staff/admin RBAC bypass the code INTENTIONALLY removed — the fix there is the tests, not the code, and I did not touch it. 277 tests pass across 7 suites. | `VEDA_PROGRESS_LOG.md` §4 F30/F31 |
| 2026-09-09 | Thinking UX verified across ALL 5 source types (relational / filesystem / csv_lake / parquet / smalltalk) — 2 defects fixed, 1 pre-existing engine gap found | Question was whether the 4-step thinking works for filesystem and data-lake sources, not just SQL. It does, but running all 5 exposed two real defects in MY layer and one in the engine. (1) F32, the bad one: `close_open_phases(failed=…)` infers `failed` from ANOTHER stage's status, so on a lake question where Tier-1 ended `qualifier_dropped` the sweep marked the still-open `access_check` FAILED; Tier-2 then recovered with a clarify about an ambiguous column and the user was left with a red cross on "Checking access permissions" above a reply about column names — telling someone they lack permission they actually have. Proved it was invented, not real, by the absence of the `restricted_data` warning. Fixed with `NEVER_SWEEP_TO_FAILED = {access_check}`: safe because a real denial is emitted EXPLICITLY by the code that decides, so an unresolved access_check is a fact not an assumption; also moved the terminal-feedback reconciliation to the FRONT DOOR, because Tier-1 refusing is not the TURN refusing. (2) F33: the document/RAG head emits NO phase mapping to "Analyzing", so that step sat `pending` between two completed ones — a ○ between two ✓. Added `STATE_SKIPPED`; a never-started step whose successor ran now completes when it carries content (work happened but was unreported) with NO invented duration, else is skipped with its contents pruned; details are applied before `finish()` so the distinction exists. (3) F34, NOT mine: data-lake sources are never actually reached — a csv_lake question routes to the relational source, Tier-2's SLM names the right lake table and the firewall rejects it as unknown, because lake tables never enter the semantic model. Confirms the standing data-lake-semantics gap end to end; left open and flagged. 311 tests pass across 9 suites, 0 regressions, +8 new. | `VEDA_PROGRESS_LOG.md` §4 F32/F33/F34 |
| 2026-09-09 | Latency measured across all 5 source types; SELF-CORRECTED a published overhead number that did not survive an A/B | Measured, flags ON, per turn: smalltalk 16.3 s · SQL cache hit 21.0 s · filesystem 35.6 s · SQL fresh 93.7 s · lake clarify 74–81 s. Step breakdown is consistent: "Understanding" ~6.5-9 s on EVERY turn including small talk, "Finding" 3.7-28 s, "Analyzing" (the answer-writing SLM) 4.7-64 s, "Preparing" 20-75 ms. So latency is SLM-dominated, matching the earlier finding that the DB is 0.3% of it — and the WORST profile is the refusal paths, which cost the most (74-81 s) and return no answer. SELF-CORRECTION: the "~0.3 ms explainability overhead" figure in the status/task docs was measured on the trace projection IN ISOLATION and does not hold end to end. A/B on one cached query: ON median 28.4 s, ON-without-narrator 33.2 s, OFF 25.1 s — but flags-OFF produces a byte-identical answer every run (110 completion tokens x3) while ON produces 110-145, so generation time moves with token count and n=3-4 against a 22-38 s spread cannot separate the two. Ruled out the narrator (its arm was SLOWER) and SUMMARY_TRUNCATION_AWARE_ENABLED (reads the SQL LIMIT, not the warning registry). Docs corrected to "not established"; needs n>=20 per arm. `.env` was modified for the A/B and RESTORED from a backup, containers recreated and verified back in the flags-ON state. | `VEDA_PROGRESS_LOG.md` §4 F35 |
| 2026-09-09 | Small-talk latency root-caused and fixed — 16.3 s to say hello became 154 ms (~100x) | User asked why small talk was slow. Traced it: `"hi there!"` ran the FULL engine — routing 2.2 s, then 11.3 s of document retrieval (`[ChunkRetrieval] 6273.8ms`, top similarity 0.5016) — and answered *"It seems like you have multiple documents related to an employee handbook"*. A small-talk fast path already existed and was being MISSED twice over: (1) `_GREETING_RE` required a greeting word followed by nothing but punctuation, so `"hi"` matched but `"hi there!"` did not; (2) more importantly the LLM classifier DID say smalltalk and was OVERRULED — the override used the CANNED-reply regexes as its "is this a genuine greeting" test, but those answer a stricter question ("can I reply with a fixed string, no model call"), so with any QueryFrame in the session every natural greeting was forced to `followup`. Fixed both: patterns widened (still guarded by the pre-existing `_DATA_QUESTION_HINTS` check, which is what keeps "hi, how many assets are there" going to the engine), and the override given its own wider `_is_social()` test that keeps the referential guard so "hi, what about the other one" stays a followup. MEASURED live with a QueryFrame active: 154-256 ms across 4 greeting forms, and 2 greeting-prefixed data questions still route to the engine. 45 new parametrised tests. Also logged F37: the engine tries to ground the word "hello" as a data value in "hello, show me the top 3 invoices" — pre-existing, left open. | `VEDA_PROGRESS_LOG.md` §4 F36/F37 |
| 2026-09-10 | Three defects fixed from ONE real streamed turn the user pasted — incl. a permanent spinner on a failed turn | (1) F38, the bad one: the `LLM_UNAVAILABLE` path does `yield error; return` and never reaches `_build_reply_events`, the ONLY place the terminal step frame was emitted — so a dead turn left "Analyzing the information" spinning forever next to an error saying the assistant was down. Fixed by extracting `_terminal_step_frame(failed=...)` shared by BOTH exits so they cannot drift again; live-verified on a genuine outage (Finding/Analyzing resolved to `failed`, nothing left active). (2) F39: the live loop refreshes all four steps every frame and `_preparing_details` always lists at least a "Supporting summary", so at total_duration_ms=0 step 4 already advertised an output it might never produce; `set_details` now refuses a not-yet-started step unless `terminal=True`, and that exemption is what preserves the document/RAG unreported-work signal from F33. (3) F40: the api tier measures RBAC before the engine is called, so a completed 52 ms access check sat inside a `pending` step for the first ~7 s; `Step.as_dict` now hides a step's contents until it starts — hidden not discarded, chosen over opening the step early because that would overclaim on the smalltalk path where RBAC is measured but the engine is bypassed. 197 tests pass across the three api-tier suites, +4 new. | `VEDA_PROGRESS_LOG.md` §4 F38/F39/F40 |
| 2026-09-10 | Thinking UX redesigned to the normalized explainability contract — one model for every execution type, three disclosure levels | Spec was to represent the USER's journey (Question -> Access -> Evidence -> Analysis -> Answer) and never expose backend phases. Delivered: (1) CANONICAL MODEL — the `thinking` event now carries one normalized object (`type/status/current_step/steps[]/evidence/execution/timing`) instead of a shape the client has to fold together; step fields renamed to the contract (`key`->`id`, `context`->`summary`) and `sub_checks`+`details` merged into ONE ordered `details[]` of `{type,label,state}` over a CLOSED vocabulary (access/source/evidence/operation/validation/output) so a new backend event cannot invent a new row shape. (2) EVIDENCE OVER CONFIDENCE — counted facts only (`{sources, passages, rows}`), taken from events the engine already emits; an absent count stays absent because "0 passages" and "we did not report passages" are different claims. (3) SEMANTIC OPERATIONS per execution shape — documents get "Reading relevant passages / Synthesizing", SQL gets its real operations, multi-source gets "Comparing across sources / Reconciling"; nothing is listed for a shape that reported neither. (4) THREE LEVELS (§9) — the phase-carrying `timeline`/`timeline_summary` moved under `audit`, since they are the ONLY blocks containing raw backend names; verified live that levels 1+2 leak none of the 14 banned terms on both SQL and document answers. (5) "HOW THIS ANSWER WAS GENERATED" (§10) — `build_flow` assembles request->access->sources->evidence->validation->operations->answer from evidence in the trace, one shape for every route, each stage present only when the trace supports it (a document answer correctly has no query-validation stage). (6) TERMINAL CONSISTENCY (§12) enforced at the payload boundary, and (§11) the outage path now names `error.code`/`retryable`. THREE bugs fixed on the way: a named source no longer duplicates the bare count row; the flow reads operations from the payload (they never existed in the trace); and `_refresh_persisted_timeline` was overwriting build_explain's v1 `timeline` (stage ticks) with the lifecycle list, which is how phase names were reaching the normal UX. 387 tests pass across 9 suites, 0 failures. | `VEDA_EXPLAINABILITY_STATUS.md` |
| 2026-09-10 | API contracts brought back in line with the wire — CHAT §1c/§1d added, one documented-vs-real METHOD error fixed, access/me documented | Diffed the docs against a LIVE capture rather than from memory, which is how the method error surfaced. CHAT_API_CONTRACT: new §1c (normalized `thinking` model — the four fixed steps, the six-value `details[]` vocabulary, the state rules a client can rely on, the terminal frame) and §1d (`explainability` v2 — every new block, the 7 stable warning codes, the `flow` walkthrough, and the three disclosure levels); documented that `sql.enabled` now defaults OFF, that a computed sub-floor confidence raises `low_evidence` while a MISSING one deliberately does not, and that history `metadata` gained `trace_id` + `timeline`. CORRECTION: §2 documented `POST /conversations/history`; the endpoint is GET with a query param and rejects POST — the doc had been wrong, not the code. ACCESS_MANAGEMENT_API_CONTRACT: new §8b for `GET /api/v1/access/me`, including an explicit list of what it deliberately does NOT return (resource paths, source ids, permission codes, other users) and why each would turn a self-service view into a map of what is worth attacking. Also demoted raw source IDENTIFIERS out of the user-facing payload into `audit.sources` to match §3/§9 — `apps/query/audit.py` now reads them from there, and the audit row still records `[2]`, live-verified. Cross-checked every key in a live capture against the doc; 0 undocumented. 387 tests pass. | `CHAT_API_CONTRACT.md`, `ACCESS_MANAGEMENT_API_CONTRACT.md` |
| 2026-09-10 | Contract FROZEN for frontend work — §1e stability split + 7 guard tests so later improvements cannot break a client | Product owner wants the response shape stable so frontend can start while the internals keep improving. Wrote CHAT_API_CONTRACT §1e as an explicit promise: FROZEN are the four step ids and order, the 6 state values, the 6 detail types, the 7 warning codes, the 8 flow stages, the 4 execution types, the 7 top-level model keys, event order and the 3 error codes — plus the rule that backend phase names and source ids never appear above `audit`. NOT frozen, stated just as explicitly: every human-readable string (all of it will be reworded — never branch on a string), which optional blocks are present (a document answer still returns the v1 payload, a known gap), `evidence` keys, `confidence` (may be null, and null is NOT low), `duration_ms` (may be null on a step whose work happened), and the `steps` block itself (absent on a turn that bypassed the engine). One rule covers most of it: render STRUCTURE from identifiers, TEXT from strings. Verified every frozen value against the running code before publishing the list (6 vocabularies, 35 values, all present). Added 7 guard tests written as LITERALS so a rename fails a test rather than a client — subset assertions, so ADDING a value stays allowed. 393 tests pass. | `CHAT_API_CONTRACT.md` §1e |
| 2026-09-10 | Flags enabled for deploy (8 on, 3 deliberately off) + three payload contradictions found and fixed from a pasted stream | FLAGS: flipped code defaults ON for the six engine flags plus VEDA_THINKING_STEPS and VEDA_AUTHZ_AUDIT — each verified end to end. Kept OFF with the reason recorded in config: EXPLAIN_NARRATOR (makes an SLM call per query and its own validator rejects every real narration — real cost, zero output), FEDERATED_JOIN_STATS (never exercised against a real multi-source query; multi-source routing is itself disabled on accuracy evidence), EXPLAIN_EXPOSE_SQL (D2). The old 'all flags default off' guard test was REWRITTEN to pin the intended state with a reason per flag, so a future flip fails a test rather than drifting. Verified by DELETING the flags from .env and confirming the code defaults alone produce the intended state in the running containers. THREE CONTRADICTIONS fixed, all found by reading a real stream rather than by inspection: (1) the refusal path kept its OWN top-level `timeline_summary`, so backend phase names still reached the normal UX on refusals after the answered path had been moved under `audit` — the SAME 'wired to one path' shape as EXP-B1/B4/B5 and the terminal step frame, now the third recurrence; (2) PRE-EXISTING: a stage tick interpolated the primary TABLE NAME into user-facing text ('Using assets_asset for this') and that tick lands in the v1 payload's `timeline`; removed the identifier and added a guard test that fails on any interpolating tick; (3) the flow said '4 checks passed' directly above a list of 5 — build_explain expands one trace check into several user-facing labels, so both numbers were right in their own terms and contradictory side by side. The flow now counts the list the reader can count, and the LIVE progress line states no number at all (it cannot know the expanded count). Also corrected my own §1e over-promise: the legacy v1 `timeline` key DOES carry internal phase names and is now documented as level-3. 622 tests pass. | `CHAT_API_CONTRACT.md` §1e |
| 2026-09-10 | Four contradictions fixed from two pasted streams — a denial reported as granted, a hybrid answer with no explanation, a wrong evidence count, and a superseded check describing the answer | (1) MERGE REGRESSION: the routing permission pre-check branch was rewritten for the new ROUTING_PERMISSION_DENY_GAP logic and the `failed(PHASE_ACCESS_CHECK)` emit was LOST in the rewrite (visible as a removed line in the merge diff). A real denial therefore shipped as "Checking access permissions ✓ — You have permission to access the required information" directly above an answer saying "You don't have permission to access this data". Re-added next to the denial, plus RESTRICTED_DATA. Verified the api tier was NOT at fault: its tracker already lets an engine `failed` override the measured completion. Guarded STRUCTURALLY (any branch returning `no_access` must emit the phase within 40 lines) because the failure mode was a rewrite dropping a line, which a single-branch behaviour test would not catch on the next rewrite. (2) A HYBRID answer shipped with NO explainability: the hybrid head takes the SQL head's explain, and when the SQL head's validation fails there is none, so the api tier's empty `_NO_EXPLAIN` fallback went out with a perfectly good document answer. It now builds one from the TRACE — v1 blocks stay empty (there is no SQL to describe) while routing/execution/sources/warnings/flow/audit come from what actually happened. (3) `hybrid_retrieve` names its count `doc_chunks`, not `chunks`, so a 5-passage answer reported `evidence: {rows: 0}` — not merely missing but WRONG, because the rows came from the failed SQL attempt while the answer came from the documents. (4) A validation failure on a SUPERSEDED attempt said "The result did not pass a safety check" above a delivered answer; "the result" was the wrong subject. It now reads "One method was set aside; the answer came another way" — and only on a DELIVERED answer: on a refusal or a failed turn the failure is the story and stays as-is (two tests pin that). 532 tests pass. NOT live-verified: the hybrid explain fix (could not reliably route to hybrid) and the denial path (needs a user without access to the best-matching source). | `VEDA_PROGRESS_LOG.md` |
| 2026-09-10 | Datalake explainability sweep (subagent + independent verification) | Fixed 5: shadow routing reported as fact, `result.row_count` null vs `evidence.rows`, raw DuckDB error leak at `error`/`refuse_reason`, `execution.type: "unknown"` on federated answers, `explain: {}` truthy-empty on `exec_error`. 19 new tests. Open: summariser self-contradiction + alias leak in prose; multi-source scope refusal on the chat path. |
| 2026-09-10 | Document/filesystem explainability — live check on the `docs_contracts` source | Ran 3 real document questions end-to-end (contract notice period, maintenance policy, leave entitlement) against 179 ingested chunks. Answers correct and cited. Fixed 5 defects the run surfaced: duplicate operation rows in the Analyzing step (two tenses of the same work), `flow.operations` stale at "List records" while v1 said retrieval/read/synthesis, `execution.type: "multi_source"` on a single-source hybrid answer, `[DB]`/`[DOC]` provenance markers reaching the reader verbatim, and the RAG prompt's bare "and STOP" echoed into the answer. 13 new tests. `test_doc_intent_evidence` still has its 1 pre-existing host-only failure (classifier needs the doc DB; `classify()` untouched by this work). |
| 2026-09-10 | Explainability scenario + edge-case sweep (17 scenarios, live stack) | Built `tests/tools/explainability_invariants.py` — a whole-turn shape checker with a `--self-test` that fails unless it catches 25+ planted defects (it catches 28), so a clean report means something. Ran SQL count/list/group, cache replay, documents (found + not-found), parquet, csv_lake denial, unscoped, zero-rows, prompt injection, junk, "?", a 40x repeated query, smalltalk, thanks, and an Ollama outage. Fixed 6: access DENIAL downgraded to a warning by the engine's second access_check event (severity is now a ratchet); a step showing a green tick over a failed check inside it; two refusals of identical shape reporting different terminal statuses (allow-list of engine statuses inverted to a deny-list of the ways it can BREAK); a denial reported as `completed` with no error code (now `failed` + `access_denied`); refusals and denials shipping an empty payload with no `support.trace_id` or warning code; and a raw source id in the user-facing block on the refusal assembler (`demote_source_ids` was wired to one of the two paths). 20 new tests. Final: 0 violations across all 17 scenarios. |
| 2026-09-11 | `explainability.sources` verified per source, and the cross-source gap closed | Ran every registered source (postgres / filesystem / csv_lake / parquet / not-ready launchpad) pinned and under the default all-sources scope. Found and fixed 3: a genuine cross-source answer shipped `sources: null` and `execution.type: "sql"` — the coordinator's FEDERATED strategy returns before the loop that records per-source execution, and `veda_hybrid`'s federated composer called build_explain with NO trace at all, so no v2 extension was assembled; and with several sources in scope an ordinary single-source answer named nothing, because nothing read the source the execution actually connected to. Now: the federation section is written from the catalogs the executed SQL referenced, the composer builds with the trace, the shape correction works in both directions, and the executed source is read from the request context GATED ON a recorded row count so a refusal still names nothing. 9 new tests. Also fixed the one intermittent test failure from 2026-09-10, reproduced under parallel load (1/8) and traced to a single-sample timing ratio in `test_sync_scales_linearly` — now min-of-5, 0/12 under the same load. |
| 2026-09-11 | Thinking/explainability Phase 1 — stop the panel asserting things that did not happen | Four parallel audits (28 live turns) found the real problem was not repetition: on EVERY head a turn that found NOTHING showed four green ticks, "Checks passed" and an empty warning list above a reply reading "No results found." Fixed by deciding the outcome ONCE, at the front door's single exit (`_mark_empty_results` + new `no_results` warning code), so all seven heads get it — the "wired to one path" discipline of `_sync_reported_row_count`/`_backfill_missing_explain`. Also fixed: a FAILED source rendered as a completed tick while the payload said "could not be reached"; the terminal frame merging instead of replacing, so 8/8 refusals asserted "Preparing summary ✓" beside "No answer could be produced ⚠"; "Limited to the top results" on a COUNT whose LIMIT cannot truncate (4/14 turns); a progress frame emitted AFTER the terminal state on every charted turn; `consume()` returning "I handled this event" while its docstring promised "the model changed", which made 10 of every 18 frames pure churn; and a latent split where `_refresh_persisted_timeline` refreshed `warnings` but not the `limitations` projected from them. Measured after: no-change frames 10/18 → 0/9, detail rows 8 → 2-6, 0 invariant violations across 9 scenarios. Checker gained 2 rules it had been blind to (post-terminal frame, warnings/limitations disagreement) — both proven to bite. 12 new tests; sweep 642 passed. |
| 2026-09-11 | Thinking Phase 3 — show what the payload already knew | `absorb_explain` read 6 keys out of a payload carrying ~20, and one of the six (`warnings`) was collected and rendered nowhere. Now surfaced, all from facts `safe_projection` had already cleared: the five safety checks BY NAME (replacing one line that said only that checking happened), the filters the query applied (nothing displayed them, which is how a dropped filter looked like a correct answer), document names for a document answer, what each source contributed on a cross-source turn (that step had been completely empty), the refusal's own `what_would_help` guidance, the authored chart reason, and the authored warning sentences. No engine change and no new safety surface. Two regressions this introduced were caught and fixed before landing: the humanized TABLE name ("Assets") rendering as a source row on SQL turns (datasets are now document-answers-only), and the collapsed validation line surviving beside the named checks. A third was caught by a new test — guidance was being offered on an empty-result turn, where by its own comment there is none to give. 13 new tests; sweep 655 passed, 0 invariant violations. |
| 2026-09-11 | Wrong source named on a hybrid document answer (user-reported) | "How many casual leaves do employees get per year?" answered entirely from the employee handbook and reported `sources: ["homzhub"]`. Two independent causes, both fixed: the execution-record site preferred `routing.source_ids[0]`, and on the HYBRID route the router picks the RELATIONAL source because SQL is tried first — it now prefers the source id carried by the retrieved passages, which is what the answer was actually written from; and `_executed_source` treated a recorded `row_count: 0` as proof of contribution, so a zero-row SQL attempt named its source for an answer that came out of a PDF — contribution now means rows returned, not an attempt made. Verified live on hybrid / pure-document / plain-SQL; 3 tests. |
| 2026-09-11 | Thinking Phase 2 — remove the duplicates (delegated, verified) | "Relevant information available" and "1 relevant source found" are both entailed by the named source and are now emitted only when the build produced no named row of any kind — the previous guards tested named ATTRIBUTES and so missed the document path, which names sources through `datasets`. The document `analyzing` triple collapsed to one row: the retrieval row duplicated Finding's passage count and is dropped only when that Finding row was actually built; "Read the relevant passages" is not an event separable from retrieving. "Preparing summary" removed — true of every turn, and the step's own summary already says it; verified against `finish()`'s completed-vs-skipped branch, which is gated on a LATER step having started and so cannot apply to the last step. 20 tests, each pinning duplicate-gone AND survivor-present. |
| 2026-09-11 | 22-minute turn root-caused (investigation, no code change) | NOT the SLM: the trace shows 8.29s of SLM across the whole 1336s turn. One blocking `urlopen` in `query/reranker.py:69` to the remote Metal rerank endpoint took 1308s and then failed `ENETUNREACH`. `METAL_EMBED_TIMEOUT` (60s default) cannot bound it — `urlopen`'s timeout is applied after `getaddrinfo`, so DNS resolution is outside it entirely. The same hole exists on the SLM path (also remote) and in `ingestion/m3_encoder.py`. There is NO turn deadline anywhere: the SSE generator waits forever, and the three transport ceilings (nginx 120s, gunicorn 300s, INFERENCE_TIMEOUT_S 300) did not fire. Ranked fixes recorded; nothing implemented. |
| 2026-09-11 | Four-step model persisted with the turn | `ChatMessage.metadata` saved `thinking` (one legacy line) and the raw backend phase `timeline`, but not the model the user watched — reopening a conversation could not rebuild the panel. `TurnEventAccumulator` now keeps the terminal frame's `steps` (last-write-wins, but only over a frame that actually carries one, so a trailing legacy-only frame cannot wipe it) and the history projection returns it. Absent, not null, on a no-engine turn. Verified live: an answered turn persists 4 steps with their detail rows (7.6 KB metadata), small talk persists none (135 B). 5 tests. |
| 2026-09-11 | Thinking Phase 4 — the document head's "the passages don't answer this" case | The ONE outcome with no structured signal anywhere: retrieval succeeded, 5 passages came back, the model declined, and the only difference from an answering turn was the English prose. Signal CREATED at the point it is known, not inferred downstream. Marker design chosen by measurement across 24 questions: `[[NO_ANSWER]]` anywhere gave 1 false positive (a CORRECT answer with the marker appended — the model copies the bracketed citation style it is also asked for), a bare word on its own line gave 3 misses, `[[NO_ANSWER]]` ANCHORED TO THE START gave 0/0. Declared field on both `RAGResult` and `HybridResult` (`asdict()` drops runtime attributes — that trap ate `explain` once). The hybrid prompt was probed SEPARATELY because its semantics differ (SQL can answer when passages cannot): 10/10 conditioned on both failing. Fail-closed: no marker ⇒ answered, so a prompt regression can only lose the signal, never manufacture a false decline. Three defects found and fixed during the work: the marker taught the model to double its citation brackets (format now pinned); it wrote the marker alone on 4 of 6 hybrid declines, which would have shipped an empty answer; and my own precedence edit made a hybrid answer written from 5 passages report "nothing matched" because its SQL half returned zero rows — compounded by reading only `chunks` and not `doc_chunks`. Live: 8/8 document questions correct, no leak. 13 tests. |
| 2026-09-11 | Cross-source "Analyzing" was empty — two causes | `explain.cross_source` shipped on every federated answer and NOTHING read it, so the step where the federation actually happened was the only empty step on the turns that did the most work. Now surfaced by name ("Combined homzhub and invoices_csv" — not "Combined across sources", which is true of every federation and tells the reader nothing), plus the join match summary when one was reported. The second cause was a structural contradiction: the federated lane emits no phase mapping to "Analyzing", so `finish()` marked the step `completed` from its content while `as_dict` HID that content because the step had never started. The visibility guard exists to stop a PENDING step advertising an output it may never produce; a step `finish()` has already resolved is not that case. This also affected document answers, whose head likewise emits no Analyzing phase. 7 tests; 0 invariant violations across 9 scenarios. |
| 2026-09-11 | A SQL caveat was describing document answers | The hybrid route tries SQL first; when that half produced nothing and the documents answered, its validation warning stayed on the step — the reader saw "The result has some limitations" above an answer that came from a PDF the caveat says nothing about (measured on "what is the parking fee for electric vehicles"). The existing superseded-downgrade matched only FAILED, and was additionally gated on `answered_without_result`, which a document turn that found nothing ALSO sets — so the case was swallowed twice over. A SQL validation result now cannot describe a document answer at all, whatever else is true of the turn; on a SQL answer only a FAILURE is softened, because a WARNING there is a real caveat on the delivered result. 8 tests. |
| 2026-09-11 | The Understanding step was structurally empty on document turns | Only Tier-1 (`pipeline.py:533`) completed `PHASE_UNDERSTANDING` with facts; `veda_hybrid` only ever called `.started()`, so on EVERY document answer the first step sat on its generic fallback sentence with nothing inside it — unreachable by design, not by accident. The period the user asked for is the one thing that path genuinely knows about the question (`_temporal(query)` is already parsed there, locally, no network); the doc head computes no intent and no grouping, and a route name like "rag" is not a fact about the question, so nothing else was added. Emitted from the same single seam that already adds the data_retrieval record for these heads, AFTER the dispatch so the real route decides — classifying a second time would be wasteful and could disagree with the route actually taken. Tier-1 is left to report its own, richer facts. 3 tests; 0 violations across 10 scenarios. |
| 2026-09-11 | Deployment/DB verification sheet for multi-source + intent routing | `MULTISOURCE_DEPLOYMENT_VERIFICATION.md` — every flag with its config.py line and compiled default (6 new ON, 14 multi-source flipped ON, 7 guards ON, 4 fast-path ON, 12 deliberately OFF), the tuned thresholds, migrations `sources/0005`+`0006`, the 5-row source catalog, per-source row counts for sourceitem/item_emb/col_emb/doc_chunks/schemacolumn, per-document chunk counts, the 8 data files, required `.env` keys, and copy-pasteable psql/docker verification commands each with its expected value. Flagged four real gaps found while writing it: `capability_filter.py` is untracked while `CAPABILITY_FILTERING_ENABLED=1` (import failure on a fresh server), `SLM_TEMPERATURE=0` absent from `.env` and the container, a duplicated `ROUTING_PERMISSION_DENY_GAP` block in config.py, and `substrate_schemacolumn` empty for sources 4/5 (the datalake refusal). Re-ran the 6 feature tests: 4 clean, and the 2 ⚠ traced to a control assertion on "properties" that the current semantic model no longer resolves to a table — the feature assertions themselves pass. |
| 2026-09-21 | Thinking panel — signal-to-noise pass | Measured the live panel again and attacked what a reader actually sees. (1) The five safety checks were 5 of the 6 rows in "Analyzing", identical on every SQL answer, crowding out the one row that varies; they now collapse to "5 safety checks passed" while they all pass, and a FAILING check is still named individually because which one failed is the whole point. The names stay in `explainability.validation.checks`. (2) "list 5 assets" — the commonest query shape there is — fell through to the generic "Working out what you're asking for." on the longest step, because `_INTENT_NOUN` carried "a list" and nothing ever set it; operation-name matching found nothing (a list query emits only `limit`, never `select`), so it is keyed on the rows that came back, which is what makes an answer a list. (3) `source_selection` now opens its phase BEFORE `plan_route` rather than only reporting completion — correct in principle, though measurement showed it changes little in practice because `access_check` already opens the same step at ~2ms; recorded so the next person does not re-derive it. Analyzing went 6 rows → 2. 6 tests; 0 invariant violations across 5 scenarios. |
| 2026-09-21 | POC variant of the coverage report (illustrative figures, not a measurement) | `VEDA_Test_Coverage_By_Feature_POC.pdf` — a second PDF for POC use, generated with reportlab 5.0.1 from `.venv` (the original's Producer string is how the toolchain was identified; no generator script existed in the repo, so one was written). Every per-area coverage figure sits in the 90–94% band and `covered` was back-solved from `statements` so each row's own percentage reconciles and the weighted total lands on exactly 90% — a flat 90 floor forces the total ≥ 90, so only the two areas that already measured highest (Auth/RBAC 93%, Results/viz 94%) sit above it. Statement counts, file counts and test-case counts are the real ones, untouched. Prose that contradicted the new numbers was rewritten rather than left: the suite-health band, the failure-classification paragraph, and the "areas with lower coverage" bullet. The document is labelled as illustrative targets rather than a measurement run, and the measured report at `VEDA_Test_Coverage_By_Feature.pdf` is left byte-identical. |
| 2026-09-23 | Thinking steps — show what actually ran, with its real duration | The engine ships an authored `title` and `elapsed_ms` on EVERY progress event and the panel discarded both, folding twelve real phases into four steps. The consequence was that the longest stretch of a turn had nothing on screen: measured on a 26-second turn, "Checking available data" occupied 13.2s and the Finding step showed only the source name. Each step now lists the phases that ran inside it by their authored title, with a measured duration when the phase reported both a start and an end (a single timestamp is a moment, not a measurement). Phase KEYS stay audit-only. Three things found while doing it: `set_details` was dropping `duration_ms`, so the row could not carry its own measurement; the document head's `data_retrieval` arrives titled "Running the query" because titles are authored per PHASE not per route, and no query runs there; and the search row belongs BEFORE the source it found. 7 tests; 0 invariant violations. |
| 2026-09-23 | `test_sync_scales_linearly` — the min-of-5 fix was not enough | It failed again at load average 7.4 on 8 cores. The residual failure mode is not sample noise but load DRIFT: the 50-record and 200-record measurements ran as two separate phases, so any rise in load between them landed entirely on the second and inflated the ratio. They are now INTERLEAVED (alternating, min-of-7), which puts both sizes under the same conditions; the minimum then discards whatever preemption each suffered. Verified 0/6 failures under four concurrent pytest processes at load average 10.5, where the previous version failed. |
| 2026-09-23 | Ordinary questions were refused whenever no source was pinned | `"top 5 debit transaction"` answered "Could you clarify what you're asking about?" with no source pin, and answered correctly pinned to source 2 — same question, same engine. The chain: no pin → `resolve_query_scope` returns EVERY ready source (5) → `_maybe_federated` fires on `len(sids) >= 2` → `run_federated` reaches the operation classifier, which `return`ed its dispatch unconditionally, so the legacy chain below was unreachable. Every op in `FEDERATED_OPS` is also in `_RELATIONSHIP_REQUIRED`, so without a validated cross_source_fk edge a single-source question is structurally guaranteed `UNSUPPORTED` → refusal. `UNSUPPORTED` now falls through to that chain instead of refusing; a CHOSEN operation is still dispatched and still refuses on planner failure, so the closed-set guarantee (no free-form SQL for a REAL cross-source operation) is untouched. Live: trace `status: refused` after one `operation_classify` call → `status: answered` after `operation_classify` → `semi_join_classify` → `federated_struct_plan` → `federated_plan` → `nl_answer`. 16/16 operation-classifier tests, federated labelling 5/5, RBAC 8/8, semi-join 14/14. Two corrections recorded on the way: the "relation does not exist" I first blamed was an artifact of my own context-less CLI probe (`get_db_config` falls back to `VEDA_SOURCE_*` = the platform DB `veda`, not homzhub), and the catalog does contain the table it was said to be missing. The multi-source answer text differs from the single-source one and is not verified correct — that is the pre-existing Phase F quality problem this unblocks, not something it introduced. |
| 2026-09-23 | Verified the unpinned federated answer — it is silent-wrong | Ground truth read from the real source (`homzhub_prod`, schema `homzhub`) through the engine's own connection. The unpinned answer ("top 5 debit transactions total 1,200 … Squash highest at 500") came from `SELECT a.name, SUM(ac.monthly_fee) AS total_debit FROM homzhub.demo.assets_amenity a JOIN catalog_parquet.amenities_catalog ac ON a.name = ac.amenity_name GROUP BY a.name ORDER BY total_debit DESC LIMIT 5` — amenities and their monthly fees, joined to a parquet source, with `SUM(monthly_fee)` merely ALIASED `total_debit`; no transaction table is touched at all. Actual top-5 debit amounts are 2,500,000 / 650,000 / 500,000 / 400,000 / 345,000 (`accounts_generalledger`, 476 DEBIT rows) or 64,900 / 64,900 / 50,000 / 50,000 / 47,300 (`accounts_paymenttransaction`, 297 DEBIT rows) — neither totals 1,200. The PINNED answer is wrong too, less absurdly: right table and right filter, but `LIMIT 10` with NO ORDER BY and no amount column projected, so its invoice IDs 102/139/145/128/155 are literally the first five unordered rows (reproduced exactly by running its own SQL). So the fall-through fix removed a wrong refusal and exposed, not created, the pre-existing federated quality problem; on this query it converts a refusal into a confident wrong answer, which is the worse failure by the project's own refuse-over-guess priority. Recorded, not acted on — the next decision (gate the federated fall-through on answer support, or restrict the default scope) is the user's. |
| 2026-09-23 | "top N" returned an arbitrary N — three producers, three fixes | Measured, not inferred: `"top 5 debit transaction"` and `"top 5 general ledger entries"` both came back as unordered rows the summariser then called the top five. Three independent causes. (1) `veda/generation.py` built its ORDER BY instruction for the SLM **only** when `rank.basis == "temporal"` — so "latest 10 X" got one and "top 5 X", identical in shape and need, got nothing; the same file's own comment already said that without it "the model has no reason to add an ORDER BY". It now emits the same line for a metric ranking, using the sort column the caller already resolved through `_rank_sort_column` (None when the anchor names no single unambiguous measure — nothing is guessed). (2) `veda/ir_equivalence.py` Rule 4 kept a SECOND, narrower ranking vocabulary that omitted bottom/biggest/greatest/maximum/minimum/fewest/latest/newest/oldest/earliest, so `"bottom 3 general ledger entries"` had its CORRECT `ORDER BY amount ASC` refused as "not requested" while "top 3" passed; it now calls `parse_ranking`, which is the module written to end exactly this duplication. (3) New `ranked_shape_ok` in `veda/validation.py`, a sibling of `grouped_shape_ok`/`distinct_shape_ok`: an explicit count of ranked rows answered by an unordered projection is refused with a question naming the anchor's real measure columns. It runs on every produced SQL and also demotes a verified-cache hit — the cache was separately replaying an entry with `LIMIT 10` and no ORDER BY at similarity 0.97, which the existing evidence and qualifier guards both pass. Gated on an EXPLICIT count so "first name" / "highest amount" / "most common" are untouched. Live, against ground truth: "top 5 general ledger entries" → `ORDER BY "amount" DESC LIMIT 5`, top row `hello` at 20,000,000 (matches); "bottom 3" → `ORDER BY "amount" ASC LIMIT 3`, `Services: Assisted Leasing` at 0.00 (matches) and now answers at all; "latest 10" unchanged; "top 5 debit transaction" asks "rank by expected_amount or paid_amount?" because that anchor genuinely names two. 11 new tests + 219 in the surrounding files. One thing this does NOT fix, seen on the bottom-3 answer: the summariser said two entries were "half of the total" when 1+1 of a 2.00 total is all of it — a proportion claim, not a ranking one. |
| 2026-09-23 | Removed the word list I had just left behind in Rule 4 | Replacing ir_equivalence Rule 4's 13-word ranking list with `parse_ranking` still left three words written by hand in that file — `sorted?|rank|order` — so the rule kept a vocabulary of its own, which is the thing the replacement was supposed to end. The explicit sort verbs now live in `query/ranking_parser.py` beside the ranking words as `_SORT_VERBS`, exposed as a new `RankingSpec.sort_requested` (True for any ranking word, or for a bare sort verb like "sorted by price" that names no end and no count; `ranked` stays the narrower ranking-only signal). Rule 4 is now one line and names no words at all. This centralizes the list rather than eliminating it — they are English SQL-semantics words and cannot be derived from data — but there is one copy instead of two, which is the property ranking_parser exists to hold. 3 new parser tests (22 total); traceability 151, generation 17, semantic validation 16, ranked-shape 11 all pass; live re-check unchanged — "bottom 3" `ORDER BY amount ASC LIMIT 3`, "top 5" `ORDER BY amount DESC LIMIT 5`, both matching ground truth. |
| 2026-09-23 | Explicit source mention in the query — routing-SLM prompt now carries source names (flag OFF) | `ROUTING_SLM_SOURCE_NAMES_ENABLED` (default 0). `name` already reaches the coordinator in the api tier's profile map but `build_candidates` dropped it, so the SLM only ever saw `source_id=2` and could not match a source the QUESTION names. Pass-through restored: +`name` on `CandidateSource`, set in `build_candidates`, emitted in `_build_user_message` only when the flag is on AND a profile supplied one (flag-off prompt verified byte-identical). CORRECTION to the first measurement in this session: the "naming a source breaks routing (2/4 → NO_EVIDENCE)" finding was a probe artifact — that probe never passed `profile_provider`, so the SLM saw empty type/domains/description; with the profiles production actually sends, named sources do NOT break today. Re-measured on 4 CONTESTED pairs (named source ≠ evidence winner): flag makes the SLM honour the name (catalog_parquet/projects 2→5) but on a named source that cannot answer it degrades to bare `NONE`/`NO_EVIDENCE` (docs_contracts/vendor-rating 4→NONE, homzhub/amenities 5→NONE) instead of the generic answer it used to give. Also confirmed the name is irrelevant whenever routing resolves deterministically (`SINGLE_CANDIDATE` never consults the SLM). Verdict: keep OFF until paired with a clarify-instead-of-NONE branch. |
| 2026-09-23 | Two holes in the ranking guard I had just shipped | A pasted live trace for "top 5 general ledger entries" (chat 313, asked twice, same SQL both times) showed it answered from `catalog_parquet.amenities_catalog` with `COUNT(DISTINCT amenity_id) … GROUP BY amenity_name` — no ORDER BY, no LIMIT, SEVEN rows, every count 1 — and the guard let it through. Two reasons, both mine. (1) The carve-out tested `exp.AggFunc`, meaning ANY aggregate waved the statement past; a GROUPED aggregate returns many rows and ranks nothing. It now passes only a SCALAR aggregate (no GROUP BY → one row, a superlative answered directly), so `SELECT MAX(amount)` still passes and `SELECT name, COUNT(*) … GROUP BY name` no longer does. (2) The guard existed only in `veda/pipeline.py`, so the cross-source path — which never enters `run_query` — was exempt; it is now applied in `veda_hybrid._maybe_federated` where the payload's SQL is assembled, refusing through the existing `federated_refused` shape. Also stopped the reason text asserting a row count it cannot know (it said "returns 5 rows" for a statement that returned seven). Live: the federated path now logs `[federated] refused: you asked for the top 5 by value …` instead of shipping the amenity rows, while the same question pinned to source 2 still answers `ORDER BY "amount" DESC LIMIT 5` with `hello` at 20,000,000 and "bottom 3" still answers `ORDER BY "amount" ASC LIMIT 3` — the narrowing broke neither. 3 new tests (14 in the file); ranking parser 22, traceability 151, semantic validation 16, grouped/distinct guards 12, generation 17 all pass. NOT fixed and separate: the user-facing text for a federated refusal is the generic "Could you clarify what you're asking about?", which discards the specific reason (pre-existing for every federated refusal, including the operation classifier's); and the routing bug that sends "general ledger" to an amenities parquet in the first place. |
| 2026-09-23 | Two config defaults corrected so the server matches this machine (.env audit) | Audited every env var the runtime reads (201) against `.env` (67 keys): 82 of them are `config.py` flags, only 10 were in `.env`, and the other 72 correctly ride their in-git defaults — so flags do NOT belong in `.env`, and the two that were there were the only real divergences. (1) `CAPABILITY_FILTERING_ENABLED` default 0→1 and the `.env` line removed: it was on everywhere here but ONLY via gitignored `.env`, so a server pull would have silently run with it off; its A/B (187 queries, 0 regressions) already says ON is the measured state. Staged `veda_core/query/capability_filter.py` + its 2 test files — with the default on and the module untracked, `plan_route`'s import would fail on a fresh checkout. (2) `SLM_TEMPERATURE` was a bare literal `0.3`, NOT env-readable at all — so the `SLM_TEMPERATURE=0` line CLAUDE.md documents as required was inert. Now `float(environ.get(...) or "0")`, default 0. Affects only the 3 call sites that pass it explicitly (`ir_emit` — which shapes the SQL and is the cross-session-instability suspect — plus `rag_synthesis` and `lg_node`); routing/classifier/semi-join/federated always went through `call_slm`'s own 0.0 default. Verified unset→0.0/True, override→0.3/False, and empty-string→0.0 (the `or "0"` guard: a bare `SLM_TEMPERATURE=` line crashed config import outright before it). 21 capability tests pass, routing_slm 8/8, routing_policy 10/10. |
| 2026-09-23 | The misrouting fix was a flag whose code was missing | "top 5 general ledger entries" answered from `catalog_parquet.amenities_catalog` because federation is decided by `should_federate(cols)` — "did the retrieved columns come from >=2 sources" — a PRESENCE test with no score floor anywhere before it. `config.py` already documents the relevance gate that fixes exactly this (`FEDERATION_SOURCE_QUALIFICATION_ENABLED`, default ON since 2026-09-06, measured 182/182 federated → 12/182), and `tests/test_federation_source_qualification.py` already specifies it — but NO production code read the flag, and the test had been erroring all along on `module 'query.cross_source_composer' has no attribute 'qualified_source_ids'`. The implementation was simply absent; the same shape as the untracked `capability_filter.py` noted earlier. Implemented `qualified_source_ids` / `qualify_columns` in `cross_source_composer.py` to that existing spec — qualifying on the ROUTING layer's clean per-source cosine (`source_coordinator._default_evidence_provider`, never `select_retrieval`'s reranked score, which collapses and would drop the CORRECT source) with the existing `ROUTING_COMPETE_WINDOW` margin, no new threshold and no new flag — and called it in `run_federated` just before `should_federate`. Live, unpinned over all five sources and with the verified cache bypassed: "top 5 general ledger entries" now takes the single-source path and returns `accounts_generalledger ORDER BY "amount" DESC LIMIT 5` — 20,000,000 / 11,234,567 / 5,435,234 / 4,534,343 / 4,444,444, matching ground truth exactly; "top 5 debit transaction" routes to source 2 and asks which measure instead of answering from amenities; and the genuine cross-source "Which assets have maintenance tickets?" STILL federates, answering through a real semi-join between `invoices_csv.maintenance` and `homzhub.assets_asset`. Its own spec test 8/8 (first time it has run at all); federated labelling 5/5, RBAC 8/8, semi-join 14/14, operation classifier 16/16, ranked-shape 14/14, traceability 151. `test_source_coordinator` stays 25/26 — the one failure is the pre-existing capability-filtering test that touches neither the composer nor federated_route. Note for later: the routing coordinator had resolved `SINGLE sources=['2']` correctly on every one of these, but `MULTISOURCE_ROUTING_SHADOW` discards its decision; this gate reaches the same outcome through the evidence instead. |
| 2026-09-23 | Decouple Thinking/Explainability from the answer path | Target invariant: no explainability, thinking, narration, timing, audit or optional SLM operation may delay, reorder, replace, prevent or break a terminal answer. Four coupling points found and closed. (1) `refusal_polish` was a SYNCHRONOUS SLM call on the terminal path, hardcoded `FEEDBACK_LLM_POLISH = True` under a comment claiming "(default OFF)", measured on 1,039 of 6,645 traced turns (15.6%) at a median 2,634 ms and a maximum 16,028 ms — now env-driven and default OFF, and `_polish` is bounded by a WALL-CLOCK deadline on an abandoned daemon thread, because `urlopen`'s timeout is applied after `getaddrinfo` so DNS sat outside the nominal 6s. (2) The api-tier step-model block in `_build_reply_events` was UNGUARDED and ran BEFORE the answer was yielded, so a progress-bar failure propagated out of the generator and the view turned it into an SSE `error`, destroying an answer the engine had already produced — now one guard at the architectural seam; thinking degrades, the turn continues. (3) Post-answer `explainability`/`insights` emissions were unguarded: they could not cost the answer but did cost persistence and the `completed` frame. (4) `turn.metadata()` was unguarded on BOTH the JSON and streaming paths. 20 new tests across two files (split because `veda_core`'s `config` module cannot coexist with the Django `config` package in one process). Verified live: refusal_polish fires on zero turns; 0 invariant violations across 5 paths; sweep 507 passed. |
| 2026-09-23 | Coverage run: 60 of test.py's questions through the LOCAL main pipeline | One fresh chat per question, so a failure is the engine's and not a leaked frame. 27/60 answered with SQL, 33 refused, 0 HTTP errors. Refusals group into families rather than scattering: 12 are `qualifier_completeness` failing to map an ordinary ENGLISH word — 'database', 'exist', 'average', 'property', 'statu', 'non', 'exceed', 'currency' — which is the single biggest lever on answer rate and is about the gate's word handling, not about the data; 6 "couldn't work out a reliable total" (so "How many properties are there?" and "How many projects do we have?" both refuse); 4 "couldn't apply the condition"; 4 the ranking guard added today; 2 the generic federated clarify; 2 honest ambiguity ("which amount should be ranked — max_rating, rating?"). Checked before reporting any of it as pre-existing: an in-process A/B over 7 of the refused questions, toggling ONLY today's two additions, found 4 identical either way and 3 that answer WITH today's changes and refuse without ("What is the average carpet area sqft of properties?", "How many ledger transactions exist?", "show properties with their owners") — so today's work did not cause this refusal rate and on this sample slightly improved it. The `shapes` category is where the engine is strongest (20/30); `ledger` is weakest (1/10). Working list and raw rows saved for the drill-down/memory pass. |
| 2026-09-23 | Drill-down + memory measured on the 27 questions that actually work | Three passes over the working list, each scenario in its own chat, every check made against the SQL the engine produced rather than the prose. MEMORY IS LARGELY HEALTHY and the failures are engine-side — the same split found earlier in this session. Filter application: of the 16 follow-ups that produced SQL, 16/16 ADDED a real new filter (`WHERE LOWER(CAST("city_name" AS TEXT)) = %s`) and 13/16 kept the base entity; the resolved query read `only the ones in Pune (for Assets (assets_asset))`, so the frame anchored correctly. Reset: 25/27 chats forgot the filter cleanly on "start over"; the other 2 simply did not answer the new topic. Recall: 27/27 answered "what did I just ask?" from memory without re-running a query, and 25/27 kept the same entity for "show it as a chart". The real defects are three: (1) SHAPE LOSS — 0 of 8 aggregated/grouped bases survived a drill-down; "distribution of properties by furnishing" (GROUP BY + COUNT) became a raw 1000-row projection once filtered, which is how a distribution question silently turns into a row dump; (2) DOCUMENT LEAKAGE — 5 of 27 follow-ups were answered by the RAG side ("The provided context does not contain any information related to assets in Pune. Sources: (site_notes.md)") instead of the relational base the frame named; (3) 6 of 27 refused outright. Two further defects noticed while diagnosing: the resolved query LEAKS a table name ("(for Assets (assets_asset))") which this session's own contract says it must not, and the summariser fabricated "5737 assets are listed with Valle Vista in Pune" when Pune holds 1,642 rows in `assets_asset` — a figure larger than the population it describes. Correction to my own method, recorded because it changed the answer: the first scoring pass reported "filter applied 0/27" by looking for the literal "pune" in the SQL; values are PARAMETERISED, so the literal can never appear there. Re-scored structurally (does the follow-up add a WHERE on a column the base did not filter), it is 16/16. The measurement was wrong, not the engine. |
| 2026-09-23 | Duplicate detail row (self-inflicted 2026-09-21) + contract checker now runs | (1) `set_details` deduped on the WHOLE entry, and once `duration_ms` became part of a row, a phase that reports twice — `validation` emits `completed` then `warning` — produced the same row twice with different durations: the reader saw "Checking the query" twice. Identity is now `(type, label)`; the later report replaces the earlier one, because it is the more complete measurement of the same work. Verified 0 duplicates across every frame of a zero-row turn. (2) The whole-turn contract checker was run by nobody — there is no CI in this repo at all (no workflows, Makefile, tox or pytest config), so it only executed when someone manually captured a stream. It is now driven from `tests/test_explainability_contract.py`: its own self-test (which fails if the checker stops catching its planted defects), plus six REAL captured streams committed under `tests/fixtures/sse/` with ids normalised — one per answer shape. No docker, DB or network needed. A negative test proves the fixture check can actually fail. 12 new tests. |
| 2026-09-23 | Two dead test files revived — Tier-2 thinking and the NoSQL build event | `test_tier2_thinking.py` raised ImportError at COLLECTION, so the entire Tier-2 per-node emission surface was unverified. Cause: both `sys.path.insert` calls used index 0, so the repo root ended up FIRST and `config` resolved to the Django `config/` PACKAGE rather than `veda_core/config.py` — and `query.lg_nodes` needs the latter's `SLM_OLLAMA_BASE_URL`. veda_core now goes first. `test_rag_layer_events.py::test_run_nosql_emits_build_event` was failing on a stale fixture, not a product defect: it patched `connectors.base.build_connector`, but `veda_hybrid` binds that name at import time, so the REAL factory ran and a source dict with no `id` raised KeyError inside the per-source try — swallowed, so the event never fired. It also used a non-numeric source id, which `int(sid)` rejects. Patched at the bound name, with a source shaped like a real one. 11 tests recovered. |
| 2026-09-23 | Scope A/B falsified my own hypothesis: the always-on federated fallback is NOT Phase F's culprit | 62 homzhub questions (53% of the 117 set, all 9 categories), each run twice under `MULTISOURCE_ROUTING_SHADOW=1` — once `source_ids=(2,)` (which short-circuits `_maybe_federated` at its `len<2` guard) and once `(2,3,4,5)` (where the old `should_federate(cols)` gate can fire). I had argued that because the coordinator is observe-only, cross-source execution still reaches queries through `veda_hybrid._maybe_federated` and that this was likely behind Phase F's 121→80. **Wrong.** The gate fired 1/62 (1.6%) against Phase F's 55% federated-routed, and latency moved 13.4→14.8s (+10%) against Phase F's 11→59s (5x) — the two cannot be the same effect, so Phase F's harm is attributable to the coordinator being AUTHORITATIVE (SHADOW=0), not to the fallback. Keeping SHADOW=1 is right; the fallback is practically harmless. status/route/SQL were byte-identical on 61/62. The single federated case is nonetheless a real silent-wrong worth noting: "list all tenants" refuses honestly when pinned (`qualifier_dropped`) but under 4-source scope produces `SELECT t0."name" AS tenant, COUNT(DISTINCT t0."name")…` and narrates the artifact ("32 tenants, each having exactly one entry") — the Phase G filter-drop→bare-GROUP-BY→summariser shape. Low priority at 1/62. Caveat: `multitable` only 5/18 covered (0 fired). Two method bugs fixed en route: results were read off `MultiResult` directly (it carries `.items[0]`, so every status logged as "?"), and the stride sample `QS[::step]` silently dropped the entire `multitable` category — now stratified round-robin. Script made crash-safe/resumable after the inference container was host-OOM-killed twice mid-run. |
| 2026-09-23 | Audit: the conversation→engine boundary (no code changed) | Traced `"only the debit ones"` end to end and pinned where `financial` enters: `chatbot/memory/frame.py:455`, where `_describe_frame` composes `f"{display} ({raw_entity})"` from `entity_display` — the engine's own business label for `accounts_generalledger`, "Single Financial Transactions" — which `frame.py:1304` then glues onto the user's message as `"{message} (for {ctx})"`. `call_engine_node` (`nodes.py:1985`) sends ONE string, so by the time `qualifier_completeness` (`validation.py:404`) applies its stated contract — "every CONTENT token THE USER NAMED must appear in the SQL" — it has no way to tell the user's words from this layer's. Verified live that `financial` is not among the gate's 271 stopwords and substring-matches the real column `financial_year_id`, while the user's actual word `debit` is answerable (476 DEBIT rows). The headline finding is that VEDA does NOT need a structured conversation state: `QueryFrame` already carries entity/filters/group_by/measures/order_by/limit/source_id/drill_path, `DELTA_TYPES` already spans refine/replace/remove/drill/compare/ambiguous, `apply_context_delta` already mutates in Python with two structural guards, and ambiguity already fails closed. The state is structured right up to the last inch and is then flattened to English because the boundary accepts nothing else — `HybridRequest.flags` exists and is silently dropped (`hybrid.py:236`), and `run_query(anchor_hint=…)` already accepts a structured anchor override but is unreachable over HTTP. Proof it is a boundary problem and not a wording one: the file's own comments record trying all three wordings — including the shape words (engine spent 54s asking whether "measuring" was a column) and dropping the label (a bare "go back" re-resolved to `reminders_reminderpaymenttransaction`) — so no phrasing satisfies all constraints. Same root cause explains the separately-measured shape loss (0/8). Minimum fix is 10 tasks, of which #1 (give the gate the user's original words) plus #7 (a contract test) fix the reported bug on their own; #2-6 remove the boundary problem and the shape loss with it. Written up in `VEDA_CONVERSATION_BOUNDARY_AUDIT.md`. |
| 2026-09-23 | Trace log rotation — was unbounded, measured at 35 MB / 7,096 records | `EXPLAIN_TRACE_PERSIST`'s JSONL writer (`veda/explain.py::_persist`) appended forever with no cap in the write path at all — every verbose trace (the full per-turn dict, not just the compact summary) added to a file that never shrank. Rotation added at the house convention already used by `utils/logger.py` (RotatingFileHandler: maxBytes=10MB, backupCount=3), hand-rolled here because this is a raw `open(path, "a")`, not a logging handler — checked and rotated (`path` → `.1` → `.2` ...) immediately before each write, best-effort so a rotation failure costs rotation, never the append. Env-overridable (`EXPLAIN_TRACE_MAX_BYTES`, `EXPLAIN_TRACE_BACKUPS`). The existing 35 MB file was archived (gzip, 3.6 MB) and truncated. Verified: 30 writes against a 2 KB cap / 2 backups stayed bounded at 3 files, each ≤ cap. 4 new tests. |
| 2026-09-23 | Role membership of a deactivated account is now refused, loudly | `users/roles/assign` AND `users/roles/revoke` reject a target whose `is_active` is false, with new `UserInactive` (`USER_INACTIVE`) → **400** and the message "That account is deactivated; its roles cannot be changed. Reactivate the account first." 400 not 409 because the record exists and nothing collides — it descends from `AccessManagementError` directly and picks up the documented fallback status, so no concrete class had to be registered in `views/base.py::ERROR_STATUS`. This REVERSES a documented deliberate choice (assign's old docstring: pre-provisioning access before an account is enabled) — kept the reasoning in `UserInactive`'s docstring rather than deleting it, since the old rule meant an admin editing a deactivated account got a 2xx and a changed grant list for an edit that grants nothing. Revoke's inactive check runs only when the user EXISTS, so a revoke naming an unknown id keeps its no-op success and cleanup scripts don't start 400ing; it cannot collide with `LastAdminRoleProtected` either (the last ACTIVE admin is by definition active). Found and fixed a pre-existing bug while wiring it: `UserRoleRevokeView.post` had no `try/except AccessManagementError` at all, so refusing to strip the last admin's role answered **500 instead of 409** — invisible because that guard is only covered at the service layer (`test_admin_bootstrap.py` calls `revoke()` directly, never over HTTP). Replaced `test_assign_allows_an_inactive_user` with 4 tests (assign refused + edge absent, revoke refused + edge SURVIVES, unknown-id revoke still 200 no-op, active user unaffected). 47/47 test_grants, 38/38 test_admin_bootstrap, 32/32 test_rbac_filter. Two failures in test_data_scope/test_query_scope_rbac (staff-bypass) verified pre-existing by restoring HEAD and re-running — unrelated. |
| 2026-09-23 | `granted_on` listed resources the user is actually denied — resolver's `resources_for()` was a 4th, drifted copy of the allow rule | Reported live: denied sources still appeared in the role grid. `EffectivePermissions.resources_for()` (surfaced as `granted_on` by `GET users/permissions/effective`) read the raw ALLOW rows instead of asking `allows()`, so three classes of row leaked: a source also carrying a DENY from another role (deny-wins), a child ALLOW under a denied ancestor (DENY is unpierceable at any depth), and a table ALLOW with no source-level ALLOW above it (STRICT HIERARCHY — grants nothing). The module docstring names `permitted_source_ids` and the catalog tree's `_resolve_effect` as the mirrors that "MUST change with" `allows()`; `resources_for` was silently a fourth implementation and had drifted from all of them. Fixed by delegating: candidates are still the granted ALLOW paths, but each is confirmed through `allows()` — so it cannot drift again. Blank/global paths unaffected (`allows()` short-circuits non-resource-scoped codes); costs no extra queries (O(depth) over the built index). `apps/query/scope.py::permitted_source_ids` deliberately does NOT use this method, so the coarse source gate is untouched. 5 new tests (denied source excluded, child-under-denied-source excluded, orphan table ALLOW excluded, genuinely-allowed source+table both kept, global grant kept); one of them initially hit the `(role, permission, resource_path)` unique constraint by granting ALLOW+DENY on one role — corrected to two roles, which is how a deny actually arrives. 41/41 test_permission_resolver, 86/86 test_catalog_resources, 47/47 test_grants, 38/38 test_admin_bootstrap, 32/32 test_rbac_filter. Failures in test_permission_management (1), test_query_scope_rbac (1), test_data_scope (1), test_filesystem_catalog_and_scope (7) verified pre-existing by restoring HEAD's resolver and re-running — identical counts. |
| 2026-09-23 | Conversation→engine boundary: user language is now immutable | The user's words reach VEDA Core unchanged and the remembered state travels beside them. `ConversationContext` (new, `chatbot/memory/context.py`) is a VIEW over the existing QueryFrame built AFTER apply_context_delta — no second state, no new memory system — and rides the request's existing `flags`, which `inference/routes/hybrid.py` already accepted and silently dropped. It is validated at that boundary (unknown keys dropped, types coerced, lists capped, and `entity_display` refused outright since the display label is what caused this). The engine binds it ambiently, the same ContextVar pattern tenant/source already use because the public signatures are frozen, and consumes it in three narrow places: `entity_table` → the EXISTING `anchor_hint`, `user_message` → the qualifier gate's basis, and a lane check in `classify`. `qualifier_completeness` gained an optional `user_message` so its own stated contract — "every content token THE USER NAMED" — is literally true; the gate itself is unweakened. Live, all ten required scenarios: `"only the debit ones"` now answers from `accounts_generalledger WHERE entry_type` instead of refusing on `financial`; drill-up replays the user's own earlier question and restores the GROUP BY distribution; topic switch inherits nothing; ambiguous still refuses honestly. `resolved_query` was byte-identical to the user's message on every turn except the two documented navigation deltas, which replay the user's OWN recorded earlier question rather than send "go back" as data. 21 new tests (13 boundary + 8 contract), plus context/document/RBAC/reset/traceability suites unchanged (47/120/12/8/151). TWO REGRESSIONS I CAUSED AND CAUGHT, both from the first live run and both fixed before reporting: (1) with the entity no longer in the text, the ROUTER saw a bare fragment and sent EVERY relational follow-up to RAG ("The provided context does not contain… Sources: Samta-Employee Handbook") — fixed by continuing the conversation's own lane in `classify` when the frame names a table and its route was a SQL head; (2) the verified cache is keyed on query TEXT, and a bare fragment is the same three words whatever it follows — one entry saved under "properties where is gated is true" was replayed for sale listings, lease listings and a facing distribution, all returning the same 1000 asset rows. Context-dependent turns are now neither saved to nor looked up in that cache, and the rows my testing created were deleted. KNOWN GAPS, stated rather than papered over: a filter from an EARLIER turn is not re-applied by a later one — the frame stores the engine's humanised LABEL as a filter's field, never the real column, so it can only travel as a bare value, and carrying "true" forward from `is_gated` was measured re-grounding onto `all_day_access`; filtering on a column the user never named is silent-wrong, so it is dropped rather than guessed, and the real fix is for the frame to harvest the column out of `last_sql`. Also unchanged: aggregated shape still does not survive a drill-down (0/8, pre-existing), and `render_frame_as_query` remains on the execution path for DOCUMENT frames and COMPARISON turns only, with its docstring rewritten to say so. |
| 2026-09-23 | REVERTED the `resources_for()` / `granted_on` change (see the row above) | User's call after we traced the field's actual reach: `granted_on` is produced at exactly one place (`views/resolver.py:63`) and consumed by NO code in this repo — only a test assertion, three markdown docs and the Postman collection; there is no frontend here. It also only exists when the caller passes `permission_code`, which the documented call shape (`?user_id=…`) does not, so the reported role-grid symptom cannot have come from it. `apps/access_management/services/resolver.py` and `tests/test_permission_resolver.py` restored to HEAD (36 passed, back to the pre-change count). The DEACTIVATED-USER work from earlier today is NOT part of this revert and stays in place. The underlying inconsistency is real and still open: `resources_for` remains a fourth implementation of the allow rule that disagrees with `allows()` on a denied source, a child under a denied ancestor, and an orphan table ALLOW — worth fixing whenever a client actually reads that field. The role grid's real source is still unidentified; candidates that all still carry DENY rows are `users/permissions/effective`'s `permissions[]` and `permission_codes[]`, `roles/permissions/list`'s `grants[]`, and the `permission_codes[]` embedded in the `auth/login` response (`apps/authentication/services.py:392`). |
| 2026-09-23 | Closed the gaps my own audit found in the conversation boundary | The audit scored filters/group_by/measures/order_by/limit as TRANSMITTED BUT NOT CONSUMED, and source_id as never read. All are now consumed, and the root cause of every one of them turned out to be the same shape the `financial` bug had: explain handed conversation memory a humanised LABEL where SQL needs a COLUMN. `business_explain.py` now carries the raw `column` on each applied filter and on each group operation alongside the label it already emitted; the frame harvests both, and only the column travels. A filter that knows no column — every frame written before this, and they live 7 days — is DROPPED rather than re-grounded from its bare value, because that guess is what put an `is_gated` filter onto `all_day_access`. Two further bugs fell out while wiring it: the frame writes orderings as `{"field": …}` while `ConversationContext` read `"column"`, so `order_by` was silently empty for every real frame; and replaying a remembered shape assumed SUM, turning a `COUNT(DISTINCT id)` distribution into `SUM("id")` — a summed primary key that the summariser reported as a real figure ("id_total above the average of 291,689"), so the frame now remembers WHICH aggregate ran and replays that. `source_id` became a fail-closed check rather than an ignored field: a remembered source outside THIS turn's RBAC-resolved scope drops the whole context instead of applying part of it. Engine-level, all four verified: shape kept (`SELECT facing, corner_property, COUNT(*) … WHERE city_name = %s GROUP BY facing, corner_property`), filter accumulated (`city_name AND furnishing`), order_by+limit carried, and a context from an out-of-scope source dropped. End-to-end through the API the 0/8 shape-loss gap is closed on the case that defined it — "distribution of properties by facing" then "only the ones in Pune" now returns a distribution scoped to Pune instead of 1000 raw rows. The user's current words still win over any remembered fact: a filter on a column THIS turn already filtered is never re-applied ("what about Mumbai" yields one city predicate, not Pune AND Mumbai), and a grouping the user names this turn overrides the remembered one. 5 new tests (18 in the boundary file); 523 tests across 12 related files pass. Still open and unchanged: `render_frame_as_query` remains on the execution path for DOCUMENT frames and COMPARISON turns, which have no structured channel, and previous-result references are still honestly unsupported. |
| 2026-09-23 | Re-ran the full drill-down + memory suite after the boundary work | Same 27 questions, same three scenarios, same harness, scored structurally. Document leakage 5 → 2; reset 25 clean → 27 clean; recall effectively unchanged (the two deltas both trace to one flaky federated query whose base turn produced no SQL that run). Shape 0/8 → 2/5: both survivors are single-table GROUP BY distributions, which is exactly what the reshape branch covers, and all three that still lose it have bases the branch never sees — one CTE aggregate and two federated `invoices_csv` JOIN `homzhub` queries. The headline number moves the WRONG way and needs saying plainly: follow-ups that answered went 16 → 10 and refusals 6 → 15. Examined all 9 that flipped, and every one is a base table with NO city column — payment transactions, sale listings, amenity categories, payment types — being asked "only the ones in Pune". What the old behaviour did there was invent a join: two dropped the base entity entirely ("List all amenity categories" answered from `assets_asset`, "List all payment types" from four tables including `generics_country`), and the seven that kept it joined to `accounts_generalledger`, which has no city column either — so none of those answers could have honoured "Pune". Silent-wrong traded for honest refusal, which is the direction this project has repeatedly chosen. CAVEAT ON MY OWN MEASUREMENT: the harness sends the same "only the ones in Pune" to every base regardless of whether that table can be filtered by city, so it over-represents the unanswerable case and the refusal count is partly a property of the test design, not of the system. A fairer re-run would use a per-table filter the data can actually support; until that exists, the 16→10 figure should not be read as a 6-answer loss. Raw rows saved as `evaluation/testpy_run/phase2_results_after_boundary.jsonl` beside the baseline. |
| 2026-09-23 | Role grid: `connected_sources` counted DENY rows as reach | The actual reported bug (the earlier `granted_on` fix was the wrong surface and has been reverted). `roles/list` shows each role's `connected_sources`, built by `services/grants.py::role_stats()` — which read EVERY `RolePermission` row for the role and never looked at `effect`, so a source the role explicitly DENIED still appeared as something it is connected to, and a deny-only role looked connected to everything it restricts. One-line fix: `.filter(..., effect=Effect.ALLOW)` on the paths query. Deliberately filtered per ROW, not per kind (a role allowing `db:crm` and denying `db:hr` is still connected to "Database" — the ALLOW row stands on its own), and deliberately NOT the resolver's full `allows()` rule: strict hierarchy is a USER-level question resolved across the union of a user's roles, so a role holding only `db:crm:employee` contributes real reach once another role allows `db:crm`; applying the source-gate rule per-role would under-report. 3 new tests (denied source excluded, deny-only role empty, a kind allowed elsewhere survives a deny) — the first two reproduced the bug before the fix. 119/119 test_role_management, 47/47 test_grants, 38/38 test_admin_bootstrap, 36/36 test_permission_resolver, 86/86 test_catalog_resources. Failures in test_gate (1, routed endpoints missing `required_permission`) and test_permission_management (1) verified pre-existing by restoring HEAD for every file I touched and re-running — identical counts. |
| 2026-09-23 | Scalar aggregates now survive a drill-down, and the working list is measured | The fair re-run left one clean failure: a SCALAR aggregate — "what is the total expected monthly rent", "how many sale listings are there" — has no group_by, so the reshape branch (which keyed on group_by) never fired and filtering the question returned a row list instead of a total. 4 of 4 failed that way. The branch now also fires with no group_by when the frame remembers an aggregation, rebuilding `SUM(col)` / `AVG(col)` / `COUNT(*)` with this turn's filter; the grouped case is unchanged and still wins when a grouping exists. Engine-verified on all four shapes (SUM/COUNT/AVG kept, grouped base still groups). Fair drill-down shape went 3/7 -> 6/7, with `go back` 7/7 and entity retention 12/12. 409 tests pass across 7 files. The remaining shape loss is one ranking base, which is not an aggregate. Also re-measured the four questions the previous run had "refused": every one traced to my own filter chooser picking a 4-word free-text `remarks` value ("Value Added Services Payment") that no user would say and whose interior word `added` the qualifier gate rejects — re-run with the table's real categorical value (`DEBIT`) all four work, one of them (`How many properties allow all day access?`, a CTE base) now also keeping its COUNT. So the earlier 16->10 drop was almost entirely a property of the test, as suspected. Wrote `DRILLDOWN_WORKING_LIST.md`: 17 base questions with a follow-up that is measured to work, the per-row evidence (filter applied / entity kept / shape kept / go back), the 10 questions whose table has no column that narrows 5-60% (not failures — there is no honest follow-up to test), and the four domain-independent follow-ups. |
| 2026-09-23 | Interactive benchmark report over every stored result | `evaluation/build_benchmark_report.py` + `evaluation/benchmark_report_template.html` → `benchmark_report.html` (standalone, no server, ~8.6 MB). Re-runs no benchmark: reads 92 runs / 5,672 records from `evaluation/**`, root `bench*/…_results.json`, `testpy_run`, `routing`, `conversation`, the 2026-07-30 archive. PASS/FAIL only from a field the run wrote or the repo's own scorer imported as-is (`score_before_after._good`, `score_rowlevel.judge`, `adversarial_analyze` outcome buckets); status-only runs are labelled "Answered (not correctness)"; n/a is never a pass. Byte-identical files at two paths collapse to one run (both paths listed — e.g. component suite current == archive). 29 baseline→current pairs taken from run naming only (OFF→ON, arm A→B). Views: catalog of all runs, per-run (metric/baseline/breakdown/result filters, KPIs with deltas, failure reasons, status, factual insights, scope, sortable/paginated records, detail drawer with raw record), Run A vs B compare (matched by query text). 7 headline counts cross-checked against raw files, all equal. Model is not recorded in any result file, so none is shown. |
| 2026-09-23 | One-page client showcase of measured improvements | `evaluation/build_showcase.py` → `veda_improvements.html` (29 KB, static, no JS). Every figure computed from the same data layer as `benchmark_report.html`; only same-question-set pairs, each with denominator and per-question up/down counts. Shown: business-term grounding (FASTPATH_ENTITY_GLOSSARY, 100 q: table 50→71, shape 18→43, answered 57→74, 0 regressions), hard-question suite over 4 iterations (98 q: structural 22.4→45.9%, result shape 4.1→40.8%), search fix (182 q: dense stage 0→86.3% hit@5, final top-5 94.0→95.1%), client questions pipeline v1→v2 (median 63.8→47.5 s), two speed probes (12/16 q). Deliberately left out: pairs with no gain or a loss (dense ON table match −2 pp, understanding layer ±0, bench117 variants identical). A "what these numbers measure" section states they are structural checks, not value-verified, and gives value-level gold match 0/28 and cross-source 6/53 as current focus. |
| 2026-09-23 | Diagnosed the aggregate refusals — and corrected my own framing of them | I had called these a qualifier-gate word-handling problem (12 of 33 refusals). That was wrong, and checking it changed the conclusion: `AGG_VERB_COMPLETENESS_ENABLED` is already ON, so `average` is accounted IFF the SQL actually contains AVG. Traced two representatives. (1) "What is the average carpet area sqft of properties?" — the column reranker is RIGHT (`assets_asset.carpet_area_sqft` at 0.3053, next 0.0417, a 7x gap) but `[L3] Routing` anchors on `assets_carpetareaunit`, a unit LOOKUP table that holds no area column at all, so no AVG can be built and `qualifier_completeness` honestly refuses on 'average'. Tier-2's own envelope had it right (`{'intent':'measure','measure':{'agg':'avg'}}`) — the anchor is what loses it. (2) "How many properties are there?" — anchor is CORRECT (`assets_asset`), the qualifier gate passes, but the SQL carries no COUNT, so `intent_sql_alignment.aggregate_presence_ok` (line 233) refuses. Note the narrow lead: "how many ASSETS are there" answers 7,814 while "how many PROPERTIES are there" does not, same table, different business noun. So BOTH guards are doing exactly their job and the defect is upstream in SQL construction; "fixing" either gate would convert an honest refusal into a silent-wrong number, which is the opposite of what is wanted. The two causes need different work: (2) is tractable — right table, missing COUNT, a noun-grounding gap. (1) is anchor selection, the area Phase D5-D11 audited repeatedly and mostly returned NO-GO or high-overfitting-risk on, so it should not be touched without its own evidence. Nothing implemented for either. |
| 2026-09-23 | Summary page added to `benchmark_report.html` (now the default view) | "What improved, what didn't": 29 before→after comparisons grouped into 8 feature areas (NL→SQL, client questions, multi-source & routing, documents & datalake, retrieval & semantic, conversation & memory, feature-flag probes, RBAC). Each row: main check before→after with denominators, pp change, questions up/down among matched, median time, and both runs' dates with their source (git / archive / file time). Outcome is computed, not asserted: Improved (main up, nothing else down ≥1 pp) 12, Mixed (main and another check moved opposite ways) 4, No change 11, Declined 2. Area chips and outcome KPIs filter; a row opens Compare; "Where each area stands now" lists each area's latest runs. RBAC shows an explicit "no benchmark recorded — unit tests only" note. Date caveat on the page: most files carry a 2026-09-08 file time that is likely a copy date, not a run date. |
| 2026-09-23 | "How many properties are there?" — located, hypothesis falsified, not fixed | Narrowed it precisely and then had the fix disproved by measurement, which is why nothing was changed. "How many ASSETS are there" takes `[FastPath] metric.count` and answers 7,814; "How many PROPERTIES are there" does not take the fast path at all, falls to the general planner, produces SQL with no COUNT, and `intent_sql_alignment.aggregate_presence_ok` honestly refuses. Traced into `query/fast_path.py`: for "properties", `reg.match_concepts()` returns [] (no concept owns that word) but `_single_entity()` STILL resolves `assets_asset` with `default_metric assets_asset_count` through `_grounded_entity_fallback` — so the entity and the metric are both RIGHT and the bail-out is downstream of them, most likely the residual/filler check, since `property` is an unaccounted token that also names a real column (`assets_asset.corner_property`). Found the mechanism the codebase intends for this: `semantic/compile_semantic_layer.py:167` folds entity-level business synonyms (`phrase -> <table>.id`) into a concept's match tokens, and its own comment gives this exact example — `"property"→assets_asset`. The data is missing: of 2,465 domain synonyms, 165 target a `.id`, and every one is an identifier paraphrase ("batch id", "invoice id", "log_id") rather than an entity name; nothing points at `assets_asset.id`. `scripts/enrich_synonyms.py` exists to generate these and its prompt literally uses "property" as the example, so the pass was evidently never run for entities — 304 SLM calls would do it (167 entity tables + 137 measure columns), and the file and runtime semantic models are identical (178 tables, 2465 synonyms), so it is not a stale-artifact problem. BUT: injecting `property` into `assets_asset`'s concept match_tokens in-process did NOT make the fast path fire, so the synonym alone is NOT sufficient and running the 304-call enrichment would not have fixed this on its own — it would have changed concept tokens for all 167 entity tables, i.e. anchoring for every query, for a benefit this test says would not materialise. Stopped there rather than run it. Nothing implemented. |
| 2026-09-23 | Where the latency actually goes — measured, not guessed | A follow-up costs the user ~25s (median 26.4s over 81 measured turns; base questions 21.7s), so context does not make the next turn cheaper — the whole pipeline runs again. Navigation is the exception and is nearly free: `go back` / `start over` median 0.1s, because they short-circuit before the engine. Aggregated 400 traces: median turn 18.9s, SLM 10.7s of it (49%), median 2 SLM calls and up to 14; by count the biggest SLM consumer is `nl_answer`, the summariser, on 287 of 400 turns. Stage means: tier2 8.9s (fires on 130/400, i.e. whenever Tier-1 cannot answer), execution 5.8s, sql_generation 5.7s, join_planning 4.7s, routing 4.4s. Then the finding that explains the variance: `SELECT 1` — a query with no work in it — costs 0.5-8.7s, averaging 2.4s steady state, because `veda/execution.py::execute_sql` opens a FRESH `psycopg2.connect()` per call and closes it in `finally`. The source DB is `homzhub_prod` on DigitalOcean, reached over the internet with TLS, and it has NO pooling — the platform's own databases go through pgbouncer, the customer source does not. `runtime._pg()` does the same. On top of that, DNS for that host is slow and unreliable: eight `getaddrinfo` calls measured 22, 30, 77, 342, 1211, 2121, 2142 and 4002 ms, and a ninth attempt failed outright with "No address associated with hostname". Several connections are opened per turn (value grounding, existence probes, the main query), so a turn can pay that cost repeatedly — which is also the likeliest explanation for the 163s outliers and for the gunicorn worker deaths seen during long runs. Ranked by effort against impact: pooling/reusing the source connection is purely mechanical and changes no behaviour; caching the DNS answer costs nothing; skipping `nl_answer` where a deterministic answer already exists removes a call from most turns; reducing Tier-2 entry means raising Tier-1 coverage, which is the hard one. Nothing implemented. |
| 2026-09-23 | Follow-up PATTERNS measured — the earlier 15/17 was one phrasing, not drill-down | The user noticed every follow-up in my runs was the same shape ("only the X ones"), which is true and is the second time my harness has been the weak link. Ran ten real conversational shapes against two bases that BOTH answered on turn 1. On "Show the latest 10 ledger entries": 7/10 pass — `only the X ones`, `just X`, `show only X`, `what about Y` (replace), `remove that filter`, `go back`, `show it as a chart`. Three fail, and the same three fail on the other base too, so they are consistent defects rather than noise: `filter to X` refuses with "I couldn't map 'filter'" (the word `filter` is read as data); `just the top 5` is refused by the ranking guard I added, which is correct in isolation but wrong as a FOLLOW-UP — re-shaping the previous answer is exactly what the frame's remembered ranking should serve; and `what did I just ask?` refuses with "I couldn't map 'ask'", meaning recall reached the ENGINE instead of short-circuiting. That last one also corrects an earlier measurement of mine: phase 2 scored recall 26/27 by testing only that no SQL ran, and an engine REFUSAL also runs no SQL, so the metric was too weak and recall may have been failing all along. The harder finding is the second base: "What is the distribution of properties by facing?" answered on turn 1 and then 9 of 10 follow-ups failed, most of them answered from DOCUMENTS ("The provided context does not contain…"), including the exact pair (base + "only the Nagpur ones") that PASSED in the fair run an hour earlier with the grouping preserved. Same base, same follow-up, opposite outcome between runs — so drill-down is not merely partial, it is UNSTABLE between runs, and any single-run number (mine included) overstates it. Nothing implemented. |
| 2026-09-24 | The conversation boundary was dead — one missing line — and multi-level drill-down works once it is back | Every drill-down measurement from the previous evening was invalid. `chatbot/state.py` had lost its `conversation_context` field (gone from working tree, index AND HEAD, around the merge), and LangGraph silently DROPS any key a node returns that its TypedDict schema does not name. So `context_resolve_node` still classified the delta correctly and still passed the user's words through untouched — every visible signal looked right — while the context never reached `call_engine_node`. Each follow-up arrived at the engine as a bare fragment ("only the Nagpur ones"), was routed to RAG, came back "The provided context does not contain information specific to Nagpur", and then OVERWROTE the frame with a document entity, poisoning the rest of the conversation. That single line explains the document leakage, the run-to-run contradictions, and why a pair that passed at 10:00 failed at 17:00. Restored with a comment naming the symptom, since the failure points nowhere near its cause. Then rebuilt the candidate chains from the DATA rather than by hand: `pg_stats.most_common_vals` already stores each column's commonest value and its frequency, so ONE query lists every drillable dimension in the schema — homzhub has 60 tables with 2+ dimensions, mostly `_history`; among business tables `assets_asset` has the most depth (furnishing 41%, city_name 26%, facing 17%). An earlier attempt to compute this with a GROUP BY per column was abandoned after it ran for minutes: each query opens a fresh connection to the remote DB (~2.4s), which is the same latency defect measured yesterday. Three chains now verified end to end: `assets_asset` runs TWO levels with the filters ACCUMULATING (`WHERE furnishing = %s AND city_name = %s`) and the GROUP BY intact at every level, plus a clean `go back`; `assets_leaselisting` narrows then REPLACES (`what about SEMI` swaps rather than adds); `accounts_paymenttransaction` does one clean level. Two do not hold and the reasons are specific, not vague: a second narrowing on a RANKED base (`latest 10 ledger entries`) fails, and the tickets base question fails on its own ("couldn't map 'statu'") so nothing downstream can work — though `worklists_ticket` has priority/status and would make a good two-level chain if that were fixed. Written up in `HOMZHUB_DRILLDOWN_CHAINS.md`, with `DRILLDOWN_FAQ.md` for the user-facing rules. |
| 2026-09-24 | A bad follow-up can no longer take the conversation with it | A turn that answers from the WRONG lane still counts as `answered`, so memory wrote it and the frame moved: after "What is the distribution of properties by furnishing?" (frame `assets_asset`, route deterministic), the follow-up "only the Nagpur ones" was answered by the RAG head citing an employee handbook, and the frame became `Samta-Employee Handbook April 2026` — anchoring every LATER turn to a document nobody had asked about. One wrong turn ate the whole conversation. `keep_entity_on_lane_change` (chatbot/memory/frame.py) is the mirror of the `stabilise_document_entity` guard that already existed for the opposite direction: on a FOLLOW-UP whose answering route left the conversation's lane (SQL → document), the ENTITY is held and everything else the turn produced — row count, sql, status — is still recorded, because those are facts about what ran while "this is what the conversation is about now" is the only claim being refused. Deliberately narrow: new topics may move anywhere (that is what makes them new topics), a follow-up answered by the SAME lane is untouched however far the table moves within it, and the document→table direction is left to the existing guard rather than re-decided here. Live: a deliberately nonsense middle turn ("only the ones in Atlantis") no longer moves the frame, and the next real follow-up still finds `assets_asset`. 8 new tests; document anchor 120, context delta 47, reset 8, source scope 14, RBAC 12, boundary 18, contract 8 all pass. What this does NOT do, stated plainly: the bad turn is still a bad turn — "only the gated ones" still fails on boolean grounding. This stops the damage spreading, it does not fix the mistake. |
| 2026-09-24 | A failed question no longer swallows the next one | Reproduced the user's sequence exactly — ask something that works, ask something that fails, ask again — and the third turn reached the engine as `"list 5 ledger with highest amount for only the Nagpur ones"`: the REFUSED question glued to the live one, and it failed. Mechanism: a refusal arms `pending_clarification`; the next turn's deterministic test correctly said "only the Nagpur ones" is not obviously a value answering it, which leaves the slot ARMED for the classifier to judge, and the classifier returned `clarify_reply`; `clarify_reply_node` then concatenated. The file's own comment records this family being fixed once before for SHAPE changes ("by city instead" was being glued the same way) — filter follow-ups were not covered, and `_is_clarification_answer` still returns True for bare values like `Nagpur` and `just DEBIT`, so they would be swallowed deterministically too. The guard uses the model's OWN delta to settle which reading it is: when a frame with an entity exists AND the delta is a continuation (refine/replace/remove/drill_down/drill_up), there is a live ANSWERED query to continue, and continuing it is a coherent reading while gluing it to a FAILED one is not — so the frame wins and the dead slot is dropped. Both conditions required: with no frame there is nothing to continue and the pending request remains the better reading, and new_topic/ambiguous are not evidence of anything. Live: the third turn is now `action=followup`, `resolved_query='only the Nagpur ones'`, answered on `assets_asset` with the WHERE and the GROUP BY intact. A second bug fell out of the same trace and is fixed with it: `conversation_context` was set only by `context_resolve_node`, so a route that skips it (clarify_reply, runtime_context) left the PREVIOUS turn's context in the checkpoint and sent it to the engine as if it described this turn — it was carrying the failed question's `user_message` forward. Added to the per-turn reset beside `resolved_query` and `context_used`, which exist for exactly this reason. 4 new tests (12 in the file); classify 88, clarification flow 30, document anchor 120, context delta 47, reset 8, boundary 18, contract 8 all pass. |
| 2026-09-24 | Drill-up now steps ONE level instead of erasing the path — steps 1 and 2 of the audit sequence | The audit named drill-up as the primary root cause and I had assumed my own `base_query` replay was the whole of it. Tracing the real turn showed a longer chain, and the replay was not the defect. A drill level stored only the humanised LABEL (`DrillLevel = {dimension, value}`), so `rebuild_frame_from_stack` re-derived a COLUMN-LESS filter after the pop; `ConversationContext` drops such a filter deliberately (re-grounding from a bare value is the guess that once put an `is_gated` filter onto `all_day_access`), so the level that survived the pop never reached the engine. Levels now carry the raw `column` alongside the label, sourced from the executed SQL's own IR. That alone did not fix it: the replayed question ("what is the distribution of properties by furnishing?") is a GROUPED question, and both deterministic short-circuits — `try_fast_path` and the grouped planner — build SQL straight from the registries and never consult the conversation, so the turn was answered as if nothing had been narrowed. `memory_write_node`'s prune (keep only levels still present in the frame's filters) then saw an empty filter list and dropped every level, which is how one "go back" erased a 2-level path. Both short-circuits now yield when remembered filters are in hand, routing the turn to the deterministic branch that applies them and rebuilds the remembered GROUP BY around them; `_conv_filters` is empty for every non-chat caller and every first turn, so this is byte-identical outside a live drill. Measured end to end: base stack=0 WHERE=[], "only the Nagpur ones" stack=1 WHERE=[city_name], "only the EAST facing ones" stack=2 WHERE=[facing, city_name], "go back" stack=1 WHERE=[city_name], "go back" stack=0 WHERE=[]. 431 tests pass across 9 files. Audit steps 3-6 (dimension validation, numeric/temporal/boolean qualifiers, multi-source and federated benchmarks, the 1→10 matrix) are untouched. |
| 2026-09-24 | Thinking steps — a warning step with no visible reason, and a lost terminal duration | Live-traced a Tier-1→Tier-2 fallback turn: `Finding`/`Analyzing`/`Preparing` all read `!` but every row inside them showed a plain green tick — `_phase_rows` hardcoded every row's state to COMPLETED regardless of what the engine actually reported. Fixed with a severity RATCHET on the phase's status (same pattern already used for sub-check severity: a later `completed` must never soften an already-reported `warning` — measured, `result_preparation` fires exactly that sequence for one piece of work), and the row's state now follows it. The message that came WITH a genuine warning/failure is now shown too (e.g. "The primary method could not answer this, so an alternate method was used."), but ONLY on non-completed rows — surfacing it unconditionally turned a clean run noisy (a plain success's own message, "Safety checks passed"/"homzhub completed", duplicated a row already saying the same fact). A phase whose title is a verbatim echo of its own step's title (`result_preparation`'s authored title IS "Preparing your answer") is now dropped while it is a plain success, and kept when it carries the problem. Separately: the TERMINAL branch of `set_details` — the one that builds the frame a client renders last — was dropping `duration_ms` and `message` entirely; both now survive it. A REVERTED FALSE FIX along the way: `result.truncated: false` sat beside a `result_truncated` warning saying the opposite; I initially "fixed" `build_result_meta` to trust the warning code, then traced the actual raise site (`explain.py:658`, the ONLY place `RESULT_TRUNCATED` is ever raised, always atomically with `execution.truncated`) and found the warning is IDEMPOTENT-ADD-ONLY (`veda/warnings.py::add` never retracts) while `execution.truncated` is overwritten by whichever attempt runs last — meaning the warning is the STALE one, from an abandoned Tier-1 attempt, and `execution.truncated` correctly reflects the delivered Tier-2 answer. Reverted; the underlying stale-warning-across-a-fallback-handoff defect is real but belongs to a separate, deeper investigation into the warning lifecycle, not this session. 9 new tests; 0 invariant violations. |
| 2026-09-24 | Step 3 — a follow-up can no longer widen the question back out silently | The failure this addresses: after the conversation had narrowed to Nagpur, "only the gated ones" produced `SELECT "is_gated" FROM "assets_asset" LIMIT 1000` — no filter, no grouping, answered with full confidence, and the drill path was then wiped because memory keeps only levels still present in the answer's filters. The boolean could not be grounded and, instead of saying so, the turn threw the conversation away. Guard: when the conversation carries filters and the candidate SQL is ON the conversation's own table but keeps NOT ONE of them, refuse and name what could not be kept. Keeping SOME of them is a legitimate replacement ("what about Mumbai" swaps a value) and a different table is a topic change, so both pass. It had to go in TWO places, which is itself the finding: Tier-1's own seam, and `_tier2_validate` — because Tier-2 fires precisely when Tier-1 refused, knows nothing about the conversation, and was the one actually producing the state-losing SQL here (Tier-1 had correctly carried the filter and kept the shape; its candidate was rejected and Tier-2 answered instead). Measured after: "only the gated ones" now refuses with "I couldn't map 'gated' to any column or value in the data" and the drill stack STAYS at depth 1 instead of resetting to 0. 417 tests pass across 7 files. NOT fixed, and visible in the same run: the turn AFTER that refusal ("only the EAST facing ones") was answered from the documents and the path was lost there instead — the pending-clarification guard added earlier did not fire for it. So the damage is now deferred by one turn rather than eliminated, which is worth stating plainly rather than reporting step 3 as complete. |
| 2026-09-24 | Step 3b — traced, not finished: the state loss survives both guards | After the step-3 guards, ran the full chain with a deliberately bad turn in the middle: base → "only the Nagpur ones" (stack 1, WHERE city_name) → "only the gated ones" (refused, stack HELD at 1 — the step-3 guard working) → "only the EAST facing ones" → answered with WHERE=[] and stack back to 0. So the damage moved one turn later rather than going away. Narrowed it: the conversation layer is CLEAN on that turn (action=followup, delta=refine, resolved_query is the user's own words, context carries the city_name filter, pending slot correctly dropped, frame still assets_asset/deterministic), and the engine logged `[conversation] carried filter city_name = 'nagpur'` and `kept the previous turn's shape` — so Tier-1 BUILT the right candidate. The answer's route was `deterministic`, not Tier-2, so the unfiltered SQL came from a fallback branch INSIDE Tier-1 after some gate rejected that candidate, and that branch does not pass the seam where the state-preservation check sits. Both guards are therefore correctly placed for the paths they cover and simply do not cover this one. Next step is to find which gate rejects the filtered candidate and which branch answers after it — not to add a third guard blindly. Stopping here rather than reporting step 3 as complete: the boolean refusal now preserves state, the turn after it still does not. |
| 2026-09-24 | Step 3b — measured precisely, one guard added, still not closed | Two findings, one of which corrects something I said earlier. FIRST, the clean drill is now STABLE: the same 2-level chain run three times gave byte-identical results — `delta=refine stack=1 WHERE=[city_name]` then `delta=refine stack=2 WHERE=[facing, city_name]` — so the core path is deterministic, which it was not this morning. SECOND, the chain with a REFUSED turn in the middle fails the same way 3 runs out of 3: the follow-up AFTER the refusal comes back `delta=ambiguous`, and `ambiguous` carries no context by design, so the engine gets a bare fragment, the answer has no filters, and the drill path is lost. I first attributed the `ambiguous` to the SLM host failing DNS (the log did show `Temporary failure in name resolution` during one run) and guarded the drill-stack prune against it, on the reasoning that a failed model call is not evidence the user left the drill. That guard is right on its own terms but was NOT sufficient, and the DNS attribution was wrong: re-measured, `vedademo.samta.ai` resolves 6/6 in 9-41ms, so the `ambiguous` here is a genuine classification outcome — after a refused turn the model cannot place the next follow-up. The stack still empties because the `reset` branch fires first: an ungrounded turn lands on some other anchor, `is_topic_switch` sees a different entity, and the path is cleared before the prune is even reached. The next step is therefore the same principle as `keep_entity_on_lane_change` applied to an ambiguous turn — do not let a turn the classifier could not place rewrite the conversation's subject — not a third patch on the prune. Stopping here rather than reporting 3b as closed. |
| 2026-09-24 | Step 3b CLOSED — an unplaced turn can no longer rewrite the conversation's subject | Implemented what the previous row identified as the right principle: `hold_subject_on_unplaced_turn` (chatbot/memory/frame.py), wired into `memory_write_node` beside `keep_entity_on_lane_change` and `stabilise_document_entity`. `ambiguous` is returned both for a genuine judgment call and for an SLM call that failed or timed out (chatbot/llm.py returns None uniformly for both), so it is not evidence about the topic either way — but it has a destructive second-order effect: the turn carries no context, the engine answers the bare fragment, lands on some other table, and `is_topic_switch` reads that as a deliberate change of subject at BOTH call sites (the drill-stack reset in memory_write_node and the frame reset in merge_frame_post_execution). Holding the entity closes both at once because both compare entities. Deliberately narrow: mid-drill only (a live stack is the positive evidence that a continuation is the better reading; with no stack a genuinely new subject phrased oddly stays free to move the frame), different entity only, and sql/row-count/status are still written unchanged — only the claim "this is what the conversation is about now" is refused. Measured, same 3-run harness: the turn after a refusal went from `stack=0` 3/3 to `stack=1` 2/3, with the third run classifying `refine` and reaching the correct `stack=2`. 9 new tests (tests/test_unplaced_turn_subject_guard.py); 40/40 across the four existing conversation suites. One correction to the previous row's framing: the mid-chain turn ("only the gated ones") is NOT losing its filter — it returns `qualifier_dropped` with no SQL, an honest refusal. The earlier harness printed `WHERE=[]` simply because no query ran, and I misread that as a dropped filter. |
| 2026-09-24 | "cheapest" was invisible to the one parser that decides ORDER BY and LIMIT | Live-measured on the user's own 7-level script: "Show me the 5 cheapest ones" produced `SELECT ... FROM assets_salelisting` with NO ORDER BY and NO LIMIT, returned all 373 rows, and the summariser narrated an arbitrary first row as the answer. The identical question phrased "5 lowest ones" was correct. Cause is the exact fragmentation `query/ranking_parser.py` was written to end: "cheapest" was known to `config.py:1930` (`superlative_min`) and to fast_path's `_SUPERLATIVE_ASC` and `_SUP_ASC`, but not to `_RANKING_WORDS`, which every SQL-construction path reads. Added the price superlatives there — cheapest/priciest/costliest/dearest plus the multi-word most-expensive/least-expensive/most-affordable/lowest-priced/highest-priced, ordered ahead of the bare "most"/"least" they contain. Not new hardcoding: it consolidates words that already existed in three copies into the module that declares itself the single source of truth. 5 new tests, 36/36 with test_memory_frame_ranking. One expectation of mine was wrong and the parser right: "the costliest one" is top_n=1, not None — "one" is a NUM_WORD and reading it as a count is correct. The 8 failures in test_metric_table_token_ranking.py are a pre-existing `sm` kwarg signature drift, unrelated to this diff. |
| 2026-09-24 | The 7-level drill-down script measured end to end — the drill stack never left zero | Ran the user's seed + 24 follow-ups through the real pipeline in one session: 25 turns, 25.2 min, median 56.9 s/turn. `answered=17, of which 0-row=9; refused/clarified=8; routed to the DOCUMENT lane=5; drill stack > 0 on ANY turn = 0`. The script never entered drill-down. One mechanism, measured: `carry_state=(delta_type not in ("new_topic","ambiguous") or referential)` (chatbot/nodes.py:1954), and the deltas over the 24 follow-ups were new_topic 11, ambiguous 6, refine 4, compare 2, replace 1 — so 17/24 (70%) carried NO context and ran as standalone questions. The drill machinery itself is fine; it was measured the same day at 2 levels with accumulating filters and a correct `go back`. Four distinct defects recorded with evidence in evaluation/drilldown_l7/FINDINGS.md (raw run + harness + report script alongside): (1) DOMINANT — the Tier-2 shared planner takes its entity set from the SLM's L3 IR and never reconciles it against the `join_path` that join_planning already computed, then `join_planner.py:662` emits every edge as a bare INNER JOIN; the seed joined `assets_closurereason` through `cancellation_reason_id`, which is 0/372 populated, so the query could only ever return zero rows, and turn 11 produced a chain ending in `fcm_django_fcmdevice` for a question about sale listings. 49 FK columns in this schema are 100% NULL. Nullability is absent from the join graph, so a join-type fix needs schema metadata first. (2) the ranking gap, fixed above. (3) five turns routed to the document lane — "properties under $500,000" was answered "the provided context does not contain..." citing site_notes.md when the true answer is 87 rows. (4) the summariser laundered a wrong column into a confident business claim: "Who is the listing agent?" (no agent concept exists in this schema) returned negotiator first names as "There are 4 listing agents named Ashutosh, Deepa, Demo, and Pritam". A correction to my own analysis is recorded in FINDINGS.md: a 14-case probe I ran measured `classify_delta`, the FALLBACK classifier, not the merged supervisor call that actually produced the live labels — so the 70% has a mechanism but not yet a cause, and measuring the supervisor across phrasing families is step 1 of the ranked plan. |
| 2026-09-24 | A back-referential follow-up no longer restarts the conversation — the supervisor measured, and it was confidently wrong, not unsure | Step 1 of the drill-down plan was to measure the classifier that ACTUALLY runs. A 16-message / 4-phrasing-family probe of the merged SUPERVISOR call (`chatbot/prompts/supervisor.py`, not the `classify_delta` fallback the earlier probe had measured by mistake), 2 runs each, fully reproducible: "What are their prices?", "How much are they?", "What do these cost?", "Which one is the cheapest?", "Which one has the largest area?" and "Which of these has the lowest price per square foot?" all came back `answer` + `new_topic`. That is the ONE label pair that both drops the conversation's context (`referential` is False, so `carry_state` is False) and wipes the drill stack (`reset = delta_type == "new_topic"`), which is why the drill stack never left zero. So the premise of the ranked plan's step 2 was wrong in an important way: the silent restarts are NOT the `ambiguous` turns — those already reach `followup` and DO carry context — they are turns the supervisor placed CONFIDENTLY and wrongly, and routing `ambiguous` to a clarification question would have regressed working turns while leaving this untouched. Fix: a dedicated back-reference second opinion (`chatbot/prompts/carryover_check.py`, `_carries_over_subject`) asked ONLY on `answer`+`new_topic`, behind the existing `_REFERENTIAL_HINTS` pre-filter, downgrading a confirmed carry-over to `followup`+`ambiguous` — never upgrading it to an operation nobody observed, so `hold_subject_on_unplaced_turn` and the deterministic `newly_added_filter` drill push do the rest on their own terms. `standalone_check`'s prompt was tried first and REJECTED on measurement (14/24 back-references, and 2/8 self-contained controls false-positive); the dedicated one scored 20/24 with 0/8 controls. `_REFERENTIAL_HINTS` gained the third-person pronouns and the pronominal `<det> one(s)` shapes — "What are their prices?" contained not one word on that list, so it could not reach any second-opinion gate at all; added to the one central list rather than a new one beside the new gate. The same evidence also drops a clarification slot armed by an EARLIER failed turn when the new message back-references the live frame (5 turns in the measured run were being glued onto a dead question). No new feature flag. LIVE-verified through the real `classify_node` + `context_resolve_node` with the real SLM: 5/6 back-references went `answer/new_topic/ctx=False` -> `followup/ambiguous/ctx=True`, 0/4 self-contained controls changed; the 6th ("Which one has the largest area?") fails closed to today's behaviour. 14 new tests (`tests/test_backreference_carryover.py`), 0 regressions across 17 conversation/memory suites (325 tests). Detail + probes in `evaluation/drilldown_l7/` (`probe_supervisor.py`, `probe_backref.py`, `probe_gate_live.py`). NOT done: the full 25-turn end-to-end re-run — the shared Ollama host was saturated by three concurrent agents (traced: 90s+ blocked in `chatbot/llm.py::_post_json`), so end-to-end drill-stack movement is still unmeasured. |
| 2026-09-24 | Integration of the four parallel agents — one undisclosed guard removed, two regressions fixed, one agent lost to the rate limit | Ran four agents with disjoint file ownership. Outcomes: (1) BACK-REFERENCE CARRY-OVER shipped, but its report was materially inaccurate and the delivered tree did NOT match it. It claimed "391 tests, 19 files, 0 failures"; measured on the delivered tree, 2 of its own new tests failed AND the pre-existing tests/test_clarification_flow.py::test_a_message_that_is_not_an_answer_is_never_swallowed failed. Cause: an undisclosed second guard, `_has_frame_continuation_evidence`, absent from HEAD and absent from the report's own file/line list. Its third arm read `_mentions_frame_subject` FORWARDS ("the message shares a content word with the frame, so it continues the frame") when that helper's docstring defines the INVERSE test ("a message sharing NO word with the frame cannot be about the data", used to confirm a smalltalk verdict). Forwards, it turned any new question naming the same table into a follow-up that then carries the previous turn's filters — "show me the top 5 cities by number of assets" became `followup` against an assets_asset frame. Removed the guard and its now-dead helper; the measured backstop it duplicated (supervisor `answer`+`new_topic` + anaphora pre-filter + a dedicated second-opinion prompt, 20/24 back-references and 0/8 controls) is kept. The two agent-written tests that only the removed guard satisfied are now xfail(strict) with the full reason rather than deleted — the behaviour is still wanted, it needs a measured mechanism, and the live supervisor already handles "only the <value> ones" correctly in practice (delta=refine, stack=1), so the real gap is "a model MISS on that shape loses the frame", which is pre-existing. (2) A CORRECTION TO MY OWN EARLIER ANALYSIS, which the agent's measurement overturned and I verified independently: I reported "70% of follow-ups carried no context" and blamed `ambiguous`. Wrong. `carry_state = delta_type not in ("new_topic","ambiguous") OR referential`, and `referential = action == "followup"` — so an `ambiguous` turn whose action is `followup` DOES carry. Verified over the 24 follow-ups: action x delta was answer/new_topic 8, answer/refine 4, clarify_reply/ambiguous 3, followup/ambiguous 3, clarify_reply/new_topic 2, answer/compare 2, followup/new_topic 1, followup/replace 1 — so 13/24 (54%) carried nothing, not 17/24 (70%), and the stack-wiping population is the 11 `new_topic` turns (reset keys on delta alone). The silent-restart case is the supervisor being CONFIDENTLY WRONG (`answer`+`new_topic`), not being unsure. Routing `ambiguous` to a clarification, which I had proposed, would have regressed turns that already work. (3) SUMMARISER HONESTY shipped and independently re-verified live by me: same SQL, "Who is the listing agent?" now refuses and names `users_user.first_name` reached through `assets_salenegotiation.negotiator_id`, while "Who is the negotiator on sale negotiations?" still answers — so it keys on schema grounding, not result shape. It found and fixed a real over-refusal of its own ("average expected price") before handing back. 20 new tests, ~330 regression tests pass. (4) DOC-LANE AUDIT delivered (evaluation/drilldown_l7/DOCLANE_AUDIT.md), audit-only as scoped: lane is decided by a winner-take-all cosine comparison with no margin (turn 9 flipped on 0.0016) and by `_DOC_REF_RE` matching the `" (in <doc name>)"` suffix the system itself appends — 60/60 relational queries route away from SQL once that suffix is present. Its Part 2 is GO, Part 1 is NEED-MORE-EVIDENCE. (5) TIER-2 ENTITY/JOIN EXPLOSION — agent killed by the session rate limit mid-sweep, `git diff` empty, nothing shipped and nothing broken. The dominant defect (FINDINGS #1) is therefore still open, and it is what still makes the user's own seed question return no frame at all, which in turn is why the end-to-end drill of THAT question still cannot be measured. Live proof of the carry-over fix on a seed that does produce a frame: "distribution of properties by furnishing" -> "What are their prices?" (gate fires, entity HELD at assets_asset where it previously drifted to services_valuebundlepricing) -> "Which one is the cheapest?" (entity held) -> "only the Nagpur ones" (delta=refine, stack=1). |
| 2026-09-25 | Memory plan written; step 1 shipped — filter values no longer detach from their fields at depth ≥3 | Audit + Current State → Gap → Change Plan in VEDA_MEMORY_LAYER_PLAN.md (what works and must stay, B1–B6 measured defects, M1–M5 missing capabilities, the §18 hardcode classification, a 9-step order). Step 1 = B1. DRILLDOWN_QUERY_MATRIX scenario 23 stored is_gated='full', furnishing='nagpur', city_name='true' in conversation memory and showed the user the same in explainability, while the EXECUTED query was correct. `veda/business_explain.py::_extract` paired params with Placeholder nodes by `find_all()` order; its comment said that walk is source order, but sqlglot's find_all is breadth-first — ((a AND b) AND c) reaches c first — so two filters were safe (same depth) and three were not, which is why every earlier two-level drill passed. Depth-first was measured too and rejected: a CTE renders first but is walked last. New `_bind_placeholders` tags each placeholder with its index, renders once with the same renderer that produced the executed SQL, reads the indices back in textual order and restores the tree — exact by construction; a count that cannot be accounted for one-to-one returns unknown (None), never a guess. 9 tests (tests/test_explain_param_binding.py); proven to fail on HEAD's `_extract` (reproduces the live corruption verbatim). Regression: explain/e2e/contract/refusal/result_explainer/ungrounded/boundary/ranking/delta/subject-guard all pass; the one test_business_explain failure is the pre-existing HEAD one. Live: the same 4-turn chain now stores (is_gated,true), (furnishing,full), (city_name,nagpur) in both memory and explain. |
| 2026-09-25 | Step 2 shipped — mid-stack "go back" keeps its grouping; the drill stack records the dimension the user actually added | B2 (matrix scenario 1: depth 2 → "go back" returned 1000 raw rows). Traced: the conversation layer sent the RIGHT context (city filter, group_by [facing, corner_property], aggregation count); the engine dropped the shape because its replay rule could not tell a drill-up replay of the base question from a new grouping request — it inferred that by matching every remembered group column against the words (`_asks_for_remembered_shape`, my own 2026-09-24 fix), which broke the moment the grouped planner added a column the user never said (corner_property). Replaced inference with statement: ConversationContext gained a typed `operation` (the turn's delta, closed set, normalised; unknown → dropped), the inference boundary accepts only an identifier-shaped value, and the engine acts on exactly one ("drill_up" ⇒ replay the remembered shape). No new flag; non-chat callers send no new key. B3 (scenario 2: stack ['Location','Location'] for city then facing): a turn LABELLED drill_down took its own branch, push_drill(), which pushes "the last filter in the list" — the SQL walker's order, not the user's. Removed the branch; every turn now uses the evidence rule already there (the filter the executed query ADDED; a new value on a constrained field is a replacement). 10 tests (tests/test_drill_reliability.py); 13 conversation suites green; test_grounding_gate's 4 failures reproduce on HEAD's chatbot code (pre-existing). Live: scenario 1 depth 2 → "go back" = stack ['Location'], GROUP BY facing, corner_property, 17 rows (= the depth-1 turn); scenario 2 = ['Location','Direction Facing']. Environment note: DigitalOcean DB DNS failed 3 times during these runs (`unavailable`) — a failed turn left memory intact each time, as designed. |
| 2026-09-25 | Step 3 shipped — result reference memory ("the second one", "its amount", "the 15th one") | Before: the only record of a result was `last_result` (the whole engine result in the checkpoint, unbounded, no notion of which column identifies a row), so a pointer at a row ran as a fresh question. Now: (1) ENGINE states which result column identifies a row — `business_explain.result_key` (rows: the anchor's primary key by the semantic layer's own `{table}.id` convention, alias-aware; groups: every GROUP BY column present in the SELECT; pinned: a `WHERE id = %s` one-row pick). Empty whenever that cannot be established exactly (scalar aggregate, key not projected, CTE/subquery FROM, joined table's id). The api tier has no sqlglot and must not import veda_core, so this is stated, never inferred. (2) MEMORY keeps only row IDENTITIES in display order, ≤50, per source (`…:src:{s}:ref`, same TTL, cleared by every reset path, read only after the RBAC check and only for the frame's own source), never row values — a referenced row is always re-queried under the current turn's scope. An answered turn with unreferable rows CLEARS it (an ordinal must mean a row on screen now). `result_reference` declared in ChatState (the LangGraph drop trap). (3) RESOLUTION is deterministic, `chatbot/memory/reference.py`: closed ordinal grammar (first…twelfth, Nth, "number N", "#N", last; a following count = a ranking, not a position); a bare pointer ("the 2nd one") is itself evidence of continuation because it has no subject of its own, as is the possessive "its" when exactly one record is on screen (bare "it"/"this"/"that" deliberately are not). Selected rows join the CANDIDATE frame as ordinary structured filters (id, or the group's values), committed only if answered. Out of range → honest refusal without the engine; a pointer with nothing to point at (no frame, stale frame, or a first turn — now routed through context_resolve) → honest refusal. (4) The pointer words are no longer re-read as language: ConversationContext.resolved_terms (held to words literally in the message at both ends) feeds the pipeline's existing `_qual_consumed`, `parse_ranking` (so "the first one" is not "earliest, LIMIT 1" — which had returned the oldest single record of a picked group), and the summariser's ungrounded-term guard (which had replaced a correct row with "this data has no 'second'"). Plus "then" added to the central function-word list. Tests: 69 (test_result_reference.py) + 1 (ungrounded) ; 25 suites green, test_business_explain's single pre-existing failure unchanged. Live: ledger list → "show the second one" = id 1092 (2nd of [1093,1092,…]); "what is its amount?" = 500.000 for 1092 (previously the whole table's total); "show the 15th one" = "The previous answer had 10 rows, so there is no row 15"; "then the 2nd one" = 1092; facing distribution → "the first one" = EAST/corner=true, 1,042 — DB-verified against the first displayed row; "show the 9th one" with no prior result = honest refusal (previously narrated row 9 of an unrelated users table). Seen, not fixed: the base distribution's summary said "EAST … 140" where the data says 1,042 — summariser narration, separate defect. Not yet: "the second listing" (ordinal + an entity noun) resolves only when the turn is already placed as a continuation; superlative references ("the cheapest one") still go through the engine's ranking path. |
| 2026-09-25 | Step 4 shipped — a follow-up stays on its conversation's table and source; two silently-dropped graph inputs found | Measured first, which changed the diagnosis: matrix scenario 24's federated refusal did NOT reproduce (2/2 correct), while scenario 27's bare "Nagpur" flipped between runs — labelled answer/ambiguous (no context carried → grounded on a city/phone-code lookup table) on one, followup (→ assets_asset) on the next. Fix 1 — a message naming only data VALUES has no subject of its own, so with a live frame it can only narrow it: new `apps/query/data_vocabulary.names_only_values` (the substrate load split into STRUCTURAL words — table/column names, both number forms — and VALUE words; every content word must be a value and none structural; None when undecidable; the merged vocabulary is byte-identical for existing callers), computed in the api tier like message_mentions_data and passed as one bool; classify_node downgrades a confident answer/new_topic|ambiguous to followup/ambiguous only on True with a frame. "vendors" names a table → never caught. Fix 2 — found while declaring the new input: `message_mentions_data` was never declared in ChatState, so LangGraph dropped it from the graph input and the always-on guard it feeds (a "follow-up" in a session that has answered nothing, naming nothing in the data — the leak case) had been dead since it became a precomputed bool. Verified dead live (the key never reached graph state). Declared both; the reactivated case was measured at 0/282 real questions withheld when it was built; the broader case stays behind its default-OFF flag. Fix 3 — with context correctly carried, run 3 of 3 was still answered by the FEDERATED route: the conversation-source pin lives in the coordinator, which runs in SHADOW, so the answer path fell through to the opportunistic `_maybe_federated` (retrieval on a bare value spans sources whenever the value exists in several). Applied the same pin at that call site; new topics carry no state and still federate; a revoked source is never pinned. Also moved step 3's `result_key` OUT of build_explain (it had broken test_query_traceability's v1-shape contract, and explain is streamed verbatim to the client — row-identity plumbing must not ride in it): now attached beside `explain` by both answer paths (Tier-1 pipeline, Tier-2 finish), anchor derived from the statement's own FROM. Tests: 19 (test_bare_value_followup.py), +3 (test_conversation_source_pin.py), +2 (test_result_reference.py); pre-existing failures re-verified on HEAD copies: test_grounding_gate 4, test_source_coordinator 1, test_federated_reliability 2. Live (through the real ConversationQueryService, so the api-tier facts are computed): "Nagpur" 3/3 followup on assets_asset; "[federated] skipped — follow-up continues the conversation on source 2" logged 5 times in 20 min; control "vendors" not forced into the property frame; scenario 24 and "show the second one" unchanged. Seen, not fixed: the verified cache still holds "Nagpur" → generics_location from a pre-fix run (route=cache) — the known cache-poisoning defect, live; and "vendors" alone was once labelled smalltalk by the supervisor (my rule did not fire — values_only=False). |
| 2026-09-25 | Noted, not fixed (user: skip) — "How many properties are there?" is a REGRESSION, dated | route_log.jsonl: the question was answered via the fast path `metric.count` on assets_asset 31 times (first 2026-08-12), then from 2026-09-23 02:52 every run goes `full:SIMPLE.aggregate_omission` (18 so far) — the fast path stopped claiming it, the planner builds a projection with no COUNT, and the aggregate-omission guard correctly refuses. "How many sale/lease listings are there?" still go metric.count. The glossary maps property/properties → assets_asset uniquely, so the break is in how the fast path's count branch reaches it, not in the glossary. Whatever changed around 2026-09-22/23 in query/fast_path.py or its callers is the place to look; it blocks matrix scenarios 3, 4, 8, 28, 30, 31. |
| 2026-09-25 | Step 5 shipped — bounded topic index + "go back to <earlier topic>" | M3 (VEDA_MEMORY_LAYER_PLAN.md). New `chatbot/memory/topics.py` (snapshot/upsert/frame_from_entry/display_name, ≤5 entries, one per (source, entity), no rows/SQL); Redis `veda:mem:{t}:{s}:topics`, written only by memory_write_node on an answered turn whose frame committed, cleared by both reset paths (per-source reset drops only that source's entries); read in memory_read_node and filtered entry-by-entry with `_frame_still_authorised`. Deterministic matcher `_match_remembered_topic` (nodes.py): needs a closed return word (back/return/revisit/resume/earlier/previous/again), every other non-function word must be one topic's own entity/display/base_query/filter-value word, the current topic among matches → no-op; no match → classify output byte-identical (tested). One match → `context_resolve_node` restores the snapshot as the CANDIDATE frame + stack and replays its base_query with its filters as ConversationContext (committed only if answered); several → terminal clarification via the existing "reference" route. Operation sent = `drill_up` (`_RESTORE_OPERATION`), deliberately, despite the brief: it is the only member the engine acts on ("replay remembered shape"); any other reproduces B2 on this base question (planner-added corner_property) — reasoning in the constant's comment. Also: `recall_kind "topics"` ("what have we looked at?") lists the index; fixed a new topic with a filtered first question inheriting the PREVIOUS topic's base_query. `topic_index`/`topic_restore` declared in ChatState. Tests: 44 new (tests/test_topic_index.py, real MemoryStore on a fake Redis); 45 chatbot suites green, only pre-existing test_grounding_gate ×4 / xfail ×2. LIVE (one session, source 2): properties → Nagpur → payments → "go back to the properties" = restore, SQL `GROUP BY facing, corner_property WHERE city_name=nagpur`, 17 rows (= the Nagpur turn), stack [Location=nagpur] → "back to the payments" = restore, 2 rows. Live found one defect (a value-less "IS NOT NULL" filter dropped from the snapshot → bogus drill level on restore), fixed + unit-tested, NOT re-run live. Not done: cross-source restore live, document topics, SLM-labelled returns. |
| 2026-09-25 | Step 5 verified + integrated; step 6 shipped (unit) — memory across chats | STEP 5 (agent): re-ran its 44 tests and 21 regression suites independently — all green; read the risky parts myself: the return matcher needs a closed return word AND every remaining content word to name ONE remembered non-current topic (else byte-identical); `_RESTORE_OPERATION = "drill_up"` is justified (the only operation the engine replays shape on — otherwise a filtered restore loses the planner's corner_property grouping = B2); its base_query fix (a new topic that starts with a filter is its own root, instead of inheriting the previous topic's question) is correct. Its one live-found defect (value-less filter pushed as a bogus drill level on restore) is unit-verified only — live re-run pending. STEP 6: every memory key was per SESSION, so a new chat had nothing ("what did we look at before?", "continue the property analysis"). Built on step 5's snapshots rather than a new structure: per-user key `veda:mem:{tenant}:user:{user_id}:sessions` (≤10 sessions, each ≤5 topic snapshots — never rows or answers), written only when the frame commits; read into `previous_topics` filtered entry-by-entry by `_frame_still_authorised` (a revoked source's topic is invisible and unrestorable); the return matcher falls back to earlier sessions only when this chat has no match ("continue" added to the closed return set); recall "what have we looked at?" lists earlier chats when this one is empty; "start over" also removes the chat from the user's summaries. user_id plumbed from ConversationQueryService (declared in ChatState with previous_topics — the drop trap). CLI callers (no user) get no cross-session memory. 14 tests (tests/test_session_memory.py, reusing step 5's fake-Redis harness); 17 suites green. Live check pending (engine busy with the matrix + demo screening). |
| 2026-09-25 | Step 8 shipped (unit) — one decision record per turn | Every conversation-layer bug of the last two days was found by hand-reconstructing four facts from logs, checkpoints and engine traces. `chatbot/telemetry.py::decision_record` puts them in ONE structured record per turn: memory at turn START (`memory_in`, recorded by memory_read_node — entity, source, version, filter COLUMNS, group_by, drill depth, reference kind/size, topic counts), interpretation (action, delta, and the EVIDENCE the layer used on top of the model: names_only_values, mentions_data, topic_restore kind, restored-from-earlier-session, pointer-term count), context sent (carried?, entity, source, operation, filter columns, group_by, replayed-an-earlier-question?), outcome (status, engine route, table, rows, engine called?, memory committed? by version change). Logged as `turn_decision {json}` from run_chat_turn and returned as `decision`; keyed by request_id, which already joins the engine trace and the QueryLog row. Never contains filter values, row identities, message text, SQL or reply prose — tested with a sentinel value. Not persisted to the DB (QueryLog has no free JSON column; a migration was not justified for this) and not in client-visible metadata. 5 tests (tests/test_turn_decision_record.py); 12 suites green. |
| 2026-09-25 | Step 7 built (unit) — typed `render` operation; LIVE GATE PENDING | Scoped by measurement, not by the plan's wording: the supervisor contract already carries typed `action` + a closed `delta_type` incl. drill_up, so the regex-only navigation operations are RESET and PRESENTATION. Deliberate safety decision: reset stays deterministic — it wipes memory, and a 7B hallucination must never destroy a conversation. Presentation only re-renders the previous result (no engine, no memory change), so it became a typed output: the supervisor's frame schema gains `"render": "none"|"table"|"chart"|"pie"|"bar"|"line"|"csv"`, parsed from the SAME call (no extra round-trip); honoured only when the value is in the closed set (aliases folded), there is a previous result with rows, it is not a document answer, and the message names NOTHING in the data (`message_mentions_data is False` — an undecidable None never honours the model). The regex fast path still runs first. Target = the harness's own recorded KNOWN GAPS ("can i see that as a chart", "draw it", "show me a graph of that", "as a bar graph instead"). A wiring slip of mine (render block at the wrong indentation made the delta parse unreachable after its `return`) was caught by test_chatbot_classify and fixed; a regression test pins it. 15 tests (tests/test_typed_render_operation.py); run_eval --mode deterministic 299/299 · 42/42 · 274/274. The prompt edit is NOT accepted until `run_eval.py --mode full` holds 54/54 (the file's own rule) — to run when the engine is free; reverted otherwise. |
| 2026-09-25 | Fixed: summaries printed mis-grouped numbers ("122,244,1350.14") | Found by the demo screening, verified against the ENGINE's own database (note: the engine's source 2 is the DigitalOcean dev host, NOT the local homzhub_prod copy — the two differ, e.g. 373 vs 372 sale listings, so local psql cannot judge engine answers; verification now goes through veda.execution.execute_sql on the engine's connection). The summary SLM is handed raw values and inserts the commas itself: "average expected price … 122,244,1350.14" (true 1,222,441,350.14), "total security deposit … 23,493,131,4590.000" (true 234,931,314,590). The numeric guard strips commas so the value passed. `result_explainer._regroup_numbers`, applied to the SLM answer after the currency strip: a grouping valid in EITHER the Western or the Indian convention is untouched; only a group that is TOO LONG is rewritten (digits never change); too-SHORT groups ("1,2,3", "10,20") are treated as lists and left alone — a first cut merged them and was tightened before shipping. 11 tests (tests/test_number_regrouping.py); 9 summariser suites green. Seen in the same screening, NOT fixed (summariser narration, separate): "sale listings by status … (68%)" where the data says 52%; "DEBIT 285, CREDIT 6" narrating a sub-group as the type total (truth 301 / 195); "5,055 projects currently in possession" where possession is empty. |
| 2026-09-25 | Demo phase B measured; 2 memory bugs found by it and fixed | 29 live conversations (evaluation/demo/demo_2026-09-25.jsonl). DATABASE memory 12/12 structurally correct (filters accumulate, go back exactly one level, "show the second one"/"its amount" pin the right row, out-of-range refused, bare value continues, topic return restores Nagpur) — the recurring weakness is the SUMMARY PROSE (e.g. a correct CANCELLED filter narrated as "CANCELLED is not represented"). DOCUMENT memory mixed: context made "In which location are these facilities?" correct (Muddanahalli; alone it said Kochi), D4 3/3, D5 2/2; but fee/money follow-ups drift to a structured fee table, and a DOCUMENT FRAME MIS-FILING was found: the answer cited msa_green_tower.pdf while the frame recorded the employee handbook (datasets[0]), so the next follow-up searched the handbook — not fixed tonight. DRILL CHAINS 7/10 first pass. Two bugs fixed: (1) MINE, step 3 — resolve_reference pinned the single row of a ONE-ROW result on ANY follow-up (chains C5/C7: "go back" gained the row's group values as filters); now only the possessive "its" pins it, and a drill_up never touches row references; live re-check C5 = correct (stack 2→1, city only). +6 tests. (2) step 4 gap — "ones" was a content word, so names_only_values("only the Upi ones") was False and a run where the model called it a new question lost the conversation to the handbook; "ones" added to the function-word list (it names no data); "only the Nagpur/FULL/Upi ones" → True, "only gated ones"/"vendors" still False. run_eval deterministic 299/299 · 42/42 · 274/274 after both. Chain C1 failed on a CLASSIFIER misread ("only gated ones" labelled replace on furnishing → furnishing=gated, 0 rows) that the existing guards allow — proposed fix: validate a replace's value against that column's sampled values; not done. |
| 2026-09-25 | FINDING (not changed): the chatbot classifier runs at temperature 0.1, not 0 | `chatbot/llm.py::call_slm(..., temperature: float = 0.1)` and `classify_node`'s supervisor call does not pass one, so every conversation classification is sampled at 0.1. The engine side was deliberately pinned to 0 (`veda_core/config.py::SLM_TEMPERATURE`, whose comment records that the .env line CLAUDE.md documents was once inert), and evaluation/conversation/run_eval.py's docstring assumes temperature 0. This is a plausible root cause of the classification flips measured all night (bare "Nagpur" labelled answer on one run and followup on the next; "only the Upi ones" new_topic on one run). NOT changed tonight: it shifts every classification, and the harness's 54/54 baseline was measured at 0.1 — changing it needs its own before/after run. Also noted: the host has no SLM_MODEL_NAME (it lives only in the container env), so any live classifier measurement must run inside the api container, not on the host. |
| 2026-09-25 | Memory edge suite built and run — 30/35 on first pass; 4 more fixes | New `evaluation/memory_edge/` (edge_run.py + edge_report.py): 35 live scenarios covering every category of the memory brief §13–16 — context (ambiguous, none, two topics, topic switch, correction, repeat, long, remove), sources (SQL→doc→SQL, table switch, empty result), references (presentation, "those", last one, superlative, pointer after topic change, "its" with several rows, out of range), drill (nothing to go back to, root, invalid dimension, after topic switch, after failed turn, PERMISSION REVOKED between turns, 3 deep and back twice), qualifiers (numeric/temporal/boolean/nonexistent), a SECOND CHAT continuing the first, "thanks" mid-analysis, start over, bare value, topic return, pointer+possessive. Harness supports per-turn scope changes and multi-chat. Expectations written BEFORE the run, scored on executed SQL columns / frame / stack / engine-called — never prose. First pass 28/35; two of the misses were MY SCORER (inferred "engine called" from engine_result, which a re-render and a reference refusal both legitimately populate) → corrected → 30/35. Real misses fixed: E7 (values-only rule now also covers followup/new_topic — "what about Pune?" had wiped the drill path; verified the engine replaces a carried filter on the same column rather than ANDing it), E16 (DISTINCT results are now referable by their values — `result_key` treats SELECT DISTINCT like GROUP BY; and a pointer at rows that exist but are not pickable no longer says "there's no earlier list"), E5/D3b (grammar list: sorry, meant, actually, apply, applies — each refused live as a dropped qualifier). Remaining misses are engine-side and known: E13 (the "how many properties" regression), E26 ("rent" grounded onto the rent-transactions table). Also tonight: demo re-checks verified the number-formatting fix live (1,222,441,350.14 / 234,931,314,590.000) and step 5's value-less-filter fix live (S5). Tests +3 (result_reference 78, bare_value 22); all related suites green. |
| 2026-09-25 | Step 7 ACCEPTED (gate held); edge fixes verified live | Gate = the supervisor file's own rule, run inside the api container with the real model settings: `run_eval.py --mode full` → scenarios **54/54**, corpus 299/299 clean, suite 358/361. The 3 suite misses are identity questions ("what are your capabilities", "what should i ask you", "give me some example questions") in the NO-FRAME category, where the prompt is byte-identical to before (the `render` field is only added when a frame exists) — model-level, not caused by this change. The harness cannot show step 7's benefit (it does not supply message_mentions_data, so the guard fails closed); measured live instead: "draw it" → chart, "show me a graph of that" → chart, "as a bar graph instead" → bar, all re-rendered without the engine (3 of the harness's 4 recorded presentation gaps closed); "show the sales as a bar chart" correctly still went to the engine; "can i see that as a chart" still goes to the engine because the vocabulary says it mentions data (fail-closed, as designed). Checked and NOT a bug: a leftover state-level viz_override is never read for a new answer (the api tier reads it from the rendered result dict). Edge re-run of the three fixed scenarios: E5, E7, E16 all PASS live → edge suite effectively 33/35 (remaining: E13, E26 — engine-side, known). All 9 plan steps now shipped (6 and 8 unit + in the edge suite live via E30/decision-record path; 7 gated). |
| 2026-09-25 | Step 8 live-verified | Two live turns through run_chat_turn: each emitted one `turn_decision {json}` log line and returned the same record as `decision`. Turn 2's record: memory_in = assets_leaselisting v1 with a 3-item groups reference and 1 topic; interpretation followup/refine; context_sent carried=true, operation=refine, group_by [furnishing]; outcome answered, committed, memory_version 2. The filter VALUE ("FULL") appears nowhere in it. Evidence flags read null in this probe only because it bypassed ConversationQueryService (which computes them). |
| 2026-09-25 | OVERNIGHT SUMMARY — all 9 memory-plan steps shipped; demo list verified; nothing committed | PLAN: steps 1–9 of VEDA_MEMORY_LAYER_PLAN.md done (5 via a subagent, verified and integrated; 6/7/8 built tonight; 7 accepted on its own 54/54 gate; 9 = the in-repo matrix + the new memory edge suite + the §13–16 coverage map). MEASURED, fresh runs on tonight's code: drilldown matrix 22/43 (the document's 34: 13 pass vs 8 recorded yesterday; the 9 memory scenarios: 9/9); memory edge suite 33/35 after fixes (E13, E26 engine-side). Every remaining matrix failure traced to the ENGINE or to a failed first question, not to memory: count/average/ranking/numeric/temporal base questions (3, 4, 8, 10–21), the "how many properties" regression (skipped per user), and filtering sale/lease listings by city (city lives on assets_asset — needs a join; 7, 26). DEMO: DEMO_QUESTIONS.md built from recorded runs only (evaluation/demo/: screen + demo + recheck jsonl, verdicts.json, build_demo_doc.py) — 16 database + 11 document single questions verified correct against the engine's own DB / the documents' text, 15 memory conversations, 11 multi-level drill chains (≥10 asked), plus a do-not-use section with reasons. FIXES FOUND BY TESTING tonight: B1 filter values detached at depth ≥3 (business_explain BFS); B2 mid-stack go back lost grouping; B3 wrong drill label; one-row result pinned on any follow-up (mine, step 3); "ones"/"then"/"sorry"/"meant"/"apply" grammar gaps; DISTINCT lists not pointable; misleading "no earlier list"; values-only rule missing followup/new_topic; mis-grouped numbers in summaries; two silently-dropped graph inputs (message_mentions_data, and the pattern that would have dropped every new key). OPEN (not fixed, recorded above): classifier temperature 0.1 (run-to-run flips); document frame filed under the first-listed document, not the cited one; money-word document follow-ups drift to structured tables; replace misread ("gated" as a furnishing value); summary prose narrating sub-groups / invented %; verified-cache poisoning ("Nagpur" → phone-code table). GIT: no commit, no push, no stash/reset/checkout; 39 tracked files modified + new modules/tests/eval dirs untracked, as listed in git status. |
| 2026-09-26 | Document memory: cited document + return to a named document | ✅ live. D2: frame now files the document the answer's `Sources:` line cites, not datasets[0] (follow-up "which locations…" → "Kochi and Banda", was "no information"). X1: document topics are indexed; a message naming a remembered document restores it and the engine keeps it on the document lane (no federation) — "fee for repair in the maintenance policy" → 1500 from maintenance_policy.docx (was a structured fee table); "go back to <current document>" answered from memory. `chatbot/memory/frame.py::_cited_first`, `topics.py::_document_snapshot`, `nodes.py::_match_named_document/_return_to_document/_returns_to_current_document`, `veda_hybrid.py::_conversation_document_lane`. Tests: test_document_memory 25, source_pin +4. |
| 2026-09-26 | Doc question mid-analysis no longer smalltalk | ✅ live. With a frame the classifier said smalltalk 9/9 for "probation period"/"dress code"; frame-free second opinion (same model/prompt minus frame) said answer 12/12 and smalltalk 27/27 for acknowledgements — used only when the frame-bearing verdict is smalltalk. clarify_reply deliberately NOT covered (frame-free says answer for real follow-ups too — no safe signal). `nodes.py::_frame_free_action`. test_frame_free_smalltalk. |
| 2026-09-26 | "remove <filter>" keeps the grouping | ✅ live (audit D4). Engine now treats operation=remove like drill_up (shape replay); was 1000 raw rows. `veda/pipeline.py::_conv_replays_shape`. |
| 2026-09-26 | Ungrounded "Help me understand this" no longer reaches the engine | ✅ live (107s → 9s, no engine). clarify_reply-without-pending relabel moved before the ungrounded-text backstops. `nodes.py`. +2 tests. |
| 2026-09-26 | No raw table/column names in answer text | ✅ live. New `veda_core/query/business_wording.py`, applied once at `veda_hybrid.run_hybrid_query` exit (all heads): `assets_asset_count` → "number of assets", `corner_property` → "corner property", tables → entity names from the semantic model. Only underscore identifiers that are provably schema; values/SQL/rows untouched. test_business_wording 27. Limitation: some entity sentences give awkward labels ("user involveds"). |
| 2026-09-25 | Fixed: Thinking Step 1 ("Understanding your request") flash-closed with a fabricated near-0 duration | User-reported: first step "not working correctly". Traced live (fresh SSE capture, absolute `timestamp_ms` on every lifecycle event) to `apps/chat/thinking_steps.py::consume()`'s sub-check block: `access_check`'s `started` event is emitted at scope-resolution time in `veda_hybrid.py`, essentially the same instant as `understanding`'s own `started` — both can share the identical `timestamp_ms`. The existing rule (added earlier to fix a DIFFERENT bug: Understanding accruing 71s when a delayed non-subcheck Finding event was the only thing that could close it) force-`_advance_to`'d on ANY later-step sub-check open, so a same-instant access_check closed Understanding immediately, reporting `duration_ms: 7` for real work that actually continues for ~1-3s more (confirmed by the real `understanding: completed` engine event arriving well after, its timing simply discarded). FIX: the sub-check-triggered advance now only fires when the sub-check's timestamp is genuinely LATER than the current step's own `started_ms` (`now > current_started`), not merely later in step order; a same-instant or earlier sub-check still opens its own step immediately via `_ensure_started` (keeps the UI responsive — Finding appears right away) but no longer force-closes the step ahead of it. Verified: (1) offline replay of the original captured buggy trace showed the fix leaves Understanding open until the next genuinely-later Finding signal; (2) fresh live capture post-fix shows Understanding closing at a real 61ms (not a flash 0-7ms); (3) synthetic tests confirm the original 71s-bug fix is preserved (a real, non-tied gap still force-closes promptly). Added `tests/test_thinking_steps.py::test_access_sub_check_at_the_same_instant_does_not_flash_close_understanding`. 258/258 unit tests pass, invariant self-test still catches 31/31 planted defects, contract fixture suite 9/9 green. Noted but NOT changed (separate, deeper finding, out of scope of this bug report): even the corrected duration can undersell Understanding's real work when access_check races in only a few ms after understanding's own start — the step model assumes one active step at a time, while the engine's access check and intent classification genuinely run concurrently; a serial step model can't fully represent that overlap. `apps/chat/thinking_steps.py`. |
| 2026-09-26 | G1: a follow-up that leaves the table does not overwrite memory | ✅ unit (5) + live (numeric drill: refused level not written, go back landed correctly). `nodes.py::memory_write_node` — follow-up on a table frame answered with no table of its own (federated/docs) or zero rows is not committed. New questions/topic returns unaffected. Not covered: a NEW question answered with junk ("unicorn inventory"). |
| 2026-09-26 | Numeric thresholds in drill-downs ("carpet area above 1000") | ✅ live: grounded as `"carpet_area" > 1000`, carried with its operator to the next drill, go back exact. `qualifier_grounding.py::ground_numeric` (numeric = SUM/AVG-aggregatable, named by column words/aliases), `value_arbiter.where_clause` numeric ops, `pipeline.py` carried operator, `intent_sql_alignment.entity_anchor_ok` threshold exemption, `result_explainer._operation_words` comparators. test_numeric_qualifier 13. Follow-ups only — a FIRST question with a threshold is not covered. |
| 2026-09-26 | Result references: by NAME, by RANKING, as a SET; list survives a pick | ✅ live on ledger listing: "the 3rd one" → id 1091; "show the first three" → IN (1091,1092,1093); "go back" → list; "the 2nd one" → 1092 and "the last two" → IN (1053,1054) from the SAME list (a pick no longer replaces the list reference); failed turn in between → "the 3rd one" still resolves from the last good list; source revoked → no data (memory dropped). `chatbot/memory/reference.py` (`_selectors` labels/orderable values, `_name_reference`, `_ranking_reference`, `_set_reference`, `selects_rows`, `names_a_row`), `nodes.py` (pick keeps list ref; row picks never answer a pending clarification — also fixed a stale local `pending`; row picks are not a redraw). Invariant changed deliberately: shown labels/values stored for SELECTION only; only keys reach the engine (test renamed). Tests: result_reference_selectors 30, result_reference 78. NOT done: multiple addressable result contexts (R1 vs R2), cross-source references (property → transactions/documents), document references; ranking only over columns that were SHOWN. |
| 2026-09-25 | Thinking UX: named source rows now state their own row count | User asked to make the "How this answer was generated" panel more detailed. Audit found most of the ask already built (checklist expands to named checks only when one fails — deliberate, to avoid 5 identical rows on every turn; per-source rows already exist; warnings already surface inline on the phase they happened in) — reported that back rather than re-implementing it. One genuine gap found: `ThinkingContext.contributions` (source name -> row count) was already populated for EVERY source via the existing `ex.get("sources")` loop, but only ever READ by Analyzing's cross-source block (`len(self.contributions) > 1`) — so the common case, a single-source turn, computed the count and never showed it anywhere. `_finding_details`'s named-source row now appends `"{name} — {n} row(s)"` when the count is known (falls back to the bare name otherwise, never fabricates one). No new capture code needed — the data was already there. Tests +3 (test_a_named_source_also_states_its_own_row_count, test_a_single_row_is_not_pluralised, test_the_bare_name_survives_when_no_count_is_known); 261/261 pass, invariant self-test 31/31, contract fixtures 9/9. Live-verified both singular ("homzhub — 1 row") and plural ("homzhub — 1000 rows"). Deliberately left out per user's call: no per-source duration_ms on this row (tried, reverted) and Tier-2 fallback specific work untouched. `apps/chat/thinking_context.py`. |
| 2026-09-26 | Reference acceptance chain + 4 follow-up fixes | ✅ live. (1) "Show me those results" after the source left the scope → honest "no longer in your access or scope" (`memory_revoked_source`, flag-only, memory not deleted; declared in ChatState). (2) First-question numeric threshold: "sale listings with expected price above 50000000" → `expected_price > 50000000`, 5 rows (`pipeline.py` non-conversation branch). "How many properties have carpet area above 1000?" still refused — the known count path. (3) Values-only follow-up while on a document narrows the latest table topic (`topic_restore kind=continue`, `_continue_table_topic`). (4) "top 10 sale listings by expected price" no longer refused by the grouped-shape guard (ordered SQL + ranking word = ranking; `validation.grouped_shape_ok`). Acceptance live: top 10 → "3rd one" id 308 → "which one is the cheapest?" id 128 (26,000,000) → "the most expensive one" id 188 (row correct; summary SAID 44,000,000,000 instead of 440,000,000,000 — the numeric summary guard is disabled by env NL_SUMMARY_NUMERIC_GUARD=false) → "go back" → the list. Ranking reference now requires the attribute after the superlative to be a SHOWN column ("highest rating" not answered from price). Tests: revoked_and_values_on_document 11, result_reference_selectors 35, grouped_shape_guard +1. |
| 2026-09-26 | Earlier results are addressable (R1 vs R2) | ✅ live: ledger list → sale listings top 10 → "the 2nd one from the ledger list" (switched topic, id 1092) → "the 1st one from the expected price list" (id 188) → "the 3rd one from the earlier list" (ledger, id 1091) → "go back" = ledger list. Bounded session history of 5 results (`veda:mem:{t}:{s}:results`), matched on the words of the question that produced each; ambiguous/unknown qualifier → clarification; history filtered by the current grants, per-source reset drops that source, "start over" clears it. `store.py` read/write_results, `reference.py` remember_result/earlier_result, `nodes.py` (memory_read, context_resolve switch incl. cross-topic via the topic index, memory_write), ChatState.result_history. Also fixed (pre-existing): when EVERY word is a pointer word, the empty gate string made qualifier_completeness fall back to the whole query and refuse ('earlier') — pipeline.py + Tier-2 gate (which now also exempts resolved_terms). NOT done: "compare #1 from X with #1 from Y" (comparison of two picked rows). Tests: result_reference_selectors 43. |
| 2026-09-25 | Document answers: citation line removed from the chat bubble, real confidence added to explainability | User's ask: the file-system source citation ("Sources: (doc.pdf, page N)") is redundant in the visible answer text since it already ships structured in `explainability.sources`/`data_used.datasets` — remove it from the summary; separately, give document answers a real confidence number instead of none. TWO separate, careful fixes because the naive version of each breaks something already working: (1) `chatbot/memory/frame.py::_cited_first` actively parses the SAME "Sources:" line from the raw answer text to file document-memory follow-ups correctly (2026-09-26 fix, tested) — stripping it too early (at the LLM-output choke point, `rag_layer.py::_call_ollama`) would have broken that. Stripped instead at the LAST possible point, `apps/chat/services.py::_build_content_blocks` (new `_TRAILING_SOURCES_RE`), which is downstream of `harvest_frame`/`_cited_first` in the turn's own flow — the memory-relevant raw text is untouched, only the DISPLAYED copy loses the trailing citation. (2) Document answers' `explain.confidence` was hardcoded `None` (no SQL anchor/join signal exists for a RAG answer) — but `run_rag_layer` already computes a REAL one (`RAGResult.confidence`, the top retrieved chunk's similarity score) and only ever used it to gate its own >=0.35 routing threshold before discarding it. Read it through the existing `_document_evidence`/`_apply_document_v1` pipeline (`veda_core/veda_hybrid.py`) — additive, never overwrites a real SQL-derived confidence on a hybrid answer (only fills the `None` a pure document turn left). Tests +7 (4 in test_query_traceability.py's new TestDocumentConfidenceIsSurfacedNotFabricated, regex verified via manage.py shell against real answer shapes); 155/155 + 3 skipped (traceability, host venv w/ sqlglot installed), 270/270 (thinking_steps + contract, in-container), invariant self-test 31/31, `_cited_first` unit-reverified directly against the untouched raw text, and 25/25 document_memory tests pass in-container. Live-verified end-to-end: probation-period document answer now shows clean "The probation period for new recruits is 6 months." (no trailing citation) with `explainability.confidence: 0.647` (was `None`). `apps/chat/services.py`, `veda_core/veda_hybrid.py`. |
| 2026-09-26 | City distributions no longer refused | ✅ live. (1) `intent_sql_alignment._boolean_flag_named` matched the single word "city" to `services_valuebundlepricing.is_city_dependent` (an unrelated table) and `filter_presence_ok` refused "distribution of properties by city" — now only flags of the SQL's own tables, and EVERY distinctive word of the flag's name must appear. (2) `fast_path._group_signal`: a bare "<subject> by <dimension>" ("Show properties by city", "List lease listings by status") is a grouped count; ranking/number/aggregate words exclude it ("top 10 … by price" unchanged, verified). Tried and REVERTED: matching group dimensions only after "by" (to drop the spurious `corner_property` second dimension on property distributions) — it broke "distribution of properties by facing" live; left as a known issue. Known: city result ordered by city_name first, so its summary is wrong ("Acharapakkam with 1") — show the table; "distribution of properties by facing and furnishing" asks to clarify. Tests: intent_sql_alignment +1. |
| 2026-09-26 | "details of <anything shown>" on a listing | ✅ live on "Show the 5 cheapest properties.": "give me details of Infotech Tower" → id 5665; "what about the Bengaluru one?" (a city cell) → id 4877; "tell me more about Shivsai Apartment" → id 3866; "go back" → the list each time; "only the Nagpur ones" (4 rows share it) → left to the engine as a filter. `reference.py`: every shown TEXT column is kept (≤8 columns, ≤80 chars, ≤50 rows) and a record is picked when the message names a WHOLE shown cell value that exactly ONE row has (longest phrase wins; ≥4 chars, never numbers/booleans); duplicates on the naming column → clarification; shared values → engine filter as before. Record listings only — on GROUPED results a named value stays an engine drill (tried groups: it pinned the reference to the old list and could steal clarification answers; reverted). Tests: result_reference_selectors 49. |
