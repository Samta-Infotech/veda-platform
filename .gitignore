# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/staticfiles/
/media/

# Environment / secrets
.env
.env.*
!.env.example
docker/userlist.txt

# Model weights & heavy artifacts — belong on a mounted volume (plan §9, §6.5)
# (kept out of source control; mount at deploy time)
model_cache/
*.safetensors
*.gguf
veda_core/ingestion/client_bge/model.safetensors

# OS
.DS_Store

# Migration/eval generated output
evaluation/results/*.html
evaluation/results/*.json

# Derived ingestion artifacts — regenerated every ingest, never in source control
# (semantic model, graphs, glossary/synonyms/concepts, precompute indexes, pkls,
# per-(tenant,source,version) artifact scope trees). Plan §3.1 / Track 1 delta.
veda_core/data/
veda_core/schema/*.pkl
parity_baseline.json

# AI Related
.omc